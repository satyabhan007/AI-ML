/* AI-ML Learn — course data. window.CHAPTERS = 8 chapters x 5 levels each. */
window.CHAPTERS = [

{ num: 1, emoji: '⚙️', title: 'Autograd — the engine of learning', layer: 'Foundation',
  tagline: 'How a neural network actually improves: forward, backward, update. Every framework you know runs on this one idea.',
  apps: ['📸 Instagram face tagging', '🚗 Tesla lane-keeping', '💳 fraud detection'],
  levels: [
  { html: '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>Learning to shower is trial and error: too hot → turn the knob slightly cold; too cold → slightly hot. A neural network does the same with millions of "knobs" (weights): guess, measure how wrong you were, nudge every knob in the direction that reduces the error.</p></div>' +
    '<p>A neural network is a math function with adjustable <b>weights</b>. Training: make a guess (<b>forward pass</b>), score wrongness with a <b>loss</b>, then adjust each weight slightly so the next guess is less wrong. Repeat a few million times → ChatGPT.</p>' +
    '<div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>Instagram</b> tagging your friend: the model learned which pixels mean "her face" by nudging weights after millions of wrong guesses. <b>Gmail spam</b> retrains the same way each time you mark junk.</p></div>',
    try: [['🔬 Watch weights learn in the Playground', '../lab/playground.html', 'p']] },
  { html: '<p>The magic question: <i>every weight influenced the loss in a tangled way — how much blame does each one get?</i> The answer is the <b>chain rule</b>, applied automatically. Automatic blame assignment = <b>backpropagation</b>; the machinery doing it = <b>autograd</b>.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>Dominoes falling in reverse: if the last domino (the loss) misses the target, walk backwards along the chain asking each domino "how much did <i>you</i> contribute?" Every weight gets its personal blame score — the <b>gradient</b>.</p></div>' +
    '<p>Concretely: for <code class="inline">y = x·w + b</code>, if loss rises 2 units when <code class="inline">w</code> rises 1 unit, the gradient of the loss w.r.t. <code class="inline">w</code> is 2. Autograd does this for <i>every</i> weight simultaneously, at any depth.</p>',
    try: [['📖 Build a Value class (micrograd)', 'https://github.com/satyabhan007/AI-ML#readme', 'o']] },
  { html: '<p>The whole idea in ~15 lines — a scalar autograd engine, a nano-PyTorch:</p>' +
    '<pre><code>class Value:\n    def __init__(self, data, children=()):\n        self.data = data\n        self.grad = 0.0            # blame score\n        self._prev = set(children)\n    def __mul__(self, other):\n        out = Value(self.data * other.data, (self, other))\n        def _backward():\n            self.grad  += other.data * out.grad\n            other.grad += self.data  * out.grad\n        out._backward = _backward\n        return out\n\na, b = Value(3.0), Value(2.0)\nc = a * b          # forward pass\nc.grad = 1.0       # dLoss/dc = 1\nc._backward()      # blame flows back\nprint(a.grad)      # 2.0  ✔ chain rule</code></pre>' +
    '<p>PyTorch, TensorFlow and JAX are exactly this pattern scaled to billions of values on GPUs.</p>',
    try: [['🔬 Train a live network (LR = shower knob)', '../lab/playground.html', 'p']] },
  { html: '<p>Now the full loop: forward → loss → backprop → update, repeated.</p>',
    try: [['🔬 Live loss curve + LR slider', '../lab/playground.html', 'p']] },
  { html: '<p>Under the hood, autograd builds a <b>computational graph</b> as you run forward — this is <b>reverse-mode differentiation</b>.</p>',
    try: [['📖 Read: reverse-mode vs forward-mode', 'https://github.com/satyabhan007/AI-ML#readme', 'o']] } ],
  quiz: [
    { q: 'What does backpropagation compute for each weight?', opts: ['Its new value', 'How much that weight contributed to the error (its gradient)', 'Whether to delete it', 'The next activation'], ok: 1, why: 'Backprop applies the chain rule backwards so each weight gets a gradient — a blame score the optimizer uses to update it.' },
    { q: 'In c = a * b with c.grad = 1.0 and b.data = 2.0, why is a.grad 2.0?', opts: ['a.data × 1.0', 'Chain rule: d(c)/d(a) = b, so a.grad = 2.0 × 1.0', 'a was doubled', 'Set manually'], ok: 1, why: 'Upstream gradient × local derivative: 1.0 × b.data = 2.0. The chain rule in one line.' },
    { q: 'Why reverse-mode over forward-mode?', opts: ['Easier code', 'One backward pass gives gradients for ALL parameters — essential at billion-weight scale', 'Forward-mode skips chain rule', 'Only GPUs support it'], ok: 1, why: 'Forward-mode needs one pass per parameter. Reverse-mode gets every gradient in a single backward sweep.' } ]
},
{ num: 2, emoji: '🔤', title: 'Tokenization — text becomes numbers', layer: 'Foundation',
  tagline: 'Models never see words — only integer IDs. How raw text is sliced into tokens, why it matters, and what breaks when it goes wrong.',
  apps: ['💬 ChatGPT context limits', '🌐 multilingual models', '🔍 search indexing'],
  levels: [
  { html: '<p>Neural networks only multiply numbers — so the first step of any language model is converting text into integers. <b>Tokenization</b> chops "Hello world" into pieces (<b>tokens</b>) and maps each to an ID the model can embed.</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>Like assigning every LEGO brick shape a part number so a robot can follow building instructions. The robot never sees "red 2x4 brick" — it sees part #3001. The model never sees words — it sees token #15496.</p></div><div class="reallife"><span class="lbl">📱 In your daily life</span><p>When <b>ChatGPT</b> says you have "4,096 tokens left", it is counting these chunks — roughly 3/4 of a word each in English. Paste a long article and watch the limit: that counter <i>is</i> the tokenizer running.</p></div>',
    try: [['🔬 Split text into tokens live', '../tokenizer/', 'p']] },
  { html: '<p>Three classic strategies: <b>word-level</b> (split on spaces — huge vocabulary, breaks on typos), <b>character-level</b> (tiny vocabulary, very long sequences), and <b>subword</b> (the modern winner: common words stay whole, rare words split).</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>Packing for a trip: whole outfits for everyday wear (common words), mix-and-match pieces for unusual occasions (rare words split into parts). Subword tokenizers pack the vocabulary the same way.</p></div><p>Subword keeps vocabularies small (~30k-100k) while never producing an "unknown" token — any string can be spelled out from pieces.</p>',
    try: [['🔬 Word vs char vs subword demo', '../tokenizer/', 'p']] },
  { html: '<p>The dominant algorithm is <b>BPE (byte-pair encoding)</b>: start with single characters, then repeatedly merge the most frequent adjacent pair until the vocabulary is full. Frequent words end up as single tokens; rare ones stay split.</p><pre><code>corpus: "low low lower"\nmerge "l"+"o" -&gt; "lo"   (freq 3)\nmerge "lo"+"w" -&gt; "low" (freq 3)\nfinal:  "low" is ONE token, "lower" = "low"+"er"</code></pre><p>GPT models use BPE variants; SentencePiece (used by LLaMA) trains the same idea directly on raw text — no pre-splitting on spaces — which handles Chinese/Japanese cleanly.</p>',
    try: [['🔬 Train a mini-BPE on your own text', '../tokenizer/', 'p']] },
  { html: '<p>Tokenizers add <b>special tokens</b>: start/end markers, padding, unknowns, separators. Two engineering traps: <b>off-by-one decoding</b> (spaces glued to the wrong token) and <b>tokenizer-model mismatch</b> (fine-tuning with a different tokenizer silently scrambles every ID).</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>Two walkie-talkie teams using different codebooks: "eagle" means attack to one team, retreat to the other. Same signal, opposite meaning — that is what happens when the tokenizer and model disagree.</p></div><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>Multilingual keyboards</b> suggesting wrong autocompletions on mixed-language text are often fighting tokenization built for one language applied to another.</p></div>',
    try: [['📖 Tokenizer pitfalls (HuggingFace course)', 'https://huggingface.co/learn/nlp-course/chapter6/1', 'o']] },
  { html: '<p>Expert view: <b>fertility</b> (tokens per word) measures tokenizer efficiency per language — English ~1.3, Burmese ~10+. High fertility = shorter effective context + higher API cost. Modern fixes: <b>byte-level BPE</b> (GPT-2: never emits UNK), <b>Unigram</b> (probabilistic segmentation), and <b>tokenizer-free</b> models (CANINE, ByT5 on raw bytes).</p><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>API pricing is per token</b> — a Hindi user can pay 5-8x more than an English user for the same message because of fertility gaps. Tokenizer choice is a fairness and cost decision.</p></div>',
    try: [['📖 GPT-2 paper: byte-level BPE', 'https://cdn.openai.com/better-language-models/language_models_are_unsupervised_multitask_learners.pdf', 'o']] } ],
  quiz: [
    { q: 'Why do modern models use subword tokenization instead of whole words?', opts: ['It is faster to type', 'Small vocabulary that can still represent ANY string — common words stay whole, rare words split', 'Models cannot count past 30,000', 'Words use too little memory'], ok: 1, why: 'Word-level vocabularies explode and need UNK tokens. Subword (BPE/Unigram) keeps ~30-100k entries while guaranteeing any text can be spelled from pieces.' },
    { q: 'What is "fertility" and why does it matter?', opts: ['How fast a model trains', 'Average tokens per word — high fertility means shorter effective context and higher per-message API cost', 'The number of GPUs used', 'How many layers a model has'], ok: 1, why: 'Fertility ~1.3 for English but 5-10+ for some languages. Since context windows and prices are counted in tokens, high fertility shrinks usable context and raises cost.' },
    { q: '"tokenization" split as "token" + "ization" is whose signature?', opts: ['Word-level splitting', 'Character-level splitting', 'BPE/subword — frequent chunks kept whole, rare words split at learned boundaries', 'Random splitting'], ok: 2, why: 'BPE merges frequent pairs first, so common morphemes become single entries and rare full words compose from them. Exactly the subword signature.' } ] },
{ num: 3, emoji: '🧮', title: 'Matmul & GPUs — the compute engine', layer: 'Systems',
  tagline: 'One operation runs the whole AI revolution: matrix multiplication. Why GPUs exist, how parallelism works, and what FLOPs really buy you.',
  apps: ['🎮 gaming graphics cards', '⚡ fast ChatGPT replies', '🏋️ training runs'],
  levels: [
  { html: '<p>Strip away the hype and nearly all of deep learning is one operation repeated billions of times: <b>matrix multiplication</b> (matmul). Every layer of a neural network is "multiply inputs by a weight matrix, add bias, apply a curve".</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>A marks spreadsheet: rows are students, columns are subjects, one formula computes every weighted total at once. Matmul is that formula — one instruction scoring thousands of combinations simultaneously instead of cell by cell.</p></div><div class="reallife"><span class="lbl">📱 In your daily life</span><p>Every <b>Instagram filter</b>, <b>face unlock</b>, and <b>voice transcription</b> on your phone is thousands of tiny matmuls running on the mobile GPU/Neural Engine — billions of multiply-adds per photo.</p></div>',
    try: [['🔬 Multiply matrices by hand (visual)', '../lab/playground.html', 'p']] },
  { html: '<p>Matmul is <b>embarrassingly parallel</b>: each output cell depends only on one row and one column, so thousands of cells can be computed at the same time with zero coordination. CPUs have ~8-16 powerful cores; GPUs have <b>thousands of small cores</b> built exactly for this pattern.</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>Painting a fence: one master painter (CPU) is unbeatable for a portrait, but 5,000 volunteers with rollers (GPU) finish a mile of fence in an afternoon. Neural nets are fences, not portraits — same simple op everywhere.</p></div><p>This is why a gaming graphics card from 2016 can train a real neural network: rendering pixels and multiplying matrices are the same parallel workload.</p>',
    try: [['🔬 CPU vs GPU: time the same matmul', '../lab/playground.html', 'p']] },
  { html: '<p>Counting compute: one multiply-add = <b>2 FLOPs</b>. A GPT-3-scale training run costs ~3e23 FLOPs — that number decides the budget, the cluster size, and the schedule. Inference is the same math in miniature: each generated token = one forward pass = billions of FLOPs in under 100ms.</p><pre><code>params P, tokens T  →  train ≈ 6·P·T FLOPs\nGPT-3: 175B × 300B tokens ≈ 3.1e23 FLOPs\n1× A100 (312 TFLOPS) → ~30,000 GPU-days</code></pre><p><b>Memory bandwidth</b> is the usual bottleneck: the GPU can multiply faster than it can fetch weights, so big-model speed is about feeding the beast, not just more cores.</p>',
    try: [['📖 Scaling laws (Chinchilla)', 'https://arxiv.org/abs/2203.15556', 'o']] },
  { html: '<p>Beyond raw matmul: modern GPUs win with <b>tensor cores</b> (hardware that multiplies 4x4 blocks in one tick), <b>mixed precision</b> (FP16/BF16 halves memory traffic with ~zero accuracy loss), and <b>fusion</b> (combining ops so intermediate results never leave fast on-chip memory).</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>A food-truck kitchen vs a home kitchen: smaller counter (memory), but the grill, fryer and prep station are one fused unit — ingredients never walk across the room. Tensor cores + fusion keep data on the "counter" (SRAM) instead of trekking to the "pantry" (HBM).</p></div><div class="reallife"><span class="lbl">📱 In your daily life</span><p>Why <b>ChatGPT replies stream fast</b> on huge models: the servers run FP16/BF16 with fused kernels — same answers, half the memory traffic, ~2x the tokens per second.</p></div>',
    try: [['📖 FlashAttention: IO-aware exact attention', 'https://arxiv.org/abs/2205.14135', 'o']] },
  { html: '<p>Expert view: performance is a <b>roofline</b> — you are either compute-bound (big batches, training) or memory-bound (single-user decoding). <b>KV-cache</b> turns generation from recompute-everything into read-the-cache; <b>quantization</b> (INT8/INT4) shrinks weights so more fit in fast memory; <b>batching + continuous batching</b> (vLLM) keeps the GPU fed across users.</p><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>Why the first ChatGPT token is slow then words pour out</b>: the first pass processes your whole prompt (compute-bound), then each new word reuses the KV-cache (memory-bound but tiny) — prefill vs decode, visible in every chat app.</p></div>',
    try: [['📖 vLLM: continuous batching + PagedAttention', 'https://arxiv.org/abs/2309.06180', 'o']] } ],
  quiz: [
    { q: 'Why are GPUs — not CPUs — the engine of deep learning?', opts: ['GPUs have bigger hard drives', 'Neural nets repeat one simple parallel op (matmul) billions of times; GPUs have thousands of small cores built exactly for that', 'CPUs cannot do math', 'GPUs use less electricity for everything'], ok: 1, why: 'Matmul output cells are independent, so thousands compute simultaneously. CPUs optimize single-thread latency (~16 big cores); GPUs optimize throughput (thousands of small cores) — a perfect match for uniform parallel math.' },
    { q: 'Roughly how many FLOPs does training cost for P params on T tokens?', opts: ['P + T', 'P × T / 1000', 'About 6·P·T (forward + backward + update per token)', 'It is constant regardless of size'], ok: 2, why: 'Each token costs ~2·P FLOPs forward and ~4·P backward+update ≈ 6·P·T total. This single formula sizes GPT-3-scale runs (~3e23 FLOPs) and every training budget since.' },
    { q: 'During single-user chat decoding, what usually bottlenecks speed?', opts: ['CPU clock speed', 'Memory bandwidth — fetching weights + KV-cache per token dominates; compute is mostly idle', 'Internet latency only', 'Disk read speed'], ok: 1, why: 'Decoding does little math per token but must stream all weights (and KV-cache) from HBM each step — classic memory-bound regime. That is why quantization and KV-cache matter more than raw FLOPs for chat latency.' } ] },
{ num: 4, emoji: '👁️', title: 'Attention & Transformers — the architecture', layer: 'Model',
  tagline: 'How models look at the right words at the right time. Queries, keys, values — then the transformer block that ate the world.',
  apps: ['✍️ ChatGPT writing', '🌍 Google Translate', '📝 Gmail autocomplete'],
  levels: [
  { html: '<p>Old models read left-to-right and forgot the start by the end. <b>Attention</b> fixes this: when predicting the next word, the model looks back at <i>every</i> previous word and decides how much each one matters — "it" in "the animal drank because it was thirsty" attends to "animal", not "water".</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>A student answering an open-book exam: instead of memorizing the textbook (old RNNs), she flips back to exactly the right pages for each question. Attention is the flipping — dynamic lookup, not memorization.</p></div><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>Gmail autocomplete</b> finishing your sentence correctly ("attached is the... report") works because attention links "attached" back to the email subject and earlier lines — context-aware, not just last-word guessing.</p></div>',
    try: [['🔬 Attention heatmap: watch the focus move', '../attention/', 'p']] },
  { html: '<p>The mechanism in one line: <b>Attention(Q, K, V) = softmax(Q·K / sqrt(d)) · V</b>. Each token emits a <b>query</b> ("what am I looking for?"), compares it against every token\'s <b>key</b> ("what do I offer?"), and takes a weighted mix of their <b>values</b> ("my actual content"). Divide by sqrt(d) keeps the scores calm; softmax turns them into weights summing to 1.</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>A library search: your question is the <b>query</b>, book blurbs are <b>keys</b>, full books are <b>values</b>. You score every blurb against your question, then read a blend of the best-matching books — mostly the top hit, a little of the rest.</p></div>',
    try: [['🔬 Q·K·V with real numbers', '../attention/', 'p']] },
  { html: '<p><b>Multi-head</b> attention runs this lookup 8-96 times in parallel, each head learning a different relation — one tracks grammar, one tracks names, one tracks long-range topic. Stacked with feed-forward layers + residual connections + normalization, one stack = a <b>transformer block</b>. GPT-3 stacks 96 of them.</p><pre><code>x = x + MultiHeadAttention(norm(x))   # gather context\nx = x + FeedForward(norm(x))          # think per position\n# repeat 12–96x → the model</code></pre><p><b>Positional encodings</b> inject word order (attention itself is order-blind), and <b>causal masking</b> hides future words so training to "predict next" is honest.</p>',
    try: [['🔬 Build a transformer block step by step', '../attention/', 'p']] },
  { html: '<p>Cost reality: attention is <b>O(n^2)</b> — every token compares with every other, so 100k-token contexts mean 10 billion comparisons per layer. Fixes that shipped: <b>FlashAttention</b> (exact math, tiled so it never leaves SRAM — 2-4x faster), <b>KV-cache</b> (store past keys/values, decode becomes O(1) per token), <b>GQA/MQA</b> (share key/value heads — smaller cache, ~same quality), and <b>sliding-window / sparse</b> attention (each token sees neighbors + a few globals).</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>A town-hall meeting: full attention = everyone hears everyone (chaos past ~50 people). Fixes: breakout groups with notes shared back (tiling), a secretary recording minutes so newcomers catch up instantly (KV-cache), representatives instead of full crowds (GQA).</p></div>',
    try: [['📖 FlashAttention paper', 'https://arxiv.org/abs/2205.14135', 'o']] },
  { html: '<p>Expert view: <b>"Attention is All You Need" (2017)</b> killed RNNs/CNNs for sequences — one architecture now rules text, vision (ViT), audio, even proteins (AlphaFold2\'s Evoformer). Open frontiers: <b>linear/subquadratic</b> attention (Mamba, RWKV, Hyena) for million-token contexts, <b>retrieval-augmented</b> attention (attend over external memory — see Ch.6), and mechanistic interpretability finding <b>induction heads</b> (the circuits that do in-context learning).</p><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>ChatGPT "losing" facts mid-long-conversation</b> is attention dilution + KV eviction in action; <b>Claude\'s 200k context</b> is engineering (efficient kernels + GQA + memory hierarchy) more than raw model size.</p></div>',
    try: [['📖 Attention Is All You Need (2017)', 'https://arxiv.org/abs/1706.03762', 'o']] } ],
  quiz: [
    { q: 'In one sentence, what does attention compute?', opts: ['The next word directly', 'For each token, a weighted mix of all token values, weighted by query-key match — dynamic context lookup', 'The loss of the model', 'A random shuffle of embeddings'], ok: 1, why: 'softmax(QK/sqrt(d)) gives relevance weights; multiplying by V blends content accordingly. Every position builds its own context summary each layer.' },
    { q: 'Why does long context cost O(n^2)?', opts: ['Models get confused by long text', 'Every one of n tokens scores against all n tokens — n×n comparisons per head per layer', 'GPUs slow down over time', 'Tokenizers duplicate text'], ok: 1, why: 'The Q·K matrix is n×n. 100k tokens → 10B scores per layer per head. FlashAttention, KV-cache, GQA and sparse patterns all attack this exact matrix.' },
    { q: 'What does the KV-cache do during generation?', opts: ['Compresses the model weights', 'Stores past keys/values so each new token only computes its own row — decode becomes cheap per step', 'Deletes old conversation', 'Speeds up training only'], ok: 1, why: 'Without it, generating token t would recompute attention over all t tokens from scratch. The cache turns an O(t) recompute into an O(1) lookup per step — why chat streams fast after the first token.' } ] },
{ num: 5, emoji: '🧭', title: 'Embeddings & Vector Search — meaning as geometry', layer: 'Model',
  tagline: 'Words become points in space where distance = meaning. How embeddings are learned and how billion-scale similarity search powers recommendations.',
  apps: ['🎵 Spotify recommendations', '🛒 Amazon similar items', '📸 Google Photos search'],
  levels: [
  { html: '<p>An <b>embedding</b> turns token #15496 into a list of ~hundreds of numbers (a vector) capturing its meaning. King − man + woman ≈ queen is the famous demo: relationships become <b>directions</b> in this space, similarity becomes <b>distance</b>.</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>A personality quiz scoring everyone on 300 traits: people with similar tastes cluster together. Embeddings are the quiz answers for words — "puppy" and "kitten" land side by side, "puppy" and "payroll" far apart.</p></div><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>Spotify "sounds like"</b> and <b>Amazon "similar items"</b> are nearest-neighbor lookups in embedding space: your song is a point, the recommendations are its closest neighbors.</p></div>',
    try: [['🔬 King - man + woman arithmetic', '../lab/playground.html', 'p']] },
  { html: '<p>How are they learned? The model starts with random vectors and training nudges them: words appearing in similar contexts get pulled together (<b>distributional hypothesis</b> — "you shall know a word by the company it keeps"). Similarity is measured by <b>cosine</b> (angle) or <b>dot product</b>; training objectives (skip-gram, contrastive/CLIP) directly reward "right neighbors close, wrong ones far".</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>Seating plan at a wedding: start random, then swap chairs so friends sit near friends (pull) and feuding families separate (push). After enough swaps the table map <i>is</i> the social graph — embeddings are the seating plan of meaning.</p></div>',
    try: [['🔬 Train tiny word vectors (Word2Vec)', '../lab/playground.html', 'p']] },
  { html: '<p>Finding nearest neighbors among <b>billions</b> of vectors in milliseconds needs <b>ANN search</b>: <b>HNSW graphs</b> (navigate a layered highway network to the neighborhood, then walk locally), <b>IVF + product quantization</b> (compress vectors 8-32x, search cluster shortlists), and <b>reranking</b> (cheap recall, then exact scoring on the top-k).</p><pre><code>1B vectors × 768 dims × 4 bytes ≈ 3 TB\nPQ ×16 compression → ~200 GB (fits!)\nHNSW query: ~1ms, recall@10 ≈ 98%</code></pre><p>FAISS, ScaNN, DiskANN, and every vector DB (Pinecone, Qdrant, pgvector, Chroma) are packaging of these three ideas.</p>',
    try: [['🔬 Billion-scale search in 1ms (FAISS demo)', '../lab/playground.html', 'p']] },
  { html: '<p>Production reality: embeddings <b>drift</b> (retrain the model → all vectors move → rebuild the index or version it), suffer <b>hubness</b> (a few "popular" vectors become everyone's neighbor), and encode <b>bias</b> (doctor−man+woman ≈ nurse was really in the data). Fixes: versioned indexes with dual-read cutover, max-sim/ColBERT late interaction for fine ranking, and hybrid <b>dense + BM25</b> search (exact keywords + fuzzy meaning beats either alone).</p><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>Google Photos finding "beach sunset" with no tags</b> is CLIP-style joint image-text embeddings + ANN search; when it hilariously misfires, that is usually hubness or a drifted index.</p></div>',
    try: [['📖 CLIP: text-image embeddings', 'https://arxiv.org/abs/2103.00020', 'o']] },
  { html: '<p>Expert view: modern stacks use <b>bi-encoders</b> (embed query and docs independently — fast, cacheable) for retrieval and <b>cross-encoders</b> (joint scoring — slow, accurate) for reranking. Multimodal models (CLIP, SigLIP) share one space across text+image; <b>Matryoshka embeddings</b> let one vector serve at many sizes (truncate 768→128 dims, keep ~95% quality). This layer directly feeds Ch.6: RAG is "retrieve with embeddings, then generate with attention".</p><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>Pinterest visual search</b> (snap a photo → similar pins) and <b>LinkedIn job matches</b> both run bi-encoder retrieval + cross-encoder rerank — the two-stage pattern behind nearly every recommendation box you see.</p></div>',
    try: [['📖 Sentence-BERT paper', 'https://arxiv.org/abs/1908.10084', 'o']] } ],
  quiz: [
    { q: 'What does "king − man + woman ≈ queen" demonstrate?', opts: ['Models do arithmetic homework', 'Relationships become directions in embedding space — vector offsets capture analogies', 'Embeddings store dictionary definitions', 'Training data contained that equation'], ok: 1, why: 'Consistent relational offsets emerge because training pulls co-occurring contexts together. Subtracting/adding isolates the gender direction, landing near "queen" — geometry encoding semantics.' },
    { q: 'How can you search 1 billion vectors in ~1ms?', opts: ['One giant GPU sorts them each query', 'ANN indexes: HNSW graphs / IVF + quantization navigate to the neighborhood instead of scanning everything, then rerank top-k exactly', 'Store them alphabetically', 'It is impossible — searches take minutes'], ok: 1, why: 'Exact scan of 1B×768 floats is ~3TB of math per query. ANN (graphs, clustering, compression) finds ~98% of true neighbors with 1000x less work; reranking fixes the last errors.' },
    { q: 'Why do production systems combine dense embeddings + BM25 keywords?', opts: ['To use more servers', 'Dense catches meaning/paraphrase, BM25 catches exact terms (names, codes) — hybrid beats either alone', 'BM25 is newer', 'Embeddings cannot handle English'], ok: 1, why: 'Embeddings blur rare exact strings (SKUs, error codes, surnames) while BM25 misses paraphrases. Hybrid retrieval covers both failure modes — standard in serious search/RAG stacks.' } ] },
{ num: 6, emoji: '🔗', title: 'Prompting, RAG & Agents — using the model', layer: 'Applied',
  tagline: 'The model is frozen — the skill is in how you ask, what context you attach, and what tools you let it call.',
  apps: ['🤖 ChatGPT custom GPTs', '📚 Perplexity answers', '🛠️ Copilot coding'],
  levels: [
  { html: '<p>A finished model is a prediction machine waiting for instructions. <b>Prompting</b> is programming in plain language: role ("you are a SQL expert"), format ("reply as a table"), constraints ("cite sources"), and examples. Same model, wildly different outputs — the prompt <i>is</i> the program.</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>Hiring a brilliant intern with total amnesia of your company: vague brief → vague work; crisp brief with examples and a checklist → excellent work. Prompting is the brief; the model is the intern.</p></div><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>"Summarize this in 3 bullets for a 10-year-old"</b> vs pasting raw text into ChatGPT — one extra sentence transforms a wall of jargon into something usable. That sentence is prompt engineering.</p></div>',
    try: [['🔬 Same model, 5 prompts, 5 results', '../lab/playground.html', 'p']] },
  { html: '<p>Models hallucinate because they predict plausible text, not truth. <b>RAG (retrieval-augmented generation)</b> grounds them: retrieve relevant docs with embeddings (Ch.5), stuff them into the prompt, then generate. The answer now quotes <i>your</i> data instead of inventing facts.</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>Exam policy change: closed-book (pure model — confident guesses) vs open-book with the textbook on the desk (RAG — look up, then answer). Same student, far fewer invented "facts".</p></div><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>Perplexity</b> answering with linked sources and <b>customer-support bots</b> quoting actual policy pages are RAG: search first (embeddings), speak second (generation).</p></div>',
    try: [['🔬 Build a mini-RAG over docs', '../lab/playground.html', 'p']] },
  { html: '<p><b>Agents</b> close the loop: the model plans, calls <b>tools</b> (search, code, APIs, databases), reads results, and repeats until done. Patterns that work: <b>ReAct</b> (reason → act → observe, loop), <b>plan-and-execute</b>, and <b>multi-agent</b> (researcher + critic + writer). Guardrails that matter: least-privilege tools, human approval for irreversible actions, full action logs.</p><pre><code>goal: "book me the cheapest Fri flight"\nthink → search_flights(...) → observe prices\nthink → pick + hold_seat(...) → ask user to confirm\nthink → pay(...)   # only after approval</code></pre>',
    try: [['🔬 Trace an agent solving a task', '../lab/playground.html', 'p']] },
  { html: '<p>RAG that actually works: <b>chunking</b> (512-token windows with overlap beat whole-doc dumps), <b>hybrid retrieval</b> (dense + BM25, Ch.5), <b>reranking</b> top-50 with a cross-encoder, <b>citation-forcing</b> prompts ("answer only from context; say unknown otherwise"), and <b>evals</b> (faithfulness + answer relevance on a golden set — unmeasured RAG rots silently).</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>A research assistant: bad one dumps 200 pages on your desk; good one reads them, highlights 3 paragraphs, flags what is missing, and tells you when the sources disagree. Chunking + rerank + citations = the good assistant.</p></div>',
    try: [['📖 RAG evaluation (RAGAS)', 'https://arxiv.org/abs/2309.15217', 'o']] },
  { html: '<p>Expert view: <b>function calling / tool use</b> is models emitting structured JSON the runtime executes — the bridge from chat to action. Frontiers: <b>long-horizon agents</b> (SWE-bench tasks taking 50+ tool calls), <b>self-reflection</b> (Reflexion: critique and retry), <b>guardrail stacks</b> (prompt-injection detectors, PII scrubbers, policy models), and <b>evals-as-CI</b> — agent behavior changes with every model update, so regression suites are mandatory, not optional.</p><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>Copilot writing a whole function from a comment</b> is tool-use-adjacent generation; <b>AI travel planners</b> that search, compare, and book are full agents — and their failures (wrong dates booked!) are why approval gates exist.</p></div>',
    try: [['📖 ReAct paper (reason + act)', 'https://arxiv.org/abs/2210.03629', 'o']] } ],
  quiz: [
    { q: 'Why does RAG reduce hallucinations?', opts: ['It makes the model bigger', 'Retrieved real documents go into the prompt, so generation is grounded in your data instead of pure prediction', 'It deletes wrong answers afterwards', 'Hallucinations are illegal in RAG mode'], ok: 1, why: 'A bare model predicts plausible text; RAG adds evidence to the context so the model quotes/grounds answers in retrieved passages. Citations then let humans verify.' },
    { q: 'What is the ReAct agent pattern?', opts: ['React.js frontend code', 'Loop: reason about the goal, act via a tool, observe the result, repeat until done', 'Training with reactions', 'A type of GPU'], ok: 1, why: 'ReAct interleaves thought traces with tool calls and observations, letting the model plan, gather facts, adjust, and converge — the backbone of most working agents.' },
    { q: 'Your RAG bot answers confidently from stale docs. What is the FIRST fix?', opts: ['A bigger LLM', 'Refresh + version the index, add recency metadata, and force cite-or-say-unknown prompts — stale retrieval, not model size, is the failure', 'More emojis in the prompt', 'Delete all documents'], ok: 1, why: 'Most RAG failures are retrieval/currency failures, not reasoning failures. Fresh versioned indexes with timestamps and honesty-forcing prompts fix the class of bug; a bigger model just hallucinates more fluently.' } ] },
{ num: 7, emoji: '🎓', title: 'Fine-tuning & RLHF — teaching the model', layer: 'Training',
  tagline: 'From raw next-word predictor to helpful assistant: supervised fine-tuning, preference alignment, and why RLHF tames (but never fully solves) behavior.',
  apps: ['💬 ChatGPT personality', '🚫 refusals & safety', '🏥 medical/legal assistants'],
  levels: [
  { html: '<p>Pretraining teaches "what words usually follow"; <b>fine-tuning</b> teaches "how to behave": train a few more epochs on curated instruction → response pairs ("Summarize: ... → ..."). The base model gains skills; fine-tuning installs the <b>job description</b>.</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>Medical school (pretraining: all of medicine) vs residency (fine-tuning: how THIS hospital wants rounds done). Same knowledge, new behavior — helpful format, right tone, safe boundaries.</p></div><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>Why ChatGPT answers with headers and bullets</b> instead of rambling like a raw autocomplete: instruction fine-tuning on thousands of "good answer" examples installed that format.</p></div>',
    try: [['🔬 Fine-tune a tiny model live', '../lab/playground.html', 'p']] },
  { html: '<p>Full fine-tuning updates billions of weights (expensive, forgetful). <b>LoRA</b> freezes the model and trains tiny low-rank adapter matrices (~0.1% of params) — same quality for most tasks, swappable per task, trainable on one GPU. <b>Instruction tuning</b> (FLAN, Alpaca) showed 50k good examples can transform a base model; data quality beats quantity.</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>Tailoring a suit vs reweaving the fabric: LoRA takes in the seams (small adapters) instead of rebuilding the garment (full weights). Ten outfits, one wardrobe — swap adapters per occasion.</p></div>',
    try: [['📖 LoRA paper', 'https://arxiv.org/abs/2106.09685', 'o']] },
  { html: '<p>Helpfulness is a preference, not a fact — so we learn it from humans: collect <b>comparisons</b> (response A vs B, pick better), train a <b>reward model</b> to predict human choice, then optimize the LLM against it with <b>PPO</b> (classic RLHF) or directly with <b>DPO</b> (no separate RL loop — just push probability from rejected toward chosen). Result: assistants that refuse well, hedge honestly, and follow intent.</p><pre><code>human ranks: A &gt; B  →  reward(A) &gt; reward(B)\nPPO:  maximize reward − drift-from-original\nDPO:  one loss, no RL loop, same idea</code></pre>',
    try: [['📖 InstructGPT / RLHF paper', 'https://arxiv.org/abs/2203.02155', 'o']] },
  { html: '<p>What breaks: <b>reward hacking</b> (model games the proxy — longer answers score higher, so verbosity explodes), <b>alignment tax</b> (safety tuning dents raw capability), <b>sycophancy</b> (agreeing with the user over truth), and <b>jailbreaks</b> (adversarial prompts route around refusals). Mitigations: held-out human evals, red-teaming, system-level guardrails (Ch.6), and remembering alignment shapes <i>style of compliance</i> — knowledge cutoffs and tool access do the rest.</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>Teaching to the test: students optimize grades (reward) not learning (goal) — longer essays, guessed keywords. Reward hacking is identical: the model optimizes the measurable proxy, not your true intent.</p></div>',
    try: [['📖 Constitutional AI paper', 'https://arxiv.org/abs/2212.08073', 'o']] },
  { html: '<p>Expert view: the stack is now <b>pretrain → SFT → preference-opt (DPO/PPO) → evals</b>, with <b>RLAIF</b> (AI feedback) scaling comparisons beyond human labelers and <b>process supervision</b> (reward reasoning steps, not just final answers — OpenAI\'s math gains). Frontier debate: does RLHF merely <i>mask</i> bad behavior (shallow alignment) or reshape it? Evidence is mixed — which is why deployment adds monitoring, not just training.</p><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>"As an AI, I can\'t help with that"</b> refusals, <b>medical disclaimers</b>, and models asking clarifying questions are all RLHF-shaped behaviors — preference data, not pretraining text, installed them.</p></div>',
    try: [['📖 DPO paper (direct preference opt)', 'https://arxiv.org/abs/2305.18290', 'o']] } ],
  quiz: [
    { q: 'Pretraining vs fine-tuning — what is the division of labor?', opts: ['They are the same thing', 'Pretraining teaches broad next-word prediction from raw text; fine-tuning installs behavior/format on curated instruction pairs', 'Fine-tuning teaches grammar', 'Pretraining happens after fine-tuning'], ok: 1, why: 'Pretraining = knowledge/skills from the internet; SFT = the job description (follow instructions, format well, stay in role) from curated examples. Medical school vs residency.' },
    { q: 'Why is LoRA so popular for fine-tuning?', opts: ['It removes all weights', 'Freezes the base model, trains tiny adapters (~0.1% params) — cheap, swappable per task, near full-tuning quality', 'It trains 100x more params', 'It only works on CPUs'], ok: 1, why: 'LoRA low-rank adapters capture task behavior in megabytes instead of gigabytes — one GPU, minutes, swappable per task. Full fine-tuning is rarely worth it anymore.' },
    { q: 'What is "reward hacking" in RLHF?', opts: ['Stealing GPU time', 'The model exploits the proxy reward (e.g. verbosity scores well) instead of true human intent', 'Hackers attacking the reward model', 'Deleting the reward model'], ok: 1, why: 'Optimizing a measurable proxy (human-preference scores) invites gaming it — longer, flattering, hedged answers. Held-out evals, red-teaming and guardrails exist because proxies always leak.' } ] },
{ num: 8, emoji: '🚀', title: 'Quantization & Serving — shipping it', layer: 'Production',
  tagline: 'A trained model is a 300GB file nobody can use. How compression, batching, and serving stacks turn weights into a fast, cheap API.',
  apps: ['📱 on-device AI keyboards', '💸 API pricing', '⚡ instant replies'],
  levels: [
  { html: '<p>A 70B model in FP32 weighs ~280GB — no single GPU holds it. <b>Quantization</b> stores weights in fewer bits: FP16 halves it, INT8 quarters it, INT4 takes it to ~35GB with ~1-2% quality loss. Smaller weights = fits on cheaper GPUs, loads faster, streams quicker (memory-bound decoding, Ch.3).</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>MP3 vs WAV: dropping inaudible frequencies shrinks music 10x with nobody noticing. Quantization drops precision bits the model barely uses — same song, tenth the file.</p></div><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>On-device keyboards and photo features</b> run INT8/INT4 models — full quality would need a datacenter; quantized, it fits in your phone's memory and answers offline.</p></div>',
    try: [['🔬 FP32 vs INT8: size vs accuracy', '../lab/playground.html', 'p']] },
  { html: '<p>Methods that shipped: <b>GPTQ/AWQ</b> (layer-wise 4-bit with calibration data — near-lossless), <b>GGUF</b> (llama.cppCPU-friendly quants for laptops), <b>KV-cache quantization</b> (long contexts need it — cache dwarfs weights at 100k tokens), and <b>distillation</b> (train a small model to imitate a big one — different technique, same goal: small + capable).</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>Translating an encyclopedia into pocket-guide editions: GPTQ/AWQ are expert abridgers keeping every key fact (calibration = knowing what matters); distillation is a bright student who read the encyclopedia and rewrote it shorter.</p></div>',
    try: [['📖 GPTQ paper', 'https://arxiv.org/abs/2210.17323', 'o']] },
  { html: '<p>Serving = keeping the GPU fed: <b>continuous batching</b> (vLLM: new requests join mid-generation, no waiting for the batch), <b>PagedAttention</b> (KV-cache in non-contiguous blocks — near-zero waste), <b>speculative decoding</b> (small model drafts, big model verifies — 2-3x faster), and <b>tensor/pipeline parallelism</b> (split layers across GPUs when one card cannot hold the model).</p><pre><code>naive:  batch waits → GPU idles 40%\nvLLM:   continuous batching → ~95% busy\n+ spec decoding → another ~2x tokens/sec</code></pre>',
    try: [['📖 vLLM paper', 'https://arxiv.org/abs/2309.06180', 'o']] },
  { html: '<p>What breaks in prod: <b>tail latency</b> (one long generation stalls the batch — fix with chunked prefill + max-token budgets), <b>OOMs</b> (KV-cache explodes on long contexts — paged memory + eviction policies), <b>cold starts</b> (280GB weight loads take minutes — keep warm pools / cache on local NVMe), and <b>cost cliffs</b> (idle GPUs burn money — autoscale on queue depth, spot instances for batch jobs).</p><div class="analogy"><span class="lbl">🎯 Analogy</span><p>Running a restaurant at lunch rush: one table camping for hours (long generation) blocks turnover — time-box tables (token budgets), share the kitchen across parties (batching), keep popular dishes prepped (warm pools).</p></div>',
    try: [['📖 LLM inference optimization guide', 'https://lilianweng.github.io/posts/2023-01-10-inference-optimization/', 'o']] },
  { html: '<p>Expert view: the cost equation is <b>$/million tokens = (GPU-hr price × latency) / throughput</b> — every optimization above moves one term. Frontiers: <b>on-device LLMs</b> (3-8B INT4 on phones — Gemini Nano, Apple Intelligence), <b>custom silicon</b> (TPUs, Trainium, Groq LPUs attack memory bandwidth directly), and <b>disaggregated serving</b> (prefill GPUs + decode GPUs scaled independently — Splitwise, DistServe). You now own the full stack: Ch.1 trained it, Ch.2-5 built it, Ch.6 uses it, Ch.7 aligned it, Ch.8 ships it. 🎉</p><div class="reallife"><span class="lbl">📱 In your daily life</span><p><b>Why one API charges 10x another for the "same" model</b>: quantization level, batching efficiency, and silicon choice. Two providers, same weights, totally different $/M-tokens — now you know exactly why.</p></div>',
    try: [['📖 Lilian Weng: inference optimization', 'https://lilianweng.github.io/posts/2023-01-10-inference-optimization/', 'o']] } ],
  quiz: [
    { q: 'Why does INT4 quantization barely hurt quality but massively help serving?', opts: ['Models only need 4 weights', 'Weights carry redundant precision; fewer bits = fits cheaper GPUs + less memory traffic per token (decoding is memory-bound)', 'INT4 disables attention', 'Quantization retrains the model'], ok: 1, why: 'Neural weights tolerate low precision (outliers aside — AWQ/GPTQ handle those). Since chat decoding bottlenecks on streaming weights from HBM, 4-bit weights ≈ 4x less traffic ≈ much faster + cheaper, ~1-2% quality cost.' },
    { q: 'What does continuous batching (vLLM) fix?', opts: ['Slow tokenizers', 'GPU idling while waiting for a batch to finish — new requests join mid-generation, keeping utilization ~95%', 'Bad prompts', 'Power outages'], ok: 1, why: 'Naive static batching waits for the longest generation. Continuous batching inserts/evicts requests per step (plus PagedAttention for memory), so the GPU stays fed — the core trick behind cheap fast APIs.' },
    { q: 'Full-stack check: a user asks your RAG bot a question. Name the chapter for each step.', opts: ['Only Ch.6 matters', 'Tokenize (Ch.2) → embed + retrieve (Ch.5) → attend + generate (Ch.4) → decode fast via cache/quantization (Ch.3/8), all on autograd-trained weights (Ch.1) aligned by RLHF (Ch.7)', 'Just Ch.1 and Ch.8', 'Chapters are unrelated'], ok: 1, why: 'Every query flows the whole stack: token IDs → embeddings → retrieval → attention → memory-efficient decoding → served cheaply. The course is one pipeline; this question proves you see it end to end. Course complete! 🎉' } ] }

];


