/* AI-ML Learn — PART 5 (Databases & Data Systems: production + interviews). window.CHAPTERS = 18 chapters x 5 levels. */
window.CHAPTERS = [

/* ============================ 1. DATA MODELING & NORMALIZATION ============================ */
{ num: 1, emoji: '🗂️', title: 'Data Modeling & Normalization', layer: 'Relational',
  tagline: 'Structure the data so one fact lives in one place — then know when to break that rule on purpose.',
  apps: ['every relational schema you design', 'the "model this domain" whiteboard', 'warehouse star schemas'],
  levels: [
  { html:
    '<p>A data model names the <b>entities</b> (things), their <b>attributes</b> (properties), and the <b>relationships</b> between them. <b>Normalization</b> is organising columns so each fact is stored exactly once, killing update anomalies.</p>' +
    '<pre><code>-- un-normalised: the customer name repeats on every order row → update it in 5 places, miss one, drift\norders(order_id, customer_name, customer_email, product, qty, price)\n\n-- normalised: one row per customer, orders reference it by id\ncustomers(customer_id PK, name, email)\norders(order_id PK, customer_id FK, order_ts)\norder_items(order_id FK, product_id FK, qty, unit_price)   -- PK = (order_id, product_id)</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>One source of truth.</b> You do not write your phone number on every page of a notebook — you write it once on the inside cover and everything else points there. Change it once, everywhere is correct.</p></div>',
    try: [['📘 Part 4: SQL for DS interviews (grain, star schema)', '../learn4/#ch14', 'o'], ['📖 PostgreSQL: database design', 'https://www.postgresql.org/docs/current/ddl.html', 'o']] },
  { html:
    '<p>The normal forms, in plain terms:</p>' +
    '<pre><code>1NF  atomic columns, no repeating groups (no "tags" column holding "a,b,c"; no phone1/phone2/phone3)\n2NF  1NF + every non-key column depends on the WHOLE composite key, not part of it\n3NF  2NF + no non-key column depends on another non-key column (no "zip" AND "city" derivable from zip)\nBCNF a stricter 3NF: every determinant is a candidate key\n</code></pre>' +
    '<p><b>Keys:</b> <code>natural</code> (a real-world unique value, e.g. ISBN) vs <code>surrogate</code> (a meaningless auto id — stable, compact, no PII, survives real-world changes). <code>composite</code> keys model many-to-many join tables. Most teams default to a surrogate PK plus a UNIQUE constraint on the natural key.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>3NF is "no column should be gossip about another column."</b> If <code>city</code> can be looked up from <code>zip</code>, storing both invites them to disagree.</p></div>',
    try: [['📖 Normalization (1NF-BCNF) explained', 'https://en.wikipedia.org/wiki/Database_normalization', 'o']] },
  { html:
    '<p><b>Denormalization</b> — deliberately duplicating data to avoid joins — is a performance tool, not a mistake, once you accept the cost of keeping copies in sync.</p>' +
    '<pre><code>examples:  a cached order_total on orders · a denormalised author_name on posts ·\n           a materialised "user_stats" table refreshed nightly · pre-joined report tables\nrule:      normalise for WRITE correctness (OLTP), denormalise for READ speed (reporting / hot paths),\n           and make the denormalised copy derivable + refreshable so you can rebuild it.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>A social feed does <code>SELECT post.*, user.name, user.avatar</code> on every render — 50M times/day, a join each time. Denormalise <code>author_name</code> + <code>author_avatar</code> onto <code>posts</code>, updated by a trigger / CDC when the user edits their profile. Reads drop the join; the rare profile edit fans out.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>Finance wants "orders by month by region" instantly. The OLTP schema is fully normalised (correct for writes). Build a nightly <b>star schema</b>: a <code>fact_orders</code> table (grain = one line item) + <code>dim_customer</code>, <code>dim_product</code>, <code>dim_date</code>. Reports hit the star; transactions still hit the 3NF tables.</p></div>',
    try: [['📖 Kimball: dimensional modeling', 'https://www.kimballgroup.com/data-warehouse-business-intelligence-resources/kimball-techniques/dimensional-modeling-techniques/', 'o']] },
  { html:
    '<p>Modeling patterns and traps:</p>' +
    '<pre><code>MANY-TO-MANY      a join table:  student_courses(student_id, course_id, enrolled_at)  PK on the pair\nONE-TO-ONE        rare — usually a sign two tables should be one, OR a deliberate split (hot vs cold columns)\nHIERARCHY / TREE  adjacency list (parent_id) · nested set · closure table · or a recursive CTE / ltree\nPOLYMORPHIC FK    "commentable_type + commentable_id" — flexible but no real FK constraint; consider\n                 separate tables or a supertype table instead\nEAV (entity-attribute-value)  "attributes" table with (entity, key, value) — extreme flexibility,\n                 terrible queries & no types; use JSONB instead if you truly need schemaless columns\nSOFT DELETE       deleted_at instead of DELETE — keeps history but every query must filter it;\n                 unique constraints must include it or become partial indexes</code></pre>' +
    '<p><b>SCD (slowly changing dimensions):</b> to answer "what was the customer\'s tier when this order was placed", keep dimension history (SCD type 2: a new row per version with <code>valid_from</code>/<code>valid_to</code>).</p>',
    try: [['📖 Modeling hierarchies in SQL', 'https://www.postgresql.org/docs/current/queries-with.html', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: What problem does normalization solve?\nA: Update/insert/delete anomalies. If a fact is stored once, you cannot get contradictory copies.\n   It also shrinks storage and makes constraints (FK, UNIQUE) meaningful.\n\nQ: When do you denormalize?\nA: When a read-heavy path pays a repeated join/aggregation cost that dominates, AND you can keep\n   the copy consistent (trigger, CDC, scheduled refresh) and rebuild it from the source of truth.\n   Typical: reporting tables, feed author info, precomputed counters.\n\nQ: Natural vs surrogate primary key?\nA: Surrogate (auto int / UUID): stable, small, no PII, immune to real-world changes — the usual\n   default, plus a UNIQUE on the natural key. Natural key: fewer joins for lookups, but changes\n   ripple through every FK if the "real" value ever changes.\n\nQ: 3NF in one sentence, with an example violation.\nA: Every non-key column depends on the key, the whole key, and nothing but the key. Violation:\n   an orders table storing both customer_id and customer_city (city depends on the customer, a\n   non-key column) — move city to the customers table.\n\nQ: How do you model many-to-many?\nA: A junction/join table with a composite primary key of the two foreign keys, plus any\n   relationship attributes (e.g. enrolled_at, role).\n\nQ: A column needs to hold arbitrary user-defined fields. EAV or JSONB?\nA: JSONB (in Postgres) — you keep types, GIN indexing, and reasonable queries, without the EAV\n   anti-pattern of self-joining a (entity, key, value) table for every attribute.\n\nQ: How do you keep history of a dimension attribute (e.g. price tier)?\nA: SCD type 2: version rows with valid_from / valid_to (or is_current), so historical facts join\n   to the attribute value as it was at that time.</code></pre>',
    try: [['📖 Use The Index, Luke — schema & keys', 'https://use-the-index-luke.com/', 'o']] } ],
  quiz: [
    { q: 'An "orders" table stores customer_name and customer_email on every row. What class of bug does this invite?', opts: ['Slower reads only', 'Update anomalies — changing a customer\'s email means updating many rows; miss one and the data contradicts itself', 'Foreign key violations', 'Index bloat'], ok: 1, why: 'Duplicated facts must be updated everywhere at once. Normalizing to a customers table (referenced by customer_id) means the email exists once and cannot drift.' },
    { q: 'You duplicate author_name onto a posts table to avoid a join on every feed render. What must be true for this to be safe?', opts: ['Nothing — duplication is always fine for reads', 'The copy must be kept in sync (trigger/CDC/refresh) and be rebuildable from the source of truth (the users table)', 'You must drop the users table', 'The posts table must be in 1NF only'], ok: 1, why: 'Denormalization trades write complexity for read speed. It is acceptable when the duplicated value is derivable and there is a reliable mechanism to update it when the source changes.' },
    { q: 'Which is the standard way to model a many-to-many relationship (students ↔ courses)?', opts: ['A comma-separated course_ids column on students', 'A junction table student_courses(student_id, course_id, ...) with a composite PK on the pair', 'A one-to-one table', 'A polymorphic foreign key'], ok: 1, why: 'A junction/associative table with a composite primary key of both foreign keys represents the pairing and can carry relationship attributes (enrolled_at, grade). A CSV column violates 1NF and cannot be indexed or constrained.' } ] },

/* ============================ 2. INDEXES & THE QUERY PLANNER ============================ */
{ num: 2, emoji: '🔑', title: 'Indexes & the Query Planner', layer: 'Relational',
  tagline: 'An index is a sorted shortcut. The planner guesses row counts and picks a plan — learn to read its mind.',
  apps: ['"this query is slow" — the daily task', 'EXPLAIN ANALYZE reading', 'schema review / capacity planning'],
  levels: [
  { html:
    '<p>Without an index, finding rows = a <b>sequential scan</b> (read every page). An index is a separate, sorted structure that turns "scan 10M rows" into "walk a tree to the ~10 you want".</p>' +
    '<pre><code>CREATE INDEX idx_orders_customer ON orders(customer_id);\n-- now WHERE customer_id = 42 is a B-tree lookup, not a full scan\n\ncost: every INSERT/UPDATE/DELETE must also maintain every index on the table.\n      indexes are pure overhead on writes and storage — add them for real query patterns, not "just in case".</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>The index at the back of a textbook.</b> To find "photosynthesis" you do not read all 800 pages — you flip to the alphabetical index and jump to page 214. But every time the book is revised, the index must be rebuilt too.</p></div>',
    try: [['📖 Use The Index, Luke (free book)', 'https://use-the-index-luke.com/', 'o'], ['📘 Part 4: the SQL screen', '../learn4/#ch14', 'o']] },
  { html:
    '<p>Index types and the properties that matter:</p>' +
    '<pre><code>B-TREE       default; equality AND range (=, &lt;, &gt;, BETWEEN, ORDER BY, prefix LIKE \'abc%\')\nHASH         equality only; rarely worth it over B-tree\nBITMAP       low-cardinality columns, analytic workloads (Oracle/Postgres builds them on the fly)\nGIN          "contains" queries — JSONB keys, arrays, full-text search\nGiST / SP-GiST geometric / range / nearest-neighbour\nBRIN         huge, naturally-ordered tables (time-series) — tiny index, coarse\n\nCOMPOSITE (a, b, c):  usable for WHERE a=?, WHERE a=? AND b=?, WHERE a=? AND b=? AND c=?\n                     NOT for WHERE b=? alone  → the LEFTMOST-PREFIX rule\nCOVERING (INCLUDE):   the index carries extra columns so the query never touches the table\nPARTIAL:             CREATE INDEX ... WHERE status = \'active\'  → smaller, for a hot subset\nUNIQUE:              enforces uniqueness AND serves as an index</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A phone book is sorted by (last name, first name).</b> You can find "all the Smiths" and "Smith, John". You cannot use it to find "everyone named John" — that is the leftmost-prefix rule.</p></div>',
    try: [['📖 PostgreSQL: index types', 'https://www.postgresql.org/docs/current/indexes-types.html', 'o']] },
  { html:
    '<p>The planner is a <b>cost-based optimizer</b>: it estimates how many rows each step produces (from table statistics / histograms) and picks the cheapest plan — seq scan vs index scan, nested-loop vs hash vs merge join, join order.</p>' +
    '<pre><code>EXPLAIN ANALYZE SELECT ... ;   -- shows the plan + ACTUAL rows/time. Compare estimated vs actual.\nred flags:  "Seq Scan" on a big table with a selective filter · estimated rows off by 100x\n            (stale stats → ANALYZE) · a Nested Loop over millions of rows (wanted a Hash Join)\n            · "Rows Removed by Filter: 9,900,000" · Sort spilling to disk (raise work_mem)</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>A query filtering <code>WHERE tenant_id = ? AND created_at &gt; ?</code> uses an index on <code>(created_at)</code> and is slow for big tenants. Fix: a composite index on <code>(tenant_id, created_at)</code> — now it seeks straight to that tenant\'s recent rows. Column order matters: equality column first, range column last.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p><code>WHERE lower(email) = ?</code> ignores the index on <code>email</code> because the function wraps the column. Fix: an <b>expression index</b> <code>CREATE INDEX ON users (lower(email))</code>, or store a normalised column. Same trap: <code>WHERE created_at::date = ?</code> and <code>WHERE amount + fee &gt; 100</code>.</p></div>',
    try: [['📖 Reading EXPLAIN', 'https://www.postgresql.org/docs/current/using-explain.html', 'o']] },
  { html:
    '<p>Why an index is not used (or not helping):</p>' +
    '<pre><code>• the filter is not SELECTIVE (returns &gt; ~5-20% of rows) → seq scan is genuinely cheaper\n• a function/cast/expression wraps the column → sargability broken (use an expression index)\n• leading wildcard LIKE \'%abc\' → B-tree cannot help (use trigram / full-text)\n• stale statistics → run ANALYZE; autovacuum not keeping up\n• wrong column ORDER in a composite index (leftmost-prefix)\n• OR across different columns → often needs separate indexes + a bitmap OR, or a rewrite to UNION\n• implicit type mismatch (bigint column vs text literal)\n• too many indexes → write amplification, planner confusion, bloat; drop unused ones (pg_stat_user_indexes)\n• index-only scan blocked because the table\'s visibility map is stale (vacuum)</code></pre>' +
    '<p><b>Write cost:</b> each index roughly adds a random write per row change; 10 indexes on a hot table can halve insert throughput. Index the queries you actually run.</p>',
    try: [['📖 pganalyze: how the planner uses statistics', 'https://pganalyze.com/blog/how-postgres-chooses-index', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: You have an index on (a, b, c). Which WHERE clauses can use it?\nA: a=?; a=? AND b=?; a=? AND b=? AND c=?; a=? AND c=? (partially — seeks on a, filters c);\n   also a=? AND b BETWEEN ... . It CANNOT seek for b=? alone or c=? alone (leftmost-prefix rule).\n\nQ: WHERE customer_id = ? AND status = \'open\' ORDER BY created_at DESC LIMIT 20 — index?\nA: (customer_id, status, created_at DESC). Equality columns first, then the ORDER BY column, so\n   the DB seeks to the group and reads 20 rows already in order — no sort, no extra fetch.\n\nQ: Why might the planner choose a sequential scan even though an index exists?\nA: The predicate is not selective enough — reading the index plus random heap fetches for, say,\n   40% of the table costs more than a single sequential pass. Also: stale stats, or a\n   non-sargable predicate.\n\nQ: WHERE date_trunc(\'day\', created_at) = \'2024-01-01\' is slow. Fix without changing the query much?\nA: Rewrite as a range: created_at &gt;= \'2024-01-01\' AND created_at &lt; \'2024-01-02\' so a plain\n   B-tree index on created_at works; or add an expression index on date_trunc(...).\n\nQ: What is a covering (index-only) scan?\nA: The index INCLUDEs every column the query needs, so the DB answers entirely from the index\n   without visiting the table heap — big win for hot read paths.\n\nQ: What is the downside of adding many indexes?\nA: Every write updates every index (write amplification), more storage, more bloat/vacuum work,\n   and the planner has more options to get wrong. Audit with index-usage stats and drop the dead ones.\n\nQ: EXPLAIN estimates 100 rows, ANALYZE shows 1,000,000. What do you do?\nA: The statistics are stale or the column has skew/correlation the planner cannot see. Run\n   ANALYZE, increase the statistics target, or add extended (multi-column) statistics.</code></pre>',
    try: [['📖 Use The Index, Luke — the WHERE clause', 'https://use-the-index-luke.com/sql/where-clause', 'o']] } ],
  quiz: [
    { q: 'You have a composite index on (tenant_id, created_at). Which query can seek efficiently with it?', opts: ['WHERE created_at > now() - interval \'1 day\'', 'WHERE tenant_id = 42 AND created_at > now() - interval \'1 day\'', 'WHERE tenant_id IN (SELECT ...) OR created_at IS NULL', 'ORDER BY created_at with no WHERE'], ok: 1, why: 'The leftmost-prefix rule: the index is ordered by tenant_id first, then created_at. A query that fixes tenant_id with equality and then ranges on created_at seeks straight to that tenant\'s recent rows. Filtering created_at alone cannot use it as a seek.' },
    { q: 'WHERE lower(email) = \'a@b.com\' does a sequential scan despite an index on email. Best fix?', opts: ['Add more RAM', 'Create an expression index on lower(email) (or store a normalized email column)', 'Rebuild the email index', 'Use a hash index'], ok: 1, why: 'Wrapping the column in a function makes the predicate non-sargable — the plain index on email cannot be used. An index on the expression lower(email) matches the predicate exactly.' },
    { q: 'The planner chooses a Seq Scan on a 50M-row table for a filter that matches ~30% of rows. Is this necessarily wrong?', opts: ['Yes, always add an index hint', 'No — for a non-selective predicate, one sequential pass is often cheaper than an index scan plus millions of random heap fetches', 'Yes, statistics are always stale', 'No, but only if the table has no primary key'], ok: 1, why: 'Index scans win when they touch a small fraction of rows. At ~30% selectivity the random I/O of index+heap lookups typically exceeds a streaming sequential read, so a seq scan can be the correct cost-based choice.' } ] },

/* ============================ 3. TRANSACTIONS & ISOLATION ============================ */
{ num: 3, emoji: '🔒', title: 'Transactions & Isolation', layer: 'Relational',
  tagline: 'ACID, the concurrency anomalies each isolation level allows, and the write-skew bug that bites everyone.',
  apps: ['"is this code race-safe?"', 'money movement / inventory / booking', 'debugging deadlocks & lock waits'],
  levels: [
  { html:
    '<p>A <b>transaction</b> groups statements so they succeed or fail as a unit. <b>ACID</b>:</p>' +
    '<pre><code>Atomicity    all-or-nothing; a crash mid-transaction rolls back\nConsistency  it moves the DB from one valid state to another (constraints hold)\nIsolation    concurrent transactions do not corrupt each other (degree set by the isolation level)\nDurability   once COMMIT returns, the data survives a crash (WAL / fsync)</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A bank transfer.</b> Debit A and credit B must both happen or neither. If the lights go out between the two, you must not be left with money vanished (atomicity) or double-counted (isolation).</p></div>',
    try: [['📖 PostgreSQL: transactions', 'https://www.postgresql.org/docs/current/tutorial-transactions.html', 'o']] },
  { html:
    '<p>The <b>read anomalies</b> and which isolation level forbids them:</p>' +
    '<pre><code>anomaly              READ COMMITTED   REPEATABLE READ   SERIALIZABLE\ndirty read           prevented         prevented          prevented\nnon-repeatable read   POSSIBLE          prevented          prevented\nphantom read          POSSIBLE          POSSIBLE (SQL std) prevented\nlost update           POSSIBLE*         prevented (PG)      prevented\nwrite skew            POSSIBLE          POSSIBLE            prevented\n\n(* engine-dependent; Postgres RR = snapshot isolation, blocks lost update but NOT write skew)\ndefault is usually READ COMMITTED. Serializable is the only level that guarantees the result\nequals SOME serial order of the transactions.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Isolation levels are how much you squint.</b> READ COMMITTED: you only see committed work, but it can change under you between reads. SERIALIZABLE: the DB pretends everyone took turns.</p></div>',
    try: [['📖 A Critique of ANSI SQL Isolation Levels', 'https://www.microsoft.com/en-us/research/publication/a-critique-of-ansi-sql-isolation-levels/', 'o']] },
  { html:
    '<p><b>Write skew</b> — the anomaly interviews love, because snapshot isolation does not stop it:</p>' +
    '<pre><code>rule: at least ONE doctor must stay on call.  Currently Alice and Bob are both on call.\nT1 (Alice): SELECT count(*) FROM oncall WHERE active  -- sees 2, ok  → UPDATE set Alice inactive\nT2 (Bob):   SELECT count(*) FROM oncall WHERE active  -- sees 2, ok  → UPDATE set Bob inactive\nboth COMMIT under REPEATABLE READ → now ZERO doctors on call. Each read a valid snapshot;\ntogether they broke the invariant.\n\nFIXES:  SERIALIZABLE isolation (SSI aborts one) · SELECT ... FOR UPDATE to lock the read rows ·\n        a materialised constraint (a counter row / an EXCLUDE constraint) both transactions contend on.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>Two users book the last seat at the same instant. Both <code>SELECT seats_left</code> (=1), both <code>UPDATE seats_left = 0</code>, both confirm. Fix: <code>SELECT ... FOR UPDATE</code> on the seat/row so the second transaction blocks, or a UNIQUE constraint on (event_id, seat_no) that makes the second INSERT fail.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>Wallet double-spend: a "read balance, then insert a debit" pattern under READ COMMITTED lets two concurrent withdrawals both pass a balance check. Fix: an atomic <code>UPDATE wallets SET balance = balance - :amt WHERE id = :id AND balance &gt;= :amt</code> and check the affected-row count, or SERIALIZABLE with retry.</p></div>',
    try: [['📚 Designing Data-Intensive Applications, ch.7', 'https://dataintensive.net/', 'o']] },
  { html:
    '<p>Concurrency mechanics and their failure modes:</p>' +
    '<pre><code>MVCC (Postgres, Oracle, MySQL-InnoDB): readers see a snapshot, writers create new row versions →\n  "readers do not block writers, writers do not block readers". Cost: dead tuples → VACUUM /\n  bloat; long-running transactions hold back cleanup (xmin horizon).\nLOCKS: row locks (FOR UPDATE / FOR SHARE), table locks (DDL), advisory locks (app-level mutex).\nDEADLOCK: T1 holds A wants B; T2 holds B wants A → the DB detects the cycle and aborts one\n  (deadlock victim). Fix: acquire locks in a CONSISTENT ORDER; keep transactions short; lower isolation if safe.\nLOCK WAIT / lock queue: one slow UPDATE behind an idle-in-transaction session stalls everyone →\n  set lock_timeout / statement_timeout; alert on "idle in transaction".\nDISTRIBUTED: 2PC (blocking, coordinator is a SPOF) vs SAGAS (a sequence of local txns + compensating\n  actions; eventual consistency, no global lock).</code></pre>',
    try: [['📖 PostgreSQL: explicit locking & MVCC', 'https://www.postgresql.org/docs/current/mvcc.html', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Explain the four ACID properties.\nA: Atomicity (all-or-nothing), Consistency (constraints/invariants preserved), Isolation\n   (concurrent transactions do not see each other\'s partial state, per the isolation level),\n   Durability (committed data survives a crash — WAL + fsync).\n\nQ: Difference between a non-repeatable read and a phantom read?\nA: Non-repeatable: you re-read the SAME row and its value changed (an UPDATE committed between).\n   Phantom: you re-run the SAME query and new ROWS appear/disappear (an INSERT/DELETE committed).\n\nQ: What is write skew and why does REPEATABLE READ / snapshot isolation not prevent it?\nA: Two transactions each read an overlapping set, each makes a decision valid for its snapshot,\n   and their writes together violate an invariant that neither writes the same row for. Snapshot\n   isolation only checks for write-write conflicts on the same rows. SERIALIZABLE (SSI) detects\n   the dangerous read-write dependency and aborts one.\n\nQ: Two users try to book the last item. How do you make it safe?\nA: Serialize access to the contended resource: SELECT ... FOR UPDATE on the inventory row, or an\n   atomic conditional UPDATE (SET qty = qty - 1 WHERE qty > 0) checking rows-affected, or a UNIQUE\n   constraint that makes the losing insert fail, or SERIALIZABLE with a retry loop.\n\nQ: What causes a deadlock and how do you avoid it?\nA: A cycle of lock waits. Avoid by acquiring locks in a consistent global order, keeping\n   transactions short, touching rows in a deterministic sequence, and setting lock_timeout so a\n   stuck transaction fails fast instead of piling up.\n\nQ: MVCC — one advantage and one cost.\nA: Advantage: readers and writers do not block each other (readers see a consistent snapshot).\n   Cost: obsolete row versions accumulate and must be vacuumed; long-lived transactions delay\n   cleanup and cause table/index bloat.\n\nQ: 2PC vs saga for a multi-service transaction?\nA: 2PC gives atomicity but blocks resources during the prepare phase and the coordinator is a\n   single point of failure. A saga runs local transactions per service with compensating actions\n   on failure — non-blocking, but only eventually consistent and you must design the compensations.</code></pre>',
    try: [['📖 CockroachDB: SERIALIZABLE explained', 'https://www.cockroachlabs.com/blog/what-write-skew-looks-like/', 'o']] } ],
  quiz: [
    { q: 'Under Postgres REPEATABLE READ (snapshot isolation), which anomaly can still occur?', opts: ['Dirty read', 'Lost update on the same row', 'Write skew (two transactions read an overlapping set, then write different rows, breaking a cross-row invariant)', 'Reading uncommitted data'], ok: 2, why: 'Snapshot isolation blocks dirty reads, non-repeatable reads, and write-write conflicts (lost update) — but two transactions modifying DIFFERENT rows based on a shared read can jointly violate an invariant. Only SERIALIZABLE prevents write skew.' },
    { q: 'Two concurrent withdrawals each read balance=100 and each insert a -80 debit. Best fix?', opts: ['Add an index on balance', 'Use an atomic conditional update: UPDATE wallets SET balance = balance - 80 WHERE id = ? AND balance >= 80, and verify one row was affected (or use SERIALIZABLE with retry)', 'Lower isolation to READ UNCOMMITTED', 'Run the withdrawals in a background job'], ok: 1, why: 'The read-then-write pattern has a race window. An atomic UPDATE with the balance check in the WHERE clause lets exactly one succeed; checking the affected-row count tells you if it failed. SERIALIZABLE + retry also works.' },
    { q: 'A production incident: all writes to one table hang. pg_stat_activity shows a session "idle in transaction" holding a lock for 20 minutes. Root cause pattern?', opts: ['Missing index', 'A transaction was opened (e.g. BEGIN + a row lock) and never committed/rolled back — the app forgot to end it, blocking everyone behind it', 'Deadlock detector is off', 'Autovacuum is too aggressive'], ok: 1, why: 'An open transaction holds its locks until COMMIT/ROLLBACK. "Idle in transaction" for minutes means the application acquired locks and stopped without ending the transaction, queueing all conflicting writers. Mitigate with idle_in_transaction_session_timeout and shorter transactions.' } ] },

/* ============================ 4. REPLICATION & HIGH AVAILABILITY ============================ */
{ num: 4, emoji: '🔁', title: 'Replication & High Availability', layer: 'Distributed',
  tagline: 'Copies of the data for reads, failover, and disaster recovery — and the lag that makes reads lie.',
  apps: ['read-scaling a hot database', 'surviving a node/AZ failure', 'backups & disaster-recovery drills'],
  levels: [
  { html:
    '<p><b>Replication</b> keeps copies of the data on other nodes. Reasons: <b>read scaling</b> (send SELECTs to replicas), <b>availability</b> (promote a replica if the primary dies), <b>geo-locality</b> (a replica near each region), <b>backups</b> without loading the primary.</p>' +
    '<pre><code>SINGLE-PRIMARY (leader-follower):  all writes go to the primary; it streams its WAL to replicas.\n  the common default. Replicas are read-only.\nMULTI-PRIMARY (multi-leader):  writes accepted on several nodes → conflict resolution needed\n  (last-write-wins, CRDTs, app merge). Used for multi-region write locality.\nLEADERLESS (Dynamo-style):  clients write to N nodes, read from N nodes, quorum (W + R &gt; N)\n  gives consistency. Cassandra, DynamoDB, Riak.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>One author, many photocopiers.</b> The author (primary) writes the master; copiers (replicas) reproduce it with a slight delay. Readers grab the nearest copy — which might be a few seconds out of date.</p></div>',
    try: [['📚 DDIA ch.5: Replication', 'https://dataintensive.net/', 'o']] },
  { html:
    '<p><b>Synchronous vs asynchronous</b> — the durability/latency trade:</p>' +
    '<pre><code>ASYNC   primary commits, THEN ships the WAL. Fast writes. If the primary dies before a replica\n        catches up, those committed transactions are LOST on failover (RPO &gt; 0).\nSYNC    primary waits for &ge; 1 replica to persist the WAL before returning COMMIT. Zero data loss\n        on single-node failure, but write latency = round-trip to the replica, and if the replica\n        is down, writes STALL (unless you have quorum-sync across several).\nSEMI-SYNC / quorum-sync:  wait for ANY k of n replicas — balances the two.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>A user updates their profile, the app immediately reads it back from a read replica, and sees the OLD value — <b>replication lag</b>. Fixes: route "read-your-writes" to the primary for a few seconds after a write; or use a session/consistency token; or read from the primary within the same request.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>An analytics job runs a 40-minute query on the replica. On Postgres this can conflict with WAL replay (the query needs old row versions the primary vacuumed) → the replica pauses replication or cancels the query. Fixes: <code>hot_standby_feedback</code>, a dedicated reporting replica, or bounded <code>max_standby_streaming_delay</code>.</p></div>',
    try: [['📖 PostgreSQL: streaming replication', 'https://www.postgresql.org/docs/current/warm-standby.html', 'o']] },
  { html:
    '<p><b>Failover</b> and <b>backups</b>:</p>' +
    '<pre><code>FAILOVER:  detect the primary is dead (health checks, missed heartbeats) → promote a replica →\n  repoint clients (a virtual IP, a proxy like PgBouncer/HAProxy, or service discovery).\n  DANGERS:  split-brain (old primary comes back and also accepts writes → diverging data — use\n            fencing / STONITH / a consensus lease); losing async-lagged writes; thundering herd\n            of reconnects. Automate with Patroni / repmgr / cloud-managed failover; TEST it.\nBACKUPS:   physical (a filesystem/block snapshot + WAL archive → fast restore, exact version) vs\n  logical (pg_dump / mysqldump → portable, slow to restore, per-object).\n  POINT-IN-TIME RECOVERY (PITR):  base backup + replay WAL up to "12:04:59, just before the bad DELETE".\n  a replica is NOT a backup — it faithfully replicates your mistakes. Keep offline/immutable copies.\n  RPO = how much data you can lose (backup frequency + async lag). RTO = how long recovery takes.\n  RUN RESTORE DRILLS — an untested backup is a hope, not a plan.</code></pre>',
    try: [['📖 Patroni (HA for Postgres)', 'https://patroni.readthedocs.io/', 'o']] },
  { html:
    '<p>Operational realities:</p>' +
    '<pre><code>• REPLICATION LAG is the metric to alarm on (seconds behind, bytes behind). Causes: a burst of\n  writes, a long transaction on the replica, a slow network, an under-powered replica, index builds.\n• READ-AFTER-WRITE consistency is an application concern — the DB will not give it to you for free\n  on async replicas.\n• connection routing: a proxy that sends writes + "fresh" reads to the primary and the rest to\n  replicas; handle a replica dropping out.\n• MULTI-REGION: cross-region sync replication adds tens of ms per write; usually async + one\n  write region, or a globally-distributed DB (Spanner/CockroachDB) that does consensus per shard.\n• cascading replication (replica of a replica) to fan out without loading the primary.\n• monitoring: lag, WAL disk usage (a stuck replica or an unconsumed replication slot fills the\n  primary\'s disk and takes it DOWN), failover events, backup success + last successful restore test.</code></pre>',
    try: [['📖 GitLab post-mortem (database incident, 2017)', 'https://about.gitlab.com/blog/2017/02/10/postmortem-of-database-outage-of-january-31/', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Synchronous vs asynchronous replication — the trade-off?\nA: Async: low write latency, but on primary failure you lose transactions the replicas had not\n   received (RPO &gt; 0). Sync: zero loss on single-node failure, but each write pays a replica\n   round-trip and can stall if the replica is unavailable. Quorum/semi-sync is the middle ground.\n\nQ: A user updates a setting and immediately does not see it. Why, and how do you fix it?\nA: Replication lag — the read hit an async replica that has not applied the change. Fix:\n   read-your-writes routing (send that user\'s reads to the primary for a short window), a\n   consistency/session token the replica can wait on, or read from the primary in that request.\n\nQ: What is split-brain and how is it prevented?\nA: Two nodes both believe they are the primary and both accept writes, diverging the data.\n   Prevented with fencing (STONITH), a consensus-backed leader lease (only the node holding the\n   lease may write), or requiring a quorum to be reachable before accepting writes.\n\nQ: Is a read replica a backup?\nA: No. It replicates every change, including an accidental DROP TABLE, in seconds. You need\n   independent backups (ideally immutable/offsite) plus PITR to recover to just before a mistake.\n\nQ: Define RPO and RTO.\nA: RPO (recovery point objective): the maximum acceptable data loss, measured in time — set by\n   backup interval + replication lag. RTO (recovery time objective): the maximum acceptable\n   downtime to restore service.\n\nQ: A replication slot on the primary is not being consumed. What is the risk?\nA: The primary retains all WAL for that slot indefinitely, filling its disk and eventually\n   crashing. Monitor slot lag; drop abandoned slots; set max_slot_wal_keep_size.\n\nQ: How do you scale reads without giving stale data to writers?\nA: Route each user\'s reads to the primary for a bounded time after their writes (or use a\n   consistency token); send everything else to replicas; monitor and cap acceptable lag.</code></pre>',
    try: [['📖 AWS: RDS Multi-AZ vs read replicas', 'https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.MultiAZ.html', 'o']] } ],
  quiz: [
    { q: 'Your app writes to the primary then reads from an async replica and gets stale data. This is...', opts: ['A bug in the replica', 'Replication lag — fix with read-your-writes routing (primary reads briefly after a write) or a consistency token', 'A deadlock', 'Split-brain'], ok: 1, why: 'Async replicas apply changes after the primary commits. Read-after-write consistency is an application responsibility: route the writing user\'s subsequent reads to the primary, or make the read wait for the replica to reach the write\'s position.' },
    { q: 'Why is a read replica NOT a substitute for backups?', opts: ['Replicas are slower', 'It replicates all changes including destructive mistakes (DROP TABLE, bad UPDATE) within seconds, so it cannot recover you to a point before the error', 'Replicas cannot be promoted', 'It has no indexes'], ok: 1, why: 'A replica is a live mirror. Recovering from human error or corruption requires independent, ideally immutable backups plus point-in-time recovery (base backup + WAL replay to just before the incident).' },
    { q: 'A replication slot on the primary stops being consumed (its replica died). What happens if unnoticed?', opts: ['Nothing, Postgres cleans it up', 'The primary retains WAL for that slot indefinitely, its disk fills, and the primary crashes', 'Reads fail on the primary immediately', 'The slot auto-promotes to primary'], ok: 1, why: 'Replication slots guarantee WAL is kept until the consumer acknowledges it. An abandoned slot causes unbounded WAL growth on the primary. Monitor slot lag and set max_slot_wal_keep_size to cap the damage.' } ] },

/* ============================ 5. PARTITIONING & SHARDING ============================ */
{ num: 5, emoji: '✂️', title: 'Partitioning & Sharding', layer: 'Distributed',
  tagline: 'Split one big table across partitions (same node) or shards (many nodes) — and pray you pick the right key.',
  apps: ['a table too big for one node', 'multi-tenant isolation', 'time-series data lifecycle'],
  levels: [
  { html:
    '<p><b>Partitioning</b> = splitting one table into pieces on the SAME database (a management/perf feature). <b>Sharding</b> = splitting data across SEPARATE databases/nodes (a scale feature; the app or a router decides which shard holds a row).</p>' +
    '<pre><code>VERTICAL split:   move rarely-used or huge columns to another table (hot vs cold columns);\n                 or split by feature (users DB, orders DB, analytics DB)\nHORIZONTAL split:  rows go to different partitions/shards by a PARTITION/SHARD KEY\n  RANGE   by created_at month, by id 0-1M / 1M-2M — great for time-series + dropping old data\n  HASH    hash(user_id) % N — even distribution, but range scans hit every shard\n  LIST    by region = \'EU\' / \'US\' — explicit buckets, good for data residency\n  DIRECTORY  a lookup service maps key → shard — flexible, adds a hop + a SPOF to make HA</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>One overflowing filing cabinet vs a room of cabinets.</b> Partitioning is adding drawers to the cabinet. Sharding is buying more cabinets and agreeing a rule for which cabinet each folder goes in.</p></div>',
    try: [['📚 DDIA ch.6: Partitioning', 'https://dataintensive.net/', 'o'], ['📘 Part 4: distributed training / Spark partitions', '../learn4/#ch15', 'o']] },
  { html:
    '<p><b>Choosing a shard key</b> is the decision you cannot easily undo. Good keys are:</p>' +
    '<pre><code>HIGH CARDINALITY   many distinct values → even spread\nUNIFORMLY ACCESSED  no single value is 90% of traffic (avoid "status", "country=US", "is_active")\nPRESENT in most queries  so the router can target ONE shard (a query without the key = scatter-gather\n                        across ALL shards, then merge — slow, and it does not scale)\nSTABLE             the key should not change (moving a row between shards is painful)\n\ncommon picks:  tenant_id / customer_id (multi-tenant SaaS) · user_id (consumer apps) ·\n               a hash of the entity id · (tenant_id, entity_id) composite\nAVOID:  auto-increment id as a RANGE key → all new writes hammer the last shard (hotspot).</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Sorting mail by first letter of surname.</b> Fine until you notice half your customers are named "Smith" — that sorter is buried while the others idle. Cardinality and uniformity, not just "a column that exists".</p></div>',
    try: [['📖 Citus: distributed Postgres / shard keys', 'https://docs.citusdata.com/en/stable/sharding/data_modeling.html', 'o']] },
  { html:
    '<p>The hard parts:</p>' +
    '<pre><code>RESHARDING     going from 8 → 16 shards means moving ~half the data while serving traffic.\n  hash % N is the worst — almost every key moves. CONSISTENT HASHING (or virtual buckets:\n  hash to 4096 buckets, map buckets → shards) moves only ~1/N of keys.\nCROSS-SHARD QUERIES  JOINs and aggregations that span shards need scatter-gather + a merge layer,\n  or denormalisation, or co-locating related rows on the same shard (e.g. shard orders by their\n  customer_id so a customer + their orders are together).\nCROSS-SHARD TRANSACTIONS  need 2PC or sagas (ch.3); most sharded systems avoid them by design.\nGLOBAL SECONDARY INDEXES / uniqueness  "unique email across all shards" needs a separate index\n  shard or a dedicated lookup table.\nHOTSPOTS       a celebrity user, a huge tenant → that shard melts. Mitigate: split the hot key,\n  cache it, or give big tenants a dedicated shard.\nOPERATIONS     N shards = N times the backups, migrations, monitoring, and failover paths.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>A B2B SaaS shards by <code>hash(tenant_id)</code>. One enterprise tenant is 40% of all data and its shard is constantly at 100% CPU. Fix: pull that tenant onto its own dedicated shard (the directory/lookup approach makes this a config change, not a re-hash).</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>Events table sharded by <code>range(event_id)</code> (auto-increment). Every insert goes to the newest shard → one shard at 100% writes, the rest idle. Fix: shard by <code>hash(event_id)</code> or by <code>(tenant_id, time_bucket)</code>; keep RANGE partitioning only for the time-based retention drop.</p></div>',
    try: [['📖 Consistent hashing', 'https://www.toptal.com/big-data/consistent-hashing', 'o']] },
  { html:
    '<p>Do you even need to shard? — and the alternatives:</p>' +
    '<pre><code>BEFORE sharding, exhaust:  bigger box (vertical scale) · read replicas · caching · archiving cold\n  data · a better schema/index · connection pooling · moving analytics off the OLTP DB.\nsharding adds permanent complexity to every query, migration, and on-call page.\n\nMANAGED / AUTO-SHARDED options remove most of the pain:  DynamoDB, Cassandra, MongoDB sharded\n  clusters, Vitess (MySQL), Citus (Postgres), CockroachDB / Spanner / YugabyteDB (they shard\n  ranges automatically and rebalance).\nTIME-SERIES: RANGE partition by time + DETACH/DROP old partitions = instant, index-free deletion\n  of old data (vs a DELETE that bloats the table).\nMULTI-TENANT: one shard per big tenant, a shared "pool" shard for small tenants; or schema-per-tenant\n  for isolation at the cost of migrations x N.</code></pre>',
    try: [['📖 Notion: sharding Postgres', 'https://www.notion.so/blog/sharding-postgres-at-notion', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Partitioning vs sharding?\nA: Partitioning splits a table into pieces within one database (for manageability, pruning, and\n   fast bulk delete). Sharding distributes data across independent nodes to scale beyond one\n   machine\'s capacity; a routing layer maps each key to its shard.\n\nQ: What makes a good shard key?\nA: High cardinality, uniform access (no hot value), present in almost every query so requests\n   target a single shard, and stable so rows do not migrate. Examples: tenant_id, user_id, a\n   hash of the entity id.\n\nQ: Why is hash(key) % N a bad way to assign shards long-term?\nA: Adding or removing a shard changes N, so almost every key remaps and you must move most of the\n   data. Consistent hashing or fixed virtual buckets remapped to shards moves only ~1/N of keys.\n\nQ: What is a scatter-gather query and why avoid it?\nA: A query that lacks the shard key, so the router must ask every shard and merge results.\n   Latency is bounded by the slowest shard, throughput does not scale with shard count, and it\n   gets worse as you add shards.\n\nQ: One shard is at 100% CPU because of a single huge tenant. Options?\nA: Move that tenant to a dedicated shard (easy with a directory/lookup mapping), split its data\n   further (sub-shard by a secondary key), add caching for its hot reads, or apply per-tenant\n   rate limits.\n\nQ: How do you delete a year of old time-series data cheaply?\nA: RANGE-partition by time and DROP (or DETACH) the old partitions — a metadata operation. A bulk\n   DELETE scans and marks millions of rows, generates WAL, and bloats the table and indexes.\n\nQ: How do you enforce a globally unique email across shards?\nA: A dedicated lookup table/index sharded by email (or hash of email) that reserves the value, or\n   a separate uniqueness service; you cannot rely on a local UNIQUE constraint per shard.</code></pre>',
    try: [['📖 Vitess (YouTube\'s MySQL sharding)', 'https://vitess.io/docs/overview/whatisvitess/', 'o']] } ],
  quiz: [
    { q: 'You shard an events table by range on an auto-increment event_id. What goes wrong?', opts: ['Reads become impossible', 'All new inserts target the highest-id shard, creating a write hotspot while other shards idle', 'Foreign keys break', 'The index gets too small'], ok: 1, why: 'Range-partitioning on a monotonically increasing key sends every new row to the last partition/shard. Hash the id (or shard by tenant/time-bucket) for even write distribution; keep range only for time-based retention.' },
    { q: 'A query does not include the shard key. What is the consequence in a sharded system?', opts: ['It runs only on the primary', 'It becomes a scatter-gather: every shard is queried and results merged, so latency tracks the slowest shard and it does not scale with shard count', 'It fails with a syntax error', 'It automatically picks the right shard'], ok: 1, why: 'Without the shard key the router cannot localize the query, so it fans out to all shards. This is bounded by the slowest shard and gets worse as you add shards — the main reason the shard key must appear in nearly all queries.' },
    { q: 'Before sharding a growing Postgres database, which should you generally try first?', opts: ['Immediately shard by hash(user_id)', 'Vertical scale-up, read replicas, caching, archiving cold data, schema/index fixes, moving analytics off OLTP', 'Switch to a graph database', 'Disable indexes'], ok: 1, why: 'Sharding permanently complicates every query, migration, and incident. Exhaust the simpler levers first; only shard when a single node genuinely cannot hold the working set or write throughput, and prefer an auto-sharding system if you must.' } ] },

/* ============================ 6. CAP, PACELC & CONSISTENCY MODELS ============================ */
{ num: 6, emoji: '⚖️', title: 'CAP, PACELC & Consistency Models', layer: 'Distributed',
  tagline: 'The theorem everyone half-remembers, the one that fixes it, and the spectrum from linearizable to eventual.',
  apps: ['"is this system CP or AP?"', 'choosing quorum settings', 'reasoning about stale reads'],
  levels: [
  { html:
    '<p><b>CAP:</b> when a network <b>P</b>artition splits your nodes, you must choose between <b>C</b>onsistency (every read sees the latest write, or an error) and <b>A</b>vailability (every request gets a non-error answer, possibly stale). You cannot have both <i>during a partition</i>.</p>' +
    '<pre><code>CP systems:  refuse writes/reads on the minority side to stay consistent\n             — HBase, etcd/ZooKeeper, MongoDB (majority), Spanner\nAP systems:  keep serving on both sides, reconcile later\n             — Cassandra, DynamoDB (eventual), Riak\nCA:  only meaningful with NO partitions — i.e. a single node. Not a real distributed choice.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Two shop tills lose their link.</b> CP: both stop selling the last item until they can sync (no oversell, no sales). AP: both keep selling (customers served, but you might sell the same item twice and sort it out later).</p></div>',
    try: [['📖 Please stop calling databases CP or AP (Kleppmann)', 'https://martin.kleppmann.com/2015/05/11/please-stop-calling-databases-cp-or-ap.html', 'o']] },
  { html:
    '<p><b>PACELC</b> fixes CAP\'s biggest gap — it says nothing about normal operation:</p>' +
    '<pre><code>if Partition:  choose Availability  or  Consistency        (the CAP part)\nElse (normal): choose Latency        or  Consistency        (the part CAP ignores)\n\nDynamoDB / Cassandra:  PA / EL  — available under partition, low-latency (eventual) otherwise\nMongoDB (majority):    PC / EC  — consistent under partition, consistent (slower) otherwise\nSpanner:               PC / EC  — consistency always, paid for with TrueTime latency\nPostgres (single):     no P to speak of; sync replica adds EC latency, async adds EL staleness</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>CAP is the fire-drill policy; PACELC is also the everyday policy.</b> Most of the time there is no fire — PACELC forces you to admit that "always consistent" costs you latency on every single request, not just during outages.</p></div>',
    try: [['📖 PACELC (Abadi)', 'https://en.wikipedia.org/wiki/PACELC_theorem', 'o']] },
  { html:
    '<p>The <b>consistency spectrum</b> (strongest to weakest):</p>' +
    '<pre><code>LINEARIZABLE (strong)   there is a single global order; once a write completes, every later read\n                       sees it. Behaves like one copy. Needs consensus (Raft/Paxos) → latency.\nSEQUENTIAL              all nodes see operations in the SAME order, but not necessarily real-time.\nCAUSAL                  operations that are causally related are seen in order everywhere;\n                       concurrent ones may differ. "Reply appears after the message it answers."\nREAD-YOUR-WRITES / monotonic reads / monotonic writes  — useful SESSION guarantees, weaker than causal\nEVENTUAL               if writes stop, all replicas converge... eventually. No ordering promise.\n</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>Cassandra with <code>RF=3</code>. Write at <code>QUORUM</code> (2), read at <code>QUORUM</code> (2): <code>W + R = 4 &gt; 3 = N</code> → any read overlaps a node that saw the latest write → strong-ish consistency. Drop the read to <code>ONE</code> for speed and you can read stale data; a background <b>read repair</b> and <b>hinted handoff</b> heal it later.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>A shopping cart on Dynamo-style storage: two devices add items during a partition → two conflicting versions (siblings). Last-write-wins would silently drop an item. Better: a <b>CRDT</b> (an OR-Set) or app-level merge that unions the carts, so no add is lost.</p></div>',
    try: [['📖 Jepsen: consistency models map', 'https://jepsen.io/consistency', 'o']] },
  { html:
    '<p>Reconciliation and the practical knobs:</p>' +
    '<pre><code>QUORUM math (leaderless):  W + R &gt; N  → overlap → latest value seen.  W = N → durable but no write\n  availability if any node down.  R = 1, W = 1 → fast, very stale-prone.\nVERSIONING:  vector clocks / version vectors detect concurrent writes → surface siblings to the app.\nLAST-WRITE-WINS:  simple, but needs synced clocks and SILENTLY discards a concurrent write. Fine\n  for "user profile", dangerous for "set of things".\nCRDTs:  data types (counters, sets, maps) that merge deterministically with no coordination →\n  strong eventual consistency. Powers collaborative editing, offline-first apps.\nREAD REPAIR / anti-entropy / Merkle trees:  background processes that find and fix divergent replicas.\nCLOCKS:  never trust wall-clock ordering across machines (NTP skew). Use logical clocks, or\n  Spanner\'s TrueTime (GPS + atomic clocks + an uncertainty interval it waits out).</code></pre>',
    try: [['📚 DDIA ch.9: Consistency and Consensus', 'https://dataintensive.net/', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: State CAP precisely.\nA: In the presence of a network partition, a distributed data store cannot be both consistent\n   (linearizable) and available (every non-failing node answers). Absent a partition, the trade\n   does not apply — which is why CAP alone is an incomplete model.\n\nQ: What does PACELC add?\nA: It also characterises NORMAL operation: Else (no partition), you trade Latency vs Consistency.\n   E.g. Cassandra is PA/EL, Spanner is PC/EC. It captures that strong consistency has a\n   latency cost on every request, not just during failures.\n\nQ: In Cassandra with N=3, what R and W give you consistent reads, and what is the cost?\nA: Any W and R with W + R &gt; 3, e.g. QUORUM/QUORUM (2/2). Cost: each op waits for 2 nodes, and\n   you lose availability for that op if 2+ replicas are down.\n\nQ: Linearizable vs causal vs eventual consistency?\nA: Linearizable: a single real-time global order; reads see the latest completed write (needs\n   consensus). Causal: causally related ops are ordered everywhere; concurrent ops may be seen in\n   different orders. Eventual: replicas converge if writes stop, with no ordering guarantee.\n\nQ: Why is last-write-wins risky for a shopping cart?\nA: Two concurrent writes → the "loser" (by timestamp, which may be clock-skewed) is silently\n   discarded, dropping items the user added. A CRDT set or an explicit merge preserves all adds.\n\nQ: Why not trust timestamps to order events across servers?\nA: Clock skew (NTP is only ~milliseconds accurate, and can jump). Two "later" events can get\n   earlier timestamps. Use logical clocks / version vectors, or a system like TrueTime that\n   bounds and waits out the uncertainty.\n\nQ: Give an example where AP is the right call.\nA: A shopping cart, "likes" count, presence/typing indicators, DNS, a CDN — availability and\n   low latency matter more than every reader seeing the absolute latest value instantly.</code></pre>',
    try: [['📖 Jepsen analyses (real DB consistency tests)', 'https://jepsen.io/analyses', 'o']] } ],
  quiz: [
    { q: 'CAP says that DURING a network partition you must sacrifice one of...', opts: ['Latency or throughput', 'Consistency (linearizable reads) or Availability (every non-failing node still answers)', 'Durability or atomicity', 'Reads or writes'], ok: 1, why: 'When nodes cannot communicate, a system either refuses some requests to avoid serving stale/divergent data (CP) or keeps answering on both sides and reconciles later (AP). "CA" only describes a non-distributed single node.' },
    { q: 'What does PACELC capture that plain CAP does not?', opts: ['Backup frequency', 'The consistency-vs-latency trade-off during NORMAL (non-partitioned) operation', 'The number of replicas', 'Index selection'], ok: 1, why: 'CAP only describes behaviour during a partition. PACELC adds: Else (no partition), choose Latency or Consistency — acknowledging that strong consistency costs coordination latency on every request even when nothing is broken.' },
    { q: 'In a leaderless store with N=3 replicas, which read/write quorum guarantees a read overlaps the latest write?', opts: ['R=1, W=1', 'R=2, W=2 (since W + R = 4 > 3 = N)', 'R=1, W=3 only', 'Any R and W'], ok: 1, why: 'The overlap condition is W + R > N. With N=3, QUORUM writes (2) and QUORUM reads (2) sum to 4 > 3, so the read set and the write set share at least one node that has the newest value.' } ] },

/* ============================ 7. POSTGRESQL IN PRODUCTION ============================ */
{ num: 7, emoji: '🐘', title: 'PostgreSQL in Production', layer: 'Relational',
  tagline: 'The interview-favourite RDBMS: MVCC & vacuum, connection limits, JSONB, and the ways it falls over.',
  apps: ['most new OLTP systems default here', '"why is Postgres slow / bloated?"', 'pgvector / PostGIS / partitioning'],
  levels: [
  { html:
    '<p>PostgreSQL is a single-primary, ACID, MVCC relational database with an unusually rich type system and an extension ecosystem. It is the safe default for OLTP unless you have a specific reason not to.</p>' +
    '<pre><code>strengths:  strict correctness · rich types (arrays, JSONB, ranges, geometry) · powerful indexes\n            (GIN/GiST/BRIN/partial/expression) · window functions & CTEs · extensions\n            (pgvector for embeddings, PostGIS for geo, TimescaleDB, Citus for sharding) · logical replication\nwatch-outs: one primary for writes (scale reads with replicas) · connection-per-backend model\n            (needs a pooler) · vacuum/bloat if misconfigured · schema changes can lock</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A Swiss Army knife that is also a good chef\'s knife.</b> It does far more than the average app needs, and it does the core job (transactional correctness) extremely well.</p></div>',
    try: [['📖 PostgreSQL documentation', 'https://www.postgresql.org/docs/current/', 'o'], ['📘 Part 3: pandas ↔ SQL, DuckDB', '../learn3/#ch2', 'o']] },
  { html:
    '<p><b>MVCC and VACUUM</b> — the thing that causes most Postgres surprises:</p>' +
    '<pre><code>an UPDATE does not overwrite — it writes a NEW row version and marks the old one dead.\nDELETE just marks dead. Readers with older snapshots still need the old versions.\nVACUUM reclaims dead tuples; ANALYZE refreshes planner statistics; autovacuum does both automatically.\n\nBLOAT:  autovacuum can\'t keep up (high churn table, too-conservative settings) → the table &amp;\n        its indexes grow with dead space → slower scans, more I/O.\nLONG-RUNNING TRANSACTIONS &amp; abandoned replication slots hold back the "xmin horizon" → vacuum\n        cannot remove tuples newer than the oldest snapshot in the system → bloat everywhere.\nTXID WRAsraround:  32-bit transaction ids; if autovacuum never freezes old rows, at ~2B txns the\n        DB shuts down to protect itself. Monitor age(datfrozenxid).</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A shared document where edits leave the old text struck through until everyone reading the old version finishes.</b> If one person never closes the doc, the crossed-out text can never be cleaned up and the file balloons.</p></div>',
    try: [['📖 Postgres: routine vacuuming', 'https://www.postgresql.org/docs/current/routine-vacuuming.html', 'o']] },
  { html:
    '<p>Production essentials:</p>' +
    '<pre><code>CONNECTIONS:  each connection = a backend process (~few MB + planning overhead). 5000 app\n  connections will melt Postgres. Use PgBouncer (transaction pooling) → a few hundred real\n  backends serve thousands of clients. Size the pool ~= (cores * 2) + effective_spindles-ish, then measure.\nCONFIG that matters:  shared_buffers (~25% RAM), effective_cache_size (~50-75%), work_mem\n  (per sort/hash — beware N concurrent), max_wal_size, checkpoint_timeout, random_page_cost\n  (lower on SSD/cloud), autovacuum_vacuum_scale_factor (lower for big hot tables).\nOBSERVABILITY:  pg_stat_statements (top queries by total time), pg_stat_activity (what is running\n  / blocked / idle-in-transaction), pg_locks, pg_stat_user_tables (n_dead_tup, last_autovacuum),\n  pg_stat_replication (lag). Log slow queries (log_min_duration_statement).\nJSONB:  schemaless columns with GIN indexing and operators; do not use it to avoid modelling the\n  parts you query on.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>P95 latency spikes every few minutes. <code>pg_stat_activity</code> shows bursts of "active" backends. Cause: no pooler, the app opens a connection per request under load, and Postgres spends time on process creation + lock contention. Fix: PgBouncer in transaction mode, cap the app pool.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>A 2 TB table is 60% dead tuples; queries that should be index scans read gigabytes. Cause: a nightly bulk <code>UPDATE</code> of every row + default autovacuum that never catches up. Fix: tune autovacuum for that table (scale_factor 0.01, more workers, higher cost limit), batch the update, and <code>VACUUM (FULL)</code> / pg_repack once to reclaim.</p></div>',
    try: [['📖 PgBouncer', 'https://www.pgbouncer.org/', 'o'], ['📖 pgtune (starting config)', 'https://pgtune.leopard.in.ua/', 'o']] },
  { html:
    '<p><b>Schema changes without downtime</b> (Postgres locks are the classic outage):</p>' +
    '<pre><code>SAFE-ish:  ADD COLUMN ... (no default, or a constant default on PG11+) · CREATE INDEX CONCURRENTLY\n           · ADD CONSTRAINT ... NOT VALID then VALIDATE CONSTRAINT (short lock) · DROP an unused index\nDANGEROUS: ADD COLUMN with a volatile default (rewrites the table) · ALTER TYPE (rewrite + full lock)\n           · adding a NOT NULL to a big table without a prepared check · a plain CREATE INDEX\n           (ACCESS EXCLUSIVE-ish, blocks writes) · ALTER TABLE that takes ACCESS EXCLUSIVE behind a\n           long autovacuum/txn → the whole table queues.\nPATTERN (expand/contract):  1) add the new nullable column/table  2) backfill in batches\n  3) dual-write from the app  4) switch reads  5) drop the old column later.\nalways set lock_timeout for migrations so a blocked DDL fails fast instead of freezing the table\nbehind it (a queued ACCESS EXCLUSIVE lock blocks every new reader too).</code></pre>',
    try: [['📖 Strong Migrations / "safe operations" list', 'https://github.com/ankane/strong_migrations', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Why does an UPDATE-heavy Postgres table get slow over time?\nA: MVCC keeps dead row versions until VACUUM reclaims them. If autovacuum cannot keep pace (churn\n   too high, settings too conservative, or a long transaction / stale replication slot holds the\n   xmin horizon), the table and its indexes bloat with dead space, inflating I/O.\n\nQ: Your app has 4000 connections to Postgres and it is struggling. Fix?\nA: Postgres forks a process per connection; thousands cause memory pressure and lock contention.\n   Put PgBouncer (transaction pooling) in front so a few hundred real backends serve all clients,\n   and cap the application connection pool.\n\nQ: How do you add an index to a large, live table without blocking writes?\nA: CREATE INDEX CONCURRENTLY (it does not take a write-blocking lock, at the cost of two table\n   scans and not being usable inside a transaction). Watch for it leaving an INVALID index if it fails.\n\nQ: What is transaction ID wraparound and why does it matter?\nA: Postgres uses 32-bit txids compared circularly. If old rows are never frozen (autovacuum\n   disabled/failing), approaching ~2 billion transactions the database refuses new writes to\n   avoid data corruption. Monitor relfrozenxid age and let autovacuum freeze.\n\nQ: When is JSONB the right choice, and when is it a smell?\nA: Right: genuinely variable, sparse, or user-defined attributes you rarely filter on; document\n   payloads. Smell: putting columns you frequently query, join, or constrain into JSONB to skip\n   schema design — you lose types, FKs, and clean indexing.\n\nQ: A migration froze the whole table. What happened?\nA: The DDL needed an ACCESS EXCLUSIVE lock but could not get it immediately (a long query or\n   autovacuum held a conflicting lock); the queued lock request then blocked every new\n   transaction on that table. Use lock_timeout and run heavy DDL in maintenance windows.\n\nQ: Name three views you check first when Postgres is slow.\nA: pg_stat_statements (worst queries by total/avg time), pg_stat_activity (running / blocked /\n   idle-in-transaction sessions), and pg_stat_user_tables + pg_stat_replication (dead tuples,\n   autovacuum recency, replica lag).</code></pre>',
    try: [['📖 pganalyze: Postgres performance', 'https://pganalyze.com/blog', 'o']] } ],
  quiz: [
    { q: 'An UPDATE-heavy Postgres table has become slow and much larger on disk than its live row count suggests. Most likely cause?', opts: ['Missing primary key', 'Table/index bloat from dead tuples that autovacuum has not reclaimed (often held back by a long transaction or stale replication slot)', 'Too many replicas', 'JSONB columns'], ok: 1, why: 'MVCC writes new versions and marks old ones dead. If VACUUM cannot keep up — or the xmin horizon is pinned by a long-running transaction or an abandoned replication slot — dead space accumulates, bloating the heap and indexes.' },
    { q: 'Why put PgBouncer in front of PostgreSQL?', opts: ['To encrypt traffic', 'Postgres uses one OS process per connection; pooling lets a few hundred backends serve thousands of client connections, avoiding memory pressure and contention', 'To add sharding', 'To enable JSONB'], ok: 1, why: 'The connection-per-process model does not scale to thousands of clients. A transaction-mode pooler multiplexes many short-lived client sessions onto a small set of real server connections.' },
    { q: 'You need to add an index to a 500M-row table serving live writes. Correct approach in Postgres?', opts: ['CREATE INDEX (plain) during peak traffic', 'CREATE INDEX CONCURRENTLY, accepting the extra scan cost and that it cannot run inside a transaction block', 'Drop and recreate the table', 'Add the index only to a replica'], ok: 1, why: 'A plain CREATE INDEX takes a lock that blocks writes for the duration. CONCURRENTLY builds the index without that lock (two passes, slower), which is the standard way to index a live table.' } ] },

/* ============================ 8. KEY-VALUE & IN-MEMORY ============================ */
{ num: 8, emoji: '⚡', title: 'Key-Value & In-Memory (Redis, DynamoDB)', layer: 'NoSQL',
  tagline: 'O(1) lookups by key: caches, sessions, counters, queues, leaderboards — and the hot-partition trap.',
  apps: ['caching layer in front of a DB', 'sessions, rate limiters, leaderboards', 'DynamoDB as a primary store'],
  levels: [
  { html:
    '<p>A key-value store maps a key directly to a value with O(1) access and no query language beyond get/put/delete. <b>Redis</b> is an in-memory data-structure server (also does strings, hashes, sorted sets, streams, pub/sub). <b>DynamoDB</b> is a managed, horizontally-partitioned KV/document store.</p>' +
    '<pre><code>Redis structures:  string · hash (object) · list (queue) · set · sorted set (leaderboard, rate\n                   window) · stream (log) · bitmap / HyperLogLog (cardinality) · geo\ntypical uses:  cache-aside for a SQL DB · session store · rate limiting (INCR + EXPIRE) ·\n               leaderboards (ZADD/ZRANGE) · a lightweight job queue · pub/sub / fan-out\nDynamoDB:  partition key (required, decides the shard) + optional sort key; single-digit-ms at\n           any scale; you provision or pay per request; you design tables around ACCESS PATTERNS.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A coat check.</b> You hand over a coat, get a numbered ticket, and later the ticket returns exactly that coat — instantly, no searching. It cannot answer "which coats are blue"; only "give me ticket 42".</p></div>',
    try: [['📖 Redis data types', 'https://redis.io/docs/latest/develop/data-types/', 'o'], ['📘 Part 2: LLM caching (semantic + exact + prefix)', '../learn2/#ch13', 'o']] },
  { html:
    '<p><b>Redis persistence and eviction</b> — the two settings people get wrong:</p>' +
    '<pre><code>PERSISTENCE:  RDB (periodic point-in-time snapshot — compact, can lose the last few minutes) ·\n              AOF (append every write — durable to ~1s with everysec fsync, larger, replayed on\n              restart) · both together is common. Pure cache: persistence off.\nEVICTION (when maxmemory is hit):  noeviction (errors on write — safe for a primary store) ·\n              allkeys-lru / allkeys-lfu (evict cold keys — for a cache) · volatile-* (only keys\n              with a TTL). LFU is usually better than LRU for skewed access.\nTTL:  set EXPIRE on cache entries; a key with no TTL lives forever and can fill memory.\nSINGLE-THREADED command execution:  one slow command (KEYS *, a big SORT, a Lua script) blocks\n              every other client. Use SCAN, not KEYS. Keep values and operations small.\nPIPELINING / MULTI:  batch commands to cut round-trips; Lua for atomic read-modify-write.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Redis is one very fast clerk.</b> Ask for one item and it is instant. Ask them to alphabetise the whole warehouse (KEYS *, big SORT) and the entire queue behind you waits.</p></div>',
    try: [['📖 Redis: key eviction', 'https://redis.io/docs/latest/develop/reference/eviction/', 'o']] },
  { html:
    '<p><b>Cache patterns</b> and their failure modes:</p>' +
    '<pre><code>CACHE-ASIDE (lazy):  app checks cache → miss → read DB → write cache (with TTL). Simple, common.\nREAD-THROUGH / WRITE-THROUGH:  the cache library owns DB access; write-through keeps them in sync\n  at write latency cost. WRITE-BEHIND: buffer writes, flush async (fast, risk of loss).\n\nfailures:\n  STAMPEDE / dogpile:  a hot key expires, 10k requests miss simultaneously and all hit the DB.\n    fixes: a mutex/lock so one request repopulates, "early recompute" before expiry, request\n    coalescing, a slightly randomised TTL.\n  STALE cache after a DB write:  invalidate (delete) the key on write, or write-through; TTL bounds\n    staleness. "Cache invalidation is one of the two hard problems."\n  THUNDERING HERD on restart / flush:  everything misses at once → warm the cache, or ramp traffic.\n  CACHE PENETRATION:  requests for keys that do not exist bypass the cache every time → cache the\n    negative result (with a short TTL) or a bloom filter of known keys.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>Right after a deploy, cache hit rate drops to ~0 and the primary DB CPU spikes to 100% — a cold cache stampede. Fixes: keep the cache in a separate process that survives deploys, pre-warm the top-N keys, add a per-key repopulation lock, and jitter TTLs so keys do not all expire together.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>DynamoDB table keyed by <code>order_status</code> as the partition key. 95% of rows are "COMPLETED" → that one partition is throttled while the rest sit idle (a <b>hot partition</b>). Fix: use a high-cardinality partition key (<code>order_id</code> or <code>customer_id</code>), put status in a GSI, or add a write-sharding suffix.</p></div>',
    try: [['📖 AWS: DynamoDB partition-key design', 'https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/bp-partition-key-design.html', 'o']] },
  { html:
    '<p><b>Redis Cluster / HA</b> and <b>DynamoDB modelling</b>:</p>' +
    '<pre><code>Redis HA:  Sentinel (monitors a primary + replicas, promotes on failure) or Cluster (data sharded\n  across 16384 hash slots, each slot on a primary+replicas). Cluster caveats: multi-key ops must\n  share a hash tag {user123}, cross-slot transactions are not supported, resharding moves slots live.\nRedis is (mostly) AP-ish: async replication → a failover can lose the last writes. Not a\n  system-of-record unless you accept that (or use AOF + WAIT + careful failover).\n\nDynamoDB SINGLE-TABLE DESIGN:  model ALL entities in ONE table using composite keys so one query\n  fetches a whole object graph (e.g. PK = USER#123, SK = PROFILE / ORDER#a / ORDER#b). GSIs give\n  alternate access paths. Because there are no joins and queries must hit a key, you enumerate\n  access patterns FIRST and design keys to serve them.\n  transactions (up to 100 items), TTL for auto-expiry, streams for CDC, on-demand vs provisioned\n  capacity, adaptive capacity smooths (but does not fix) hot keys.</code></pre>',
    try: [['📖 The DynamoDB Book / single-table design', 'https://www.dynamodbbook.com/', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: When is Redis a cache vs a primary datastore?\nA: Cache: data is derivable from a system of record, losing it is a performance hit not a\n   correctness bug, use LRU/LFU eviction + TTL, persistence optional. Primary store: only if you\n   accept in-memory cost, enable AOF, use noeviction, and tolerate the small data-loss window on\n   failover (or design around it).\n\nQ: RDB vs AOF persistence?\nA: RDB: periodic binary snapshot — small, fast restore, can lose everything since the last\n   snapshot. AOF: logs every write, fsync every second by default — durable to ~1s, larger file,\n   slower restart (replay). Many run both.\n\nQ: Why is calling KEYS * in production dangerous?\nA: Redis executes commands single-threaded; KEYS scans the entire keyspace and blocks every other\n   client for the duration. Use SCAN (cursor-based, incremental) instead.\n\nQ: Explain a cache stampede and two mitigations.\nA: A popular key expires and many concurrent requests all miss and hit the backend at once.\n   Mitigate with a per-key lock so only one request recomputes (others wait or serve stale),\n   early/probabilistic recomputation before expiry, and randomised TTLs.\n\nQ: A DynamoDB table is being throttled but average utilisation is low. Why?\nA: A hot partition — the partition key has low cardinality or a skewed value, so one partition\n   receives most traffic and hits its per-partition limit. Choose a higher-cardinality key, move\n   the low-cardinality attribute to a GSI, or add a sharding suffix.\n\nQ: What is single-table design in DynamoDB and why?\nA: Storing multiple entity types in one table with carefully composed partition/sort keys so a\n   single Query returns a related set of items without joins. It exists because DynamoDB has no\n   joins and every efficient access must be key-based — so you design keys around known queries.\n\nQ: Redis Cluster — one thing you lose vs single-node?\nA: Multi-key atomic operations/transactions only work if all keys hash to the same slot (use a\n   {hashtag}); arbitrary cross-slot MULTI/Lua is not supported.</code></pre>',
    try: [['📖 Redis Cluster spec', 'https://redis.io/docs/latest/operate/oss_and_stack/reference/cluster-spec/', 'o']] } ],
  quiz: [
    { q: 'Right after a deploy, your database CPU hits 100% and Redis hit-rate is near zero. Most likely?', opts: ['A slow query regression', 'Cold-cache stampede — the cache restarted empty and every request now misses and hits the DB simultaneously', 'Replication lag', 'A deadlock'], ok: 1, why: 'If the cache process restarts with the app, it comes back empty; all traffic misses at once and stampedes the backend. Keep the cache out-of-process, pre-warm hot keys, add per-key repopulation locks, and jitter TTLs.' },
    { q: 'A DynamoDB table uses "country" as its partition key and is throttled despite low overall usage. Fix?', opts: ['Increase provisioned RCUs 10x', 'Choose a high-cardinality partition key (e.g. user_id); move "country" to a GSI or add a write-sharding suffix', 'Enable RDB persistence', 'Add more replicas'], ok: 1, why: 'Low-cardinality partition keys concentrate traffic onto a few partitions, each of which has its own throughput ceiling. Spreading load requires a key with many evenly-accessed values.' },
    { q: 'Why must you avoid KEYS * against a production Redis?', opts: ['It returns too much data over the network only', 'Redis runs commands single-threaded, so scanning the whole keyspace blocks all other clients until it finishes; use SCAN instead', 'It bypasses persistence', 'It resets all TTLs'], ok: 1, why: 'A single long command monopolises Redis\'s one command thread. SCAN returns keys incrementally with a cursor, letting other clients interleave.' } ] },

/* ============================ 9. DOCUMENT DATABASES ============================ */
{ num: 9, emoji: '📄', title: 'Document Databases (MongoDB, Firestore)', layer: 'NoSQL',
  tagline: 'Store objects as documents. Embed what you read together; reference what changes independently.',
  apps: ['content / catalog / CMS-style data', 'fast-moving schemas / prototypes', 'per-user aggregates & profiles'],
  levels: [
  { html:
    '<p>A document database stores self-contained records (BSON/JSON) with nested structure, grouped into <b>collections</b>. No fixed schema per row; each document can differ. Access is by <code>_id</code> or by indexed fields; there are no server-side joins in the relational sense (MongoDB has <code>$lookup</code> but it is a left-join within one server, not the point of the model).</p>' +
    '<pre><code>{ _id: "u_123",\n  name: "Ada",\n  address: { city: "London", zip: "EC1" },      // embedded sub-document\n  roles: ["admin", "billing"],                    // array\n  lastLogin: ISODate("2024-01-01T09:00:00Z") }</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A folder of forms vs a wall of index cards.</b> Relational spreads one order across an orders card, several order_items cards, a customer card. A document keeps the whole order as one filled-in form you pull in a single grab.</p></div>',
    try: [['📖 MongoDB data modeling', 'https://www.mongodb.com/docs/manual/data-modeling/', 'o']] },
  { html:
    '<p><b>Embed vs reference</b> — the core design decision, driven by <i>how you read the data</i>:</p>' +
    '<pre><code>EMBED when:  the child is always loaded WITH the parent · it does not grow unbounded · it is owned\n             by the parent (an order\'s line items, a post\'s address, a user\'s settings)\n             → one read, no join, atomic single-document update\nREFERENCE when:  the child is large, shared, or queried on its own · it changes independently ·\n             the array would grow without limit (a user\'s activity log, a product\'s reviews)\n             → store an id, do a second query or $lookup\n\nANTI-PATTERN:  the unbounded array — embedding "comments" in a "post" that gets 100k comments →\n  the document blows past the 16 MB limit and every update rewrites the whole thing. Reference\n  comments in their own collection.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Staples vs a filing reference.</b> Staple the receipt to the expense report (embed) — they travel together. Write "see contract #4471" (reference) for the 80-page contract — you do not photocopy it into every memo.</p></div>',
    try: [['📖 MongoDB: embed vs reference', 'https://www.mongodb.com/docs/manual/tutorial/model-embedded-one-to-many-relationships-between-documents/', 'o']] },
  { html:
    '<p>Querying, indexing, transactions:</p>' +
    '<pre><code>INDEXES:  single-field, compound (leftmost-prefix rule, same as SQL), multikey (on array fields),\n          text, geo, partial, TTL (auto-expire docs). Covered queries answer from the index alone.\nAGGREGATION PIPELINE:  $match → $group → $sort → $lookup → $project ... — put $match (and $sort\n          that can use an index) FIRST to shrink the working set early.\nTRANSACTIONS:  multi-document ACID transactions exist (since 4.0 / 4.2 sharded), but they cost\n          performance and hold locks — the model prefers "keep what changes together in one document".\nSCHEMA VALIDATION:  optional JSON-schema rules per collection — use them; "flexible" should not\n          mean "no shape".\nWRITE CONCERN / READ CONCERN:  w:"majority" + readConcern:"majority" for durability/consistency;\n          w:1 is faster but a failover can lose it. readPreference routes reads to secondaries\n          (stale) or primary.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>A dashboard query <code>$group</code>s 50M events then <code>$match</code>es one customer — it scans everything. Reorder: <code>$match {customerId}</code> first (uses an index), then <code>$group</code>. Pipeline stage order is the query plan.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>A "product" document embeds a <code>reviews</code> array. Popular products hit 20k reviews, the doc nears 16 MB, and adding a review rewrites the entire document (and every index on it). Fix: move reviews to their own collection keyed by <code>productId</code>, keep only <code>reviewCount</code> + <code>avgRating</code> on the product.</p></div>',
    try: [['📖 MongoDB aggregation pipeline', 'https://www.mongodb.com/docs/manual/core/aggregation-pipeline/', 'o']] },
  { html:
    '<p><b>Sharding, consistency, and the "Mongo vs Postgres" question:</b></p>' +
    '<pre><code>SHARDING:  choose a shard key with the same rules as ch.5 (high cardinality, in most queries,\n  not monotonic). MongoDB routes via mongos; config servers hold the metadata; the balancer moves\n  chunks. A bad shard key (monotonic _id, or low cardinality) = jumbo chunks + a hot shard, and\n  it is very hard to change.\nCONSISTENCY:  a replica set has one primary; w:"majority"/readConcern:"majority" ≈ linearizable-ish\n  for a single document. Cross-document/cross-shard = weaker unless you use a transaction.\n\nMONGO vs POSTGRES:\n  Mongo shines: document-shaped data with a stable access pattern, rapid schema change, built-in\n    horizontal sharding, per-document atomicity is enough.\n  Postgres shines (often with JSONB): you need real joins, multi-row transactions, strong\n    constraints, complex ad-hoc analytics, or you simply do not need to shard yet.\n  Reality: Postgres + JSONB covers a large fraction of "we need Mongo" cases without a second system.</code></pre>',
    try: [['📖 Jepsen: MongoDB analysis', 'https://jepsen.io/analyses/mongodb-4.2.6', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: How do you decide to embed vs reference?\nA: Embed when the child is always read with the parent, is owned by it, and is bounded in size —\n   you get single-read access and single-document atomic updates. Reference when the child is\n   large, shared across parents, queried independently, or grows without bound.\n\nQ: What is the classic document-modelling anti-pattern?\nA: The unbounded embedded array (comments in a post, events in a user). It hits the 16 MB document\n   limit, and every append rewrites the whole document and its indexes. Move it to its own\n   collection.\n\nQ: In an aggregation pipeline, why does stage order matter?\nA: It IS the execution plan. Put $match and index-usable $sort first so later stages ($group,\n   $lookup) operate on a small filtered set instead of the whole collection.\n\nQ: Does MongoDB support ACID transactions?\nA: Yes — multi-document transactions since 4.0 (4.2 across shards) — but they add latency and lock\n   contention, so the idiomatic design keeps co-changing data in one document and uses\n   transactions sparingly.\n\nQ: What makes a bad MongoDB shard key and why is it hard to fix?\nA: Low cardinality (few distinct values → few chunks, hot shard) or monotonically increasing\n   (all new writes to one chunk/shard). Changing the shard key historically required dumping and\n   reloading the collection; even with resharding support it is a major operation.\n\nQ: When would you choose Postgres + JSONB over MongoDB?\nA: When you also need real joins, multi-row transactions and constraints, rich ad-hoc SQL\n   analytics, or you do not need horizontal sharding — you get document flexibility for the parts\n   that need it without running a second database.\n\nQ: What do w:"majority" and readConcern:"majority" give you?\nA: The write is acknowledged only after a majority of the replica set has it (survives a\n   failover), and reads return data acknowledged by a majority (no reading writes that could be\n   rolled back).</code></pre>',
    try: [['📖 MongoDB: sharding', 'https://www.mongodb.com/docs/manual/sharding/', 'o']] } ],
  quiz: [
    { q: 'You are modelling blog posts and their comments in MongoDB. Popular posts get 50k+ comments. Best design?', opts: ['Embed all comments in the post document', 'Store comments in their own collection referencing postId; keep commentCount on the post', 'Use one document per (post, comment) pair with the full post duplicated', 'Store comments as a single JSON string field'], ok: 1, why: 'An unbounded embedded array risks the 16 MB document limit and makes every comment insert rewrite the whole post. Referencing comments in a separate collection keeps writes cheap and the post document small.' },
    { q: 'An aggregation pipeline runs $group over the whole 50M-doc collection, then $match for one customerId. How do you speed it up?', opts: ['Add more shards', 'Move $match {customerId} before $group so an index filters to that customer first', 'Convert to a $lookup', 'Increase the BSON size limit'], ok: 1, why: 'Pipeline stage order is the plan. Filtering early with an index-backed $match shrinks the data before the expensive $group, instead of grouping everything and discarding almost all of it.' },
    { q: 'Which is a legitimate reason to choose PostgreSQL with JSONB over MongoDB?', opts: ['You need schemaless documents', 'You also need real multi-table joins, multi-row ACID transactions, strong constraints, and rich ad-hoc SQL analytics — without adding a second datastore', 'You need horizontal sharding out of the box', 'You want a 16 MB document limit'], ok: 1, why: 'JSONB gives document flexibility for the fields that need it while keeping relational joins, transactions, constraints, and SQL analytics. That covers many "we need a document DB" cases with one system.' } ] },

/* ============================ 10. WIDE-COLUMN (CASSANDRA) ============================ */
{ num: 10, emoji: '🏛️', title: 'Wide-Column Stores (Cassandra, Bigtable)', layer: 'NoSQL',
  tagline: 'Design the table for the query, not the data. Massive write throughput, no joins, and tombstone pain.',
  apps: ['time-series & event logs at scale', 'write-heavy, always-on workloads', 'multi-datacenter active-active'],
  levels: [
  { html:
    '<p>Wide-column stores (Cassandra, ScyllaDB, HBase, Bigtable) organise data by a <b>partition key</b> (which node holds the data) plus <b>clustering columns</b> (the sort order of rows within a partition). You <b>model one table per query</b> and denormalise freely.</p>' +
    '<pre><code>CREATE TABLE messages_by_room (\n  room_id   uuid,\n  bucket    date,               -- time bucket to bound partition size\n  msg_ts    timestamp,\n  msg_id    timeuuid,\n  sender    text,\n  body      text,\n  PRIMARY KEY ((room_id, bucket), msg_ts, msg_id)   -- ((partition key)) (clustering cols)\n) WITH CLUSTERING ORDER BY (msg_ts DESC);\n-- "last 50 messages in a room today" = one partition read, already sorted. No index, no join.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A warehouse where you decide the aisle layout by the picking list.</b> If you always pick "all items for order X", you store them together in one bay. Asking a different question ("all red items") means walking the whole warehouse — so you build a second warehouse laid out for that question.</p></div>',
    try: [['📖 Cassandra data modeling', 'https://cassandra.apache.org/doc/latest/cassandra/developing/data-modeling/index.html', 'o'], ['📘 Part 5: LSM trees (next chapter)', '#ch11', 'o']] },
  { html:
    '<p><b>What you give up</b> vs a relational DB:</p>' +
    '<pre><code>NO JOINS · NO aggregations across partitions · NO ad-hoc WHERE (you can only filter on the key,\n  or pay for ALLOW FILTERING which scans) · NO ORDER BY except along clustering columns ·\n  secondary indexes exist but are a trap at scale (they fan out to every node).\nreads are eventually consistent by default; you tune per-query (ONE / QUORUM / LOCAL_QUORUM / ALL).\nEVERY query you will run must be known up front and have a table designed for it.\n\nWhat you get:  linear write scalability, no single point of failure (every node equal), multi-DC\n  replication built in, predictable latency, runs on commodity disks.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A vending machine, not a chef.</b> Press B4 and the exact item drops instantly, every time. It will not "make you something with what is in stock" — no improvised queries.</p></div>',
    try: [['📖 DataStax: Cassandra query-first modeling', 'https://www.datastax.com/blog/basic-rules-cassandra-data-modeling', 'o']] },
  { html:
    '<p><b>The write path (LSM) and TOMBSTONES</b> — the operational reality:</p>' +
    '<pre><code>write path:  append to the commit log (durability) → update the in-memory memtable → when full,\n  flush to an immutable SSTable on disk. Reads merge the memtable + several SSTables (+ bloom\n  filters to skip SSTables that cannot have the key). COMPACTION merges SSTables in the background.\n\nDELETES do not remove data — they write a TOMBSTONE (a marker "this is deleted as of time T").\n  The real removal happens at compaction, after gc_grace_seconds (default 10 days, so a down node\n  cannot resurrect the row). Until then:\n  - a query over a range full of tombstones must read them all → latency, then a\n    TombstoneOverwhelmingException (default 100k) that fails the query.\n  - a queue-like table (insert, process, DELETE) generates tombstones faster than compaction\n    clears them → death by tombstones. Use TTLs or a different data model, not DELETE-per-row.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>A table modelled as <code>PRIMARY KEY (device_id, reading_ts)</code> with no time bucket. One busy device accumulates 40 million rows in a single partition → that partition no longer fits comfortably in memory during compaction/repair, latency spikes, node OOMs. Fix: composite partition key <code>((device_id, day))</code> to bound each partition to one day of readings.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>Team uses Cassandra as a work queue: insert task, worker deletes it when done. Reads of "pending tasks" get slower every hour and eventually throw <code>TombstoneOverwhelmingException</code>. Fix: do not model a queue on Cassandra — use a TTL so rows expire, or a purpose-built queue (Kafka, SQS, Redis streams).</p></div>',
    try: [['📖 Cassandra: about deletes & tombstones', 'https://cassandra.apache.org/doc/latest/cassandra/managing/operating/compaction/index.html', 'o']] },
  { html:
    '<p><b>Consistency tuning and compaction strategy:</b></p>' +
    '<pre><code>per-query consistency:  ONE (fast, stale-prone) · LOCAL_QUORUM (majority in the local DC — the\n  usual production choice) · QUORUM (majority across all DCs — slow) · ALL (no availability).\n  W + R &gt; RF for read-your-writes (e.g. RF=3, LOCAL_QUORUM read + write).\nread repair + hinted handoff + nodetool repair (anti-entropy) keep replicas converged.\nLIGHTWEIGHT TRANSACTIONS (LWT):  "IF NOT EXISTS" / "IF col = ?" via Paxos — correct but ~4x the\n  latency; use sparingly (e.g. unique username claim), never on a hot path.\n\nCOMPACTION STRATEGY (big performance lever):\n  STCS (size-tiered):  write-optimised, default; can have high read amplification &amp; space spikes.\n  LCS (leveled):  read-optimised, bounded SSTables per read, more write/IO amplification — good\n    for read-heavy or update-heavy tables.\n  TWCS (time-window):  time-series — each window compacts together and whole SSTables drop on TTL.</code></pre>',
    try: [['📖 ScyllaDB / Cassandra compaction strategies', 'https://opensource.docs.scylladb.com/stable/cql/compaction.html', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: How is Cassandra data modelling different from relational?\nA: Query-first: you enumerate every query, then create a denormalised table per query with a\n   partition key + clustering columns that make that query a single-partition, pre-sorted read.\n   No joins, no ad-hoc filtering, no cross-partition aggregation.\n\nQ: What is the partition key vs the clustering key?\nA: The partition key hashes to determine WHICH node(s) store the row and groups rows into a\n   partition. Clustering columns define the SORT ORDER of rows within that partition and allow\n   range scans along that order.\n\nQ: Why can DELETE make a Cassandra table slower over time?\nA: Deletes write tombstones, not removals. Real deletion waits for compaction after\n   gc_grace_seconds. Queries over ranges with many tombstones must scan them all, adding latency\n   and eventually failing with TombstoneOverwhelmingException. Prefer TTLs; do not model queues.\n\nQ: What consistency level do you use in production and why?\nA: Usually LOCAL_QUORUM for both reads and writes: a majority within the local datacenter gives\n   read-your-writes (W+R &gt; RF) and low latency without waiting on remote DCs. QUORUM across DCs\n   is only for stronger cross-region guarantees.\n\nQ: A single partition has grown to tens of millions of rows. Consequences and fix?\nA: Slow reads, heavy GC, compaction/repair memory pressure, possible node OOM. Fix: add a\n   bucketing component (date/hour, or a modulo) to the partition key so each partition stays\n   bounded (rule of thumb: keep partitions under ~100 MB / low millions of cells).\n\nQ: When would you NOT choose Cassandra?\nA: When you need joins, ad-hoc analytical queries, strong multi-row transactions, or your write\n   volume and availability needs are modest — a single Postgres (with replicas) is simpler and\n   more flexible.\n\nQ: STCS vs LCS compaction?\nA: Size-tiered (STCS): fewer writes/IO, but a read may touch many SSTables and space can\n   temporarily double — good for write-heavy, append-mostly. Leveled (LCS): guarantees few\n   SSTables per read, at the cost of more write amplification — good for read-heavy or\n   update-heavy tables.</code></pre>',
    try: [['📖 Jepsen: Cassandra', 'https://aphyr.com/posts/294-call-me-maybe-cassandra', 'o']] } ],
  quiz: [
    { q: 'A Cassandra table is PRIMARY KEY (sensor_id, ts) with no bucketing. One busy sensor now has 30M rows in its partition. Main problem?', opts: ['Too many tombstones', 'Unbounded partition — slow reads, GC pressure, and compaction/repair can OOM the node; add a time bucket to the partition key', 'Wrong consistency level', 'Missing secondary index'], ok: 1, why: 'All rows for a partition key live together on the same replicas. An ever-growing partition eventually exceeds what the node can handle during reads, compaction, and repair. Bucketing (e.g. ((sensor_id, day))) bounds partition size.' },
    { q: 'Why is modelling a work queue (insert task, delete on completion) an anti-pattern in Cassandra?', opts: ['Inserts are too slow', 'Deletes create tombstones that are only removed at compaction after gc_grace_seconds; reads of pending rows must scan accumulating tombstones and eventually fail', 'Cassandra has no DELETE', 'Queues need joins'], ok: 1, why: 'Cassandra deletes are markers, not removals. A high insert+delete churn produces tombstones faster than compaction clears them, degrading range reads until they throw TombstoneOverwhelmingException. Use TTLs or a real queue system.' },
    { q: 'For read-your-writes with replication factor 3 in one datacenter, which consistency setting is standard?', opts: ['ONE for both reads and writes', 'LOCAL_QUORUM for both reads and writes (2 + 2 > 3)', 'ALL for writes, ONE for reads', 'Lightweight transactions on every query'], ok: 1, why: 'LOCAL_QUORUM (majority within the local DC) for reads and writes satisfies W + R > RF, so a read always overlaps a replica that has the latest write, while staying fast by not waiting on remote datacenters.' } ] },

/* ============================ 11. STORAGE ENGINES: LSM vs B-TREE ============================ */
{ num: 11, emoji: '💾', title: 'Storage Engines — LSM vs B-Tree', layer: 'Internals',
  tagline: 'Under every database is a way to put bytes on disk. B-trees favour reads and ranges; LSM favours writes.',
  apps: ['"why is this DB write-optimised?"', 'choosing RocksDB vs InnoDB-style', 'reasoning about write amplification'],
  levels: [
  { html:
    '<p>Two dominant on-disk designs:</p>' +
    '<pre><code>B-TREE (InnoDB, Postgres heap+btree, most SQL):  a balanced tree of fixed-size pages, updated\n  IN PLACE. A lookup is O(log n) page reads; ranges are a sequential leaf walk. Every write must\n  find and modify the right page (+ a write-ahead log for crash safety).\n\nLSM TREE (RocksDB, Cassandra, LevelDB, ScyllaDB, HBase, modern time-series):  writes go to an\n  in-memory MEMTABLE (+ a WAL). When full, it is flushed as an immutable sorted file (SSTABLE).\n  Reads check the memtable, then SSTables newest→oldest (bloom filters skip most). COMPACTION\n  merges SSTables in the background to bound read cost and drop deleted/overwritten data.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>B-tree = editing the encyclopaedia in place</b> (find the page, change the entry). <b>LSM = keeping a stack of dated addenda</b> — you jot changes on new sheets and periodically merge the stack into a clean edition.</p></div>',
    try: [['📚 DDIA ch.3: Storage and Retrieval', 'https://dataintensive.net/', 'o']] },
  { html:
    '<p>The three <b>amplifications</b> — the vocabulary interviewers want:</p>' +
    '<pre><code>WRITE amplification:  bytes actually written to disk per byte of user data. LSM compaction\n  re-writes data several times as it moves down levels → higher WA (LCS worse than STCS). B-tree\n  writes a full page (+ WAL, + sometimes a full-page image) even for a tiny change.\nREAD amplification:  disk reads per lookup. LSM may probe several SSTables (mitigated by bloom\n  filters + compaction). B-tree is ~tree height, very predictable.\nSPACE amplification:  disk used vs live data. LSM holds superseded versions until compaction\n  (STCS can transiently ~2x). B-tree has page fragmentation / fill-factor slack and, in MVCC\n  systems, bloat from dead tuples.\nyou can optimise any TWO; the third gets worse. Compaction strategy is how LSM picks its point.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A see-saw with three seats.</b> Push write and space down, read pops up. Every storage engine is a chosen balance point, and its tuning knobs slide it along.</p></div>',
    try: [['📖 RocksDB: LSM & compaction', 'https://github.com/facebook/rocksdb/wiki/Compaction', 'o']] },
  { html:
    '<p><b>Supporting machinery</b> both designs rely on:</p>' +
    '<pre><code>WAL / commit log:  every change is appended (sequential, fast) BEFORE the main structure is\n  updated, so a crash mid-write is recoverable by replaying the log. fsync policy = the\n  durability/latency knob.\nBLOOM FILTER (LSM):  a tiny probabilistic set per SSTable — "definitely not here" or "maybe here"\n  → skip reading SSTables that cannot contain the key. Turns O(#SSTables) reads into ~O(1).\nPAGE CACHE / buffer pool:  hot pages/blocks live in RAM; the real question for read latency is\n  "does the working set fit in memory".\nCHECKPOINT / flush:  periodically persist in-memory state and truncate the WAL so recovery is bounded.\nMVCC vs in-place:  Postgres keeps versions in the heap (→ vacuum). InnoDB keeps them in an undo\n  log. LSM naturally versions (newer SSTable wins) and drops old versions at compaction.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>An IoT service ingests 200k rows/sec. On a B-tree SQL DB, random inserts across a wide key space thrash pages and the WAL; throughput collapses. Switching the hot table to an LSM engine (or a time-series DB built on LSM) turns inserts into sequential memtable appends + background compaction, and it keeps up.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>An analytics store on RocksDB with default size-tiered compaction shows p99 read latency 10x p50. Cause: reads fan out across many un-compacted SSTables. Fix: switch that column family to leveled compaction (bounds SSTables-per-read) and confirm bloom filters are enabled — p99 drops sharply, write IO rises a bit.</p></div>',
    try: [['📖 Bloom filters explained', 'https://en.wikipedia.org/wiki/Bloom_filter', 'o']] },
  { html:
    '<p><b>When to pick which</b> (and it is usually already chosen for you by the DB):</p>' +
    '<pre><code>LSM-leaning workloads:  very high write/ingest rate · write-once-read-sometimes (logs, events,\n  metrics, messaging) · lots of key-space, random inserts · SSDs (sequential writes extend life)\n  · you can spend background CPU/IO on compaction.\nB-tree-leaning workloads:  read-heavy OLTP · lots of point reads and short range scans · in-place\n  updates of existing rows · you want predictable read latency and simple operations · smaller\n  data that fits in cache.\nreality:  Postgres/InnoDB (B-tree) covers most OLTP; Cassandra/RocksDB/Scylla (LSM) for\n  write-heavy scale-out; many systems now offer both engines (MyRocks = MySQL on RocksDB).\ntime-series DBs (InfluxDB, Timescale hypertables, ClickHouse MergeTree) are LSM-ish + columnar.</code></pre>',
    try: [['📖 MyRocks (MySQL on RocksDB) — why', 'https://engineering.fb.com/2016/08/31/core-infra/myrocks-a-space-and-write-optimized-mysql-database/', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: LSM tree vs B-tree — the fundamental difference?\nA: B-tree updates pages in place: bounded, predictable reads; every write touches a page + WAL.\n   LSM buffers writes in memory, flushes immutable sorted files, and merges them via background\n   compaction: writes are sequential/cheap, reads may touch several files (bloom filters + compaction\n   keep it bounded).\n\nQ: Define write, read, and space amplification.\nA: Write: physical bytes written per logical byte (LSM compaction rewrites data; B-tree writes\n   whole pages + WAL). Read: disk reads per lookup (LSM ~#SSTables probed; B-tree ~tree height).\n   Space: bytes on disk per live byte (LSM keeps superseded data until compaction; B-tree\n   fragmentation / MVCC bloat). You optimise two at the third\'s expense.\n\nQ: Why is an LSM tree good for write-heavy workloads?\nA: Writes are an in-memory insert plus a sequential WAL append and a later sequential SSTable\n   flush — no random page I/O per write. The cost is deferred to background compaction.\n\nQ: What does a bloom filter do in an LSM engine?\nA: Per-SSTable probabilistic membership test: it can say "key definitely not in this file" so the\n   read skips it entirely, avoiding disk I/O for the common case of a key living in only one file.\n\nQ: What is a WAL and why append-only?\nA: A write-ahead log records each change durably before the main structure is modified, so a\n   crash is recovered by replay. Appending is sequential I/O (fast) and simple to truncate at a\n   checkpoint.\n\nQ: Leveled vs size-tiered compaction?\nA: Size-tiered: merges similarly-sized SSTables — low write amplification, but a read may hit\n   many files and space can spike. Leveled: non-overlapping SSTables per level — a read touches\n   few files (read-optimised), at higher write amplification.\n\nQ: Your write throughput is fine but read p99 is terrible on an LSM store. Likely cause?\nA: Too many un-compacted SSTables overlap for the queried key range, so each read fans out.\n   Tune/upgrade compaction (leveled), ensure bloom filters are on, and check compaction is\n   keeping up (it may be IO- or CPU-starved).</code></pre>',
    try: [['📖 The Log-Structured Merge-Tree (original paper)', 'https://www.cs.umb.edu/~poneil/lsmtree.pdf', 'o']] } ],
  quiz: [
    { q: 'You need to ingest 300k writes/sec of append-mostly event data. Which storage design is a better fit and why?', opts: ['B-tree — predictable reads', 'LSM tree — writes become in-memory inserts plus sequential WAL/SSTable flushes, avoiding random page I/O per write', 'Neither; use a spreadsheet', 'B-tree — lower write amplification always'], ok: 1, why: 'LSM converts high-rate random-key writes into sequential I/O (memtable + WAL, then sorted flushes), deferring reorganisation to background compaction. B-trees do a random page update (plus WAL) per write, which caps ingest throughput.' },
    { q: 'What does "write amplification" mean for an LSM tree?', opts: ['Writes are compressed', 'The same data is physically rewritten multiple times as compaction merges it down through levels, so bytes-written-to-disk exceed bytes-of-user-data', 'Reads count as writes', 'The WAL is disabled'], ok: 1, why: 'Compaction repeatedly merges and rewrites SSTables to bound read cost and reclaim space. Each byte of user data may be rewritten several times over its lifetime, which is the LSM write-amplification cost (higher with leveled compaction).' },
    { q: 'In an LSM engine, a read for an existing key still checks only one or two SSTables instead of all of them. What makes this possible?', opts: ['The WAL', 'A per-SSTable bloom filter that reports "key definitely not here", letting the read skip files that cannot contain it', 'Larger page size', 'Disabling compaction'], ok: 1, why: 'Bloom filters give a cheap, memory-resident "no / maybe" per SSTable. Since a key usually lives in only one file, the read probes the memtable and the one or two SSTables the filters do not rule out.' } ] },

/* ============================ 12. OLAP, COLUMNAR & THE LAKEHOUSE ============================ */
{ num: 12, emoji: '📊', title: 'OLAP, Columnar & the Lakehouse', layer: 'Analytics',
  tagline: 'Row stores for transactions, column stores for analytics — plus Parquet and open table formats.',
  apps: ['data warehouse / BI', '"why is this analytical query slow on Postgres?"', 'the lakehouse architecture round'],
  levels: [
  { html:
    '<p><b>OLTP</b> (transaction processing): many small reads/writes touching a few whole rows — row-oriented storage wins. <b>OLAP</b> (analytical processing): scan billions of rows but only a few columns, aggregate — <b>column-oriented</b> storage wins.</p>' +
    '<pre><code>row store:   [id,name,age,city][id,name,age,city]...   → SELECT * WHERE id = 7 : one seek, whole row\ncolumn store: [id,id,id...][name,name...][age,age...]  → SELECT avg(age) : read ONLY the age column\n\ncolumnar advantages:  read only needed columns · each column is one type → huge compression\n  (dictionary, RLE, delta, bit-packing) · SIMD/vectorised execution over tight arrays ·\n  late materialisation. A query touching 3 of 200 columns reads ~1.5% of the data.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A spreadsheet stored by column, not by row.</b> "Average salary" means reading one contiguous salary column, not skipping across every employee record to pick out one field.</p></div>',
    try: [['📘 Part 3: DuckDB / Polars (columnar on your laptop)', '../learn3/#ch16', 'o'], ['📘 Part 4: SQL — star schema & grain', '../learn4/#ch14', 'o']] },
  { html:
    '<p>The engines and where they sit:</p>' +
    '<pre><code>CLOUD WAREHOUSES:  Snowflake, BigQuery, Redshift — separate storage & compute, auto-scale,\n  SQL-first, pay-per-scan or per-credit. Great for BI + ELT.\nOLAP ENGINES:  ClickHouse, Apache Druid, Apache Pinot — sub-second aggregations on huge tables,\n  for user-facing analytics / real-time dashboards. DuckDB — an in-process OLAP engine (the\n  "SQLite of analytics").\nQUERY-ON-LAKE:  Trino/Presto, Spark SQL, Athena — SQL directly over files in object storage.\nFILE FORMATS:  Parquet / ORC — columnar, compressed, with per-row-group statistics (min/max) so\n  a query can SKIP whole chunks (predicate pushdown). CSV/JSON are none of this.\nnaming:  OLTP DB = system of record. Warehouse = curated analytics. Data lake = raw files in\n  object storage. LAKEHOUSE = warehouse-style tables (ACID, schema, time travel) ON the lake.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Parquet is a well-indexed archive box.</b> Each box has a label listing the min/max of what is inside, so to find "orders over $1M in March" you skip every box whose label rules it out.</p></div>',
    try: [['📖 Apache Parquet', 'https://parquet.apache.org/docs/', 'o']] },
  { html:
    '<p><b>Open table formats</b> — the lakehouse layer that makes files behave like a database:</p>' +
    '<pre><code>problem with "just Parquet files in S3":  no atomic multi-file commits, no schema evolution,\n  no deletes/updates, listing millions of files is slow, concurrent writers clobber each other.\nsolution (Apache Iceberg / Delta Lake / Apache Hudi):  a metadata layer that tracks which files\n  make up a table AT EACH VERSION → ACID commits, MERGE/UPDATE/DELETE, schema &amp; partition\n  evolution, TIME TRAVEL ("query the table as of yesterday"), hidden partitioning, compaction of\n  small files. Query engines (Spark, Trino, Snowflake, DuckDB, Flink) all read them.\n\nMODELLING:  still star/snowflake schemas; PARTITION by a low-cardinality, filter-heavy column\n  (usually date); CLUSTER/sort within partitions by a common filter column; keep files ~100 MB-1 GB\n  (the small-file problem kills scan performance — run compaction).</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>A dashboard "revenue by day, last 30 days" over a 5-year fact table takes 90 s. The table is one giant unpartitioned Parquet dataset. Fix: partition by <code>event_date</code> so the engine reads 30 partitions not 1,800; add sort-by <code>region</code> within each; queries drop to ~2 s via partition pruning + min/max skipping.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>A streaming job writes 5,000 tiny Parquet files/hour to a lake table. Read queries spend all their time listing and opening files. Fix: a scheduled compaction (Iceberg/Delta OPTIMIZE) that rewrites small files into ~256 MB files and expires old snapshots; scan time and S3 request cost both collapse.</p></div>',
    try: [['📖 Apache Iceberg', 'https://iceberg.apache.org/docs/latest/', 'o'], ['📖 Delta Lake', 'https://docs.delta.io/latest/index.html', 'o']] },
  { html:
    '<p><b>Getting data in, and modelling for it:</b></p>' +
    '<pre><code>ETL vs ELT:  ELT is now standard — load raw into the warehouse/lake, then transform IN it with\n  SQL (dbt: version-controlled, tested, documented transformation DAG; models = SELECTs;\n  incremental models for big tables; tests = not_null / unique / relationships).\nCDC (ch.16):  stream row changes from the OLTP DB into the warehouse continuously instead of a\n  nightly dump.\nSLOWLY CHANGING DIMENSIONS, grain, conformed dimensions — same as Part 4 ch.14.\nGOTCHAS:  don\'t run heavy analytics on the OLTP primary (replica or warehouse instead) ·\n  per-scan billing punishes SELECT * and unpartitioned scans · high-cardinality GROUP BY /\n  DISTINCT / window functions can blow up memory → approximate (HLL) where acceptable ·\n  UPDATE/DELETE on columnar tables is expensive (rewrites files) → prefer append + merge.</code></pre>',
    try: [['📖 dbt (data build tool)', 'https://docs.getdbt.com/', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Why is columnar storage faster for analytical queries?\nA: Analytical queries scan few columns of many rows. Column storage reads only those columns\n   (not whole rows), each column compresses far better (one type, sorted/repetitive), and\n   execution vectorises over tight arrays. Row storage must read every row to get one field.\n\nQ: Why is Parquet better than CSV for a data lake?\nA: Columnar + compressed + typed, with per-row-group min/max statistics enabling predicate and\n   projection pushdown (skip chunks and columns). CSV is row-wise, uncompressed, untyped, and\n   must be fully parsed.\n\nQ: What does an open table format (Iceberg/Delta/Hudi) add over raw Parquet files?\nA: A metadata layer giving atomic commits, MERGE/UPDATE/DELETE, schema and partition evolution,\n   time travel / snapshots, and fast file planning — so a directory of files behaves like a\n   transactional table that many engines can safely read and write.\n\nQ: OLTP database vs data warehouse vs data lake vs lakehouse?\nA: OLTP DB: the transactional system of record. Warehouse: curated, columnar, SQL analytics store.\n   Lake: raw files (any format) in cheap object storage. Lakehouse: warehouse-grade table\n   semantics (ACID, schema, time travel) implemented directly on lake files.\n\nQ: A dashboard query over a huge fact table is slow. First things to check?\nA: Is the table partitioned by the filter column (usually date) so it can prune? Is it sorted/\n   clustered by common secondary filters? Are there many tiny files needing compaction? Is the\n   query doing SELECT * or scanning years to show a month?\n\nQ: ETL vs ELT, and where does dbt fit?\nA: ELT loads raw data first and transforms inside the warehouse with SQL, exploiting its compute.\n   dbt manages those transformations as a tested, version-controlled DAG of SELECT models.\n\nQ: Why avoid frequent UPDATE/DELETE on columnar warehouse tables?\nA: Columnar files are immutable-ish; a point update rewrites whole files/partitions. Model as\n   append-only with periodic MERGE, or use an open table format\'s row-level ops sparingly.</code></pre>',
    try: [['📖 ClickHouse: why it is fast', 'https://clickhouse.com/docs/en/concepts/why-clickhouse-is-so-fast', 'o']] } ],
  quiz: [
    { q: 'A query computes SUM(amount) and AVG(amount) over 2 billion rows of a 150-column table. Why does a columnar store crush a row store here?', opts: ['It has more indexes', 'It reads only the amount column (a few % of the data), which also compresses well and vectorises, instead of scanning every full row', 'It caches the whole table in RAM', 'Row stores cannot do SUM'], ok: 1, why: 'Column storage keeps each column contiguously, so an aggregation over one column reads just that column\'s bytes, benefits from type-specific compression, and runs as a tight vectorised loop. A row store must touch every row to extract that one field.' },
    { q: 'What does an open table format like Apache Iceberg provide that a folder of Parquet files does not?', opts: ['Better compression', 'Atomic commits, row-level MERGE/UPDATE/DELETE, schema/partition evolution, time travel, and fast metadata-based file planning', 'Faster single-row lookups than Postgres', 'Automatic sharding of OLTP writes'], ok: 1, why: 'Iceberg/Delta/Hudi add a transactional metadata layer over the files, turning a passive directory into a versioned, mutable, concurrently-writable table that many query engines can use safely.' },
    { q: 'A 5-year fact table stored as one unpartitioned Parquet dataset makes "last 30 days" dashboards slow. Best fix?', opts: ['Convert to CSV', 'Partition by event_date (and sort within partitions by a common filter) so the engine prunes to ~30 partitions instead of scanning everything', 'Add a B-tree index', 'Load it all into Redis'], ok: 1, why: 'Partitioning by the primary time filter lets the query planner skip all partitions outside the window (partition pruning), and per-file min/max stats skip more. This turns a full multi-year scan into reading a handful of partitions.' } ] },

/* ============================ 13. SEARCH ENGINES (ELASTICSEARCH) ============================ */
{ num: 13, emoji: '🔍', title: 'Search Engines (Elasticsearch / OpenSearch)', layer: 'Search',
  tagline: 'An inverted index for text: relevance ranking, analyzers, shards — and where "search" ends and "database" begins.',
  apps: ['product / site / docs search', 'log & metrics analytics (the "ELK" stack)', 'faceted filtering + aggregations'],
  levels: [
  { html:
    '<p>A search engine is built around an <b>inverted index</b>: instead of "document → its words", it stores "word → the documents (and positions) containing it". That is what makes full-text search fast.</p>' +
    '<pre><code>docs:  1: "the quick brown fox"   2: "quick foxes run"\ninverted index:\n  quick → [1, 2]      brown → [1]      fox → [1]      foxes → [2]      run → [2]\nquery "quick fox" → intersect postings lists → doc 1 (and score doc 2 lower for a partial match)</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>The concordance at the back of a Bible.</b> "Where does the word \'mercy\' appear?" — you do not re-read every verse; you look up "mercy" and get the exact verse list.</p></div>',
    try: [['📖 Elasticsearch: basic concepts', 'https://www.elastic.co/guide/en/elasticsearch/reference/current/documents-indices.html', 'o'], ['📘 Part 2: hybrid dense + BM25 search', '../learn2/#ch5', 'o']] },
  { html:
    '<p><b>Analyzers</b> (text → terms) and <b>relevance</b> (BM25):</p>' +
    '<pre><code>ANALYZER = character filters → tokenizer → token filters.\n  "The Foxes\' Run!"  → lowercase → tokenize → stopword-remove → stem\n  → [fox, run]   (so a search for "running fox" matches)\nthe SAME analyzer must apply at index time and query time, or terms will not line up.\nkeyword fields (not analyzed) for exact match / sorting / aggregations; text fields for search.\n\nRELEVANCE (BM25):  term frequency (more mentions = more relevant, with diminishing returns) x\n  inverse document frequency (rare terms matter more) x field-length normalisation (a hit in a\n  short title beats one in a long body). Tune with boosts (title^3), function_score (recency,\n  popularity), and phrase/proximity queries.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>The analyzer is how both the librarian and the visitor agree to spell things.</b> If the shelf is filed under "run" but you ask for "Running!", you must both apply the same lowercasing + stemming rules or you will never meet.</p></div>',
    try: [['📖 Elasticsearch: analysis & analyzers', 'https://www.elastic.co/guide/en/elasticsearch/reference/current/analysis.html', 'o']] },
  { html:
    '<p><b>Shards, replicas, and cluster mechanics:</b></p>' +
    '<pre><code>an INDEX is split into PRIMARY SHARDS (fixed at creation — a Lucene index each); each has\n  REPLICA shards (copies, for HA + read throughput). A document routes to a shard by hash(_id).\nsizing:  aim for shards ~10-50 GB; too many small shards = cluster-state / overhead bloat\n  ("oversharding"); too few huge shards = slow recovery + hotspots. You cannot change primary\n  count without reindex (or use the split/shrink APIs).\nnodes:  master (cluster state), data (hold shards), ingest, coordinating. Dedicated masters +\n  an odd count (3) → no split-brain.\nreads:  a query fans out to one copy of every shard, each returns its top-k, the coordinator\n  merges. writes:  go to the primary, then replicas; refresh (default 1s) makes new docs\n  searchable (near-real-time, not real-time).\naggregations:  buckets + metrics computed across shards — powerful, but high-cardinality terms\n  aggs are memory-hungry.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>Logs are indexed with a dynamic mapping. Apps start emitting a unique field name per request (<code>user_1234_action</code>) → <b>mapping explosion</b>: tens of thousands of fields, cluster state huge, nodes GC-thrash. Fix: <code>"dynamic": "strict"</code> or a strict template, put variable keys into a single <code>labels</code> object with <code>flattened</code> type, and cap total fields.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>A "load more" UI uses <code>from: 100000, size: 20</code> and times out. <b>Deep pagination</b>: every shard must build and sort the first 100,020 hits. Fix: <code>search_after</code> with a sort tiebreaker (cursor pagination), or the <code>scroll</code> / <code>point-in-time</code> API for exports.</p></div>',
    try: [['📖 Elasticsearch: size your shards', 'https://www.elastic.co/guide/en/elasticsearch/reference/current/size-your-shards.html', 'o']] },
  { html:
    '<p><b>Search engine vs database — where is the line?</b></p>' +
    '<pre><code>use a search engine for:  full-text relevance ranking · fuzzy / typo-tolerant / synonym search ·\n  faceted navigation + aggregations over big text/log data · geo + text combined · log analytics.\nDO NOT use it as your system of record:  no ACID transactions · no joins (denormalise or use\n  parent-child/nested at a cost) · near-real-time, not read-your-writes · a mapping mistake can\n  mean a full reindex · updates rewrite the whole document · it will drop/rebalance under memory\n  pressure. Keep the source of truth in a real DB and feed the index (CDC / a sync job).\nreindexing is a fact of life:  to change analyzers/mappings you create a new index and use the\n  Reindex API + an alias swap for zero-downtime cutover. Always point your app at an ALIAS, never\n  a concrete index name.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>The card catalogue is not the library.</b> It helps you FIND books fast, but you do not store the only copy of a book in the catalogue drawer. The books (source of truth) live on the shelves (your database).</p></div>',
    try: [['📖 Elasticsearch: reindex + aliases', 'https://www.elastic.co/guide/en/elasticsearch/reference/current/docs-reindex.html', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: What is an inverted index?\nA: A map from each term to the list of documents (and positions) that contain it. Search\n   intersects/unions these postings lists instead of scanning documents, which is what makes\n   full-text queries fast.\n\nQ: Why must the same analyzer run at index and query time?\nA: Search matches TERMS, not raw strings. If indexing lowercases and stems ("Running" → "run")\n   but the query does not, the query term "Running" never equals the indexed term "run".\n\nQ: How does BM25 rank documents?\nA: Higher for more occurrences of the term (with saturation), higher for rarer terms (IDF), and\n   higher when the match is in a shorter field. Boosts and function scores layer business signals\n   (recency, popularity) on top.\n\nQ: You set from=100000, size=20 and it is slow / errors. Why and fix?\nA: Deep pagination — each shard must produce and sort 100,020 results to return 20. Use\n   search_after with a stable sort (cursor pagination), or scroll / point-in-time for bulk export.\n\nQ: What is a mapping explosion?\nA: Dynamic mapping creates a new field definition for every distinct key seen. Unbounded key\n   names (per-user or per-request field names) inflate the mapping and cluster state until nodes\n   thrash. Use strict mappings and a flattened/object field for variable keys.\n\nQ: Can Elasticsearch be your primary datastore?\nA: Generally no — no ACID transactions, no real joins, near-real-time visibility, expensive\n   mapping changes, and it sheds load under pressure. Keep the system of record in a database and\n   index a derived copy.\n\nQ: How do you change an analyzer on a live index with no downtime?\nA: Create a new index with the new mapping, Reindex into it, then atomically switch the alias\n   your application uses from the old index to the new one.</code></pre>',
    try: [['📖 OpenSearch documentation', 'https://opensearch.org/docs/latest/', 'o']] } ],
  quiz: [
    { q: 'Search results do not match even though the words are clearly in the documents. Most likely cause?', opts: ['The cluster is red', 'Analyzer mismatch — indexing and querying apply different tokenization/stemming/lowercasing, so the terms do not line up', 'Too few replicas', 'Deep pagination'], ok: 1, why: 'Elasticsearch matches analyzed terms. If the index analyzer stems and lowercases but the query is treated as a keyword (or a different analyzer), the produced terms differ and nothing matches. Use the same analyzer on both sides.' },
    { q: 'Apps begin emitting per-request field names like metrics.user_8471.clicks. Within a day the cluster is unstable. What happened?', opts: ['Disk filled with data', 'Mapping explosion — dynamic mapping created tens of thousands of fields, bloating cluster state and causing GC pressure', 'Replica shards were lost', 'The analyzer changed'], ok: 1, why: 'Every new field key adds to the index mapping and the cluster state that every node holds. Unbounded, dynamic key names blow this up. Fix with strict mappings and storing variable keys inside a single flattened/object field.' },
    { q: 'Why should your application query an index ALIAS rather than a concrete index name?', opts: ['Aliases are faster', 'To enable zero-downtime reindexing — you build a new index, then atomically repoint the alias, without changing application code', 'Aliases add caching', 'Concrete names are deprecated'], ok: 1, why: 'Mapping/analyzer changes require a new index and a reindex. If the app targets an alias, cutover is a single atomic alias update; if it targets a fixed index name, every reindex is a deploy.' } ] },

/* ============================ 14. VECTOR DATABASES IN PRODUCTION ============================ */
{ num: 14, emoji: '🧭', title: 'Vector Databases in Production', layer: 'Vector',
  tagline: 'HNSW/IVF/PQ tuning, the recall–latency–memory triangle, filtered search, hybrid, freshness, and cost.',
  apps: ['RAG retrieval at scale', 'semantic / image / recommendation search', 'dedup, clustering, anomaly lookup'],
  levels: [
  { html:
    '<p>A vector database stores embeddings and answers <b>approximate nearest-neighbour (ANN)</b> queries: "the k closest vectors to this one" in single-digit ms over billions of vectors, plus metadata filtering, upserts, and deletes. Part 2 covered the concepts; this is the production-ops view.</p>' +
    '<pre><code>options:  pgvector (Postgres extension — no new infra, fine to millions) · dedicated (Qdrant,\n          Weaviate, Milvus, Vespa) · managed (Pinecone, Turbopuffer) · a library (FAISS, hnswlib,\n          ScaNN) you host yourself.\nthe core tradeoff is a TRIANGLE:  RECALL (did you find the true neighbours) vs LATENCY vs\n          MEMORY/COST. You cannot max all three; the index type and its parameters pick a point.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Finding the nearest coffee shop by asking friends-of-friends.</b> Ask more people (higher recall, slower) or fewer (faster, might miss the closest). Exact search is phoning every shop in the city.</p></div>',
    try: [['📘 Part 2: vector DBs (HNSW, IVF, RRF) — concepts', '../learn2/#ch5', 'o'], ['📘 Part 5: Postgres & pgvector', '#ch7', 'o']] },
  { html:
    '<p><b>Index types and the knobs that matter:</b></p>' +
    '<pre><code>HNSW (graph):  build:  M (edges/node, 16-64 — more = better recall, more memory),\n                       efConstruction (128-512 — build effort/quality)\n               query:  efSearch (candidate list size — the RUNTIME recall/latency dial)\n  fast, high recall, but the graph lives in RAM (≈ vectors + M*neighbours*4 bytes/node).\nIVF (clusters):  build:  nlist (# centroids ≈ sqrt(N))\n                 query:  nprobe (clusters to scan — the recall/latency dial)\n  lower memory, pairs with PQ; slightly lower recall than HNSW at equal latency.\nPQ / SQ (compression):  product/scalar quantisation shrinks each vector 4-32x → fit billions in\n  RAM; recall drops → RE-RANK the top candidates with full-precision vectors (a "quantisation\n  ladder": binary shortlist → int8 → fp32 top-k).\nMEASURE recall@k on a labelled query set every time you change a parameter — do not eyeball it.</code></pre>',
    try: [['📖 Qdrant: indexing & quantization', 'https://qdrant.tech/documentation/guides/quantization/', 'o']] },
  { html:
    '<p><b>Filtered search, hybrid, and freshness</b> — where prod systems actually break:</p>' +
    '<pre><code>METADATA FILTERING (tenant_id, lang, date, permissions):\n  post-filter:  ANN first, then drop non-matching → for a selective filter you can get FAR FEWER\n    than k results (or zero for a small tenant). BAD for multi-tenant.\n  pre-filter / filtered-HNSW:  restrict the search to matching vectors → correct counts, but a\n    very selective filter can starve the graph walk (few reachable nodes) → engines fall back to\n    a brute-force scan of the filtered subset under a threshold. Test both paths.\nHYBRID (dense + keyword/BM25):  fuse the two ranked lists with Reciprocal Rank Fusion (score =\n  Σ 1/(k + rank)); dense catches paraphrase, sparse nails IDs / error codes / exact names.\nFRESHNESS:  HNSW handles inserts; heavy DELETES degrade the graph (tombstoned nodes) → periodic\n  rebuild. Pattern: a large immutable "cold" index + a small "hot" index for recent writes,\n  merged at query time and compacted on a schedule.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>Multi-tenant RAG. Small tenants get few or no results because the system does ANN then filters by <code>tenant_id</code> (post-filter). Fix: switch to pre-filtering (filtered-HNSW / payload index on tenant_id), and for tiny tenants let the engine brute-force their subset — it is fast when the subset is small.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>Recall silently drops after a model upgrade re-embedded everything at 3072 dims — the HNSW index now needs 3x the RAM and the node started swapping, so <code>efSearch</code> was quietly lowered by an autoscaler. Fix: size RAM for the new dims (or use Matryoshka: index 512-dim, re-rank with full), pin <code>efSearch</code>, alert on recall@k.</p></div>',
    try: [['📖 Weaviate: filtered vector search', 'https://weaviate.io/developers/weaviate/concepts/prefiltering', 'o']] },
  { html:
    '<p><b>Scaling, cost, and pgvector vs dedicated:</b></p>' +
    '<pre><code>SHARDING:  split vectors across nodes; a query fans out to all shards, each returns top-k, merge.\n  REPLICAS for read throughput + HA. Rebuilds are the expensive operation — do them off the hot path.\nMEMORY is the bill:  1B vectors x 768 dims x 4 bytes = ~3 TB raw. Quantise (int8 → ~768 GB;\n  binary → ~96 GB + rescore). Disk-based ANN (DiskANN) serves from SSD with a RAM cache.\nUPDATES:  upsert by stable id; keep a mapping to the source row; version the embedding model so\n  you know which rows need re-embedding after an upgrade (a full backfill).\npgvector vs dedicated:\n  pgvector:  data + vectors + filters + joins in ONE transactional store, no extra system, great\n    to ~1-10M vectors (HNSW added in 0.5+). Beyond that, memory and single-node limits bite.\n  dedicated:  purpose-built ANN, better recall/latency at scale, horizontal sharding, quantisation,\n    hybrid built in — at the cost of running (and syncing) a second datastore.</code></pre>',
    try: [['📖 DiskANN (billion-scale on SSD)', 'https://www.microsoft.com/en-us/research/publication/diskann-fast-accurate-billion-point-nearest-neighbor-search-on-a-single-node/', 'o'], ['📖 pgvector', 'https://github.com/pgvector/pgvector', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: What is the fundamental tradeoff a vector index makes?\nA: Recall vs latency vs memory/cost. Exact NN search is accurate but O(N). ANN indexes (HNSW,\n   IVF+PQ) trade a bounded recall loss for sub-linear query time and, with quantisation, far less\n   memory. The parameters (efSearch / nprobe) are the runtime dial.\n\nQ: HNSW efSearch — what does raising it do?\nA: Enlarges the candidate list explored during the graph walk → higher recall, higher latency,\n   no change to memory. It is the per-query quality knob (build knobs M / efConstruction set the\n   ceiling).\n\nQ: Pre-filtering vs post-filtering by metadata — when does it matter?\nA: Post-filter (ANN then drop non-matches) can return far fewer than k results when the filter is\n   selective — bad for multi-tenant or permissioned search. Pre-filter (restrict the search to\n   matching vectors) returns correct counts; a very selective filter may trigger a brute-force\n   scan of the small subset, which is fine because it is small.\n\nQ: Why combine dense vector search with BM25 (hybrid)?\nA: Dense retrieval captures paraphrase and concept; BM25 matches exact tokens — IDs, error codes,\n   names, rare terms. Fusing the two ranked lists (e.g. Reciprocal Rank Fusion) beats either alone.\n\nQ: You upgraded the embedding model. What has to happen?\nA: Re-embed and reindex every vector (old and new vectors are incompatible spaces), resize the\n   index for any dimension change, and ideally do a dual-read cutover. Track the model version\n   per row so you know the backfill is complete.\n\nQ: pgvector or a dedicated vector DB?\nA: pgvector when vectors, metadata, filters and joins belong together, the count is in the low\n   millions, and you value one transactional system. A dedicated DB when you need billion-scale,\n   horizontal sharding, quantisation, tuned recall/latency, and built-in hybrid — accepting a\n   second store to operate and keep in sync.\n\nQ: Deletes are hurting your HNSW index. Why, and the fix?\nA: HNSW marks deleted nodes as tombstones; the graph degrades (dangling edges, wasted hops) as\n   they accumulate. Periodically rebuild/compact the index, or use a hot/cold split where the\n   small hot index is rebuilt frequently.</code></pre>',
    try: [['📖 ANN-Benchmarks (recall vs qps)', 'https://ann-benchmarks.com/', 'o']] } ],
  quiz: [
    { q: 'In a multi-tenant RAG system, small tenants often get zero results. The pipeline does ANN search then filters by tenant_id. Fix?', opts: ['Increase k to 1000 and hope', 'Switch to pre-filtering (filtered ANN / payload index on tenant_id); for tiny tenants a brute-force scan of their subset is fast and exact', 'Add more replicas', 'Lower efSearch'], ok: 1, why: 'Post-filtering discards non-matching results after the ANN step, so a selective filter can leave far fewer than k (or none). Pre-filtering constrains the search to the tenant\'s vectors up front, giving correct, complete results.' },
    { q: 'Raising HNSW efSearch at query time primarily trades...', opts: ['Memory for recall', 'Latency for recall (larger candidate list explored per query; memory and build quality are unchanged)', 'Recall for durability', 'Nothing measurable'], ok: 1, why: 'efSearch controls how many candidates the graph traversal considers. Bigger means more of the graph is explored — higher recall, slower queries. Memory footprint is set by M and the vector count; build quality by efConstruction.' },
    { q: 'You upgrade from a 768-dim to a 3072-dim embedding model. What is required?', opts: ['Nothing — vectors are compatible', 'Re-embed and reindex all vectors, resize the index/RAM for the new dimensionality, and track the model version per row to confirm the backfill completed', 'Only re-embed new documents', 'Just rename the collection'], ok: 1, why: 'Different models produce incompatible vector spaces, so every stored vector must be regenerated. A dimension increase also multiplies index memory (a common cause of silent recall drops when the node starts swapping).' } ] },

/* ============================ 15. GRAPH DATABASES ============================ */
{ num: 15, emoji: '🕸️', title: 'Graph Databases (Neo4j, Neptune)', layer: 'Graph',
  tagline: 'When the relationships ARE the data: friend-of-a-friend, fraud rings, and knowledge graphs the joins cannot do.',
  apps: ['fraud-ring detection', 'recommendations / social graphs', 'knowledge graphs, lineage, network/IT topology'],
  levels: [
  { html:
    '<p>A graph database stores <b>nodes</b> (entities), <b>edges</b> (relationships, with direction and type), and <b>properties</b> on both. It is optimised for <b>traversal</b>: "walk from this node across these relationship types".</p>' +
    '<pre><code>(:Person {name:\'Ada\'})-[:FRIEND]-&gt;(:Person {name:\'Bob\'})-[:FRIEND]-&gt;(:Person {name:\'Cara\'})\n(:Account)-[:TRANSFER {amt:5000}]-&gt;(:Account)\n(:Movie)&lt;-[:RATED {stars:5}]-(:User)\n\ntwo families:  PROPERTY GRAPH (Neo4j, Neptune, Memgraph, TigerGraph) — query with Cypher / Gremlin.\n               RDF / TRIPLESTORE (Blazegraph, GraphDB, Neptune) — subject-predicate-object triples,\n               query with SPARQL; the model behind linked data / ontologies.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A pinboard with string between the pins.</b> The value is not the pins (rows) — it is the web of string. "Who is connected to this person within 3 hops" is following string, not scanning a table.</p></div>',
    try: [['📖 Neo4j: graph database concepts', 'https://neo4j.com/docs/getting-started/', 'o']] },
  { html:
    '<p><b>Why a graph DB beats SQL for deep traversal — "index-free adjacency":</b></p>' +
    '<pre><code>in SQL, "friends of friends of friends" = 3 self-JOINs on a friendships table. Each JOIN is an\n  index lookup; cost grows with table size AND depth. A 6-hop query is often infeasible.\nin a graph DB, each node stores DIRECT POINTERS to its edges (index-free adjacency). Traversing\n  one hop = following a pointer, O(1) per edge, INDEPENDENT of total graph size. Cost scales with\n  the size of the RESULT (the neighbourhood you touch), not the database.\n→ variable-length paths, shortest path, cycle detection, "everything within N hops" become cheap\n  and expressible:\n  MATCH (a:Person {id:$id})-[:FRIEND*1..3]-(f) RETURN DISTINCT f      // Cypher\n  MATCH p = shortestPath((a)-[:KNOWS*]-(b)) RETURN p</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A contacts app vs a phone book.</b> "Call my friend\'s work number" — the contacts app jumps straight there (pointer). The phone book makes you re-look-up a name every single time (a JOIN per hop).</p></div>',
    try: [['📖 Cypher query language', 'https://neo4j.com/docs/cypher-manual/current/', 'o']] },
  { html:
    '<p><b>Graph algorithms</b> and the queries that justify a graph DB:</p>' +
    '<pre><code>PATHFINDING:  shortest path, all paths, min spanning tree (routing, dependency resolution)\nCENTRALITY:  degree / betweenness / PageRank / eigenvector — "who is important / a bottleneck /\n  a super-spreader"\nCOMMUNITY DETECTION:  Louvain / Label Propagation / connected components — customer segments,\n  fraud rings, topic clusters\nSIMILARITY:  node2vec / graph embeddings → feed a GNN or a downstream model (Part 1 ch.5 for embeddings)\nLINK PREDICTION:  "which edges are likely to form" — friend / product recommendations\n\nGNN FEATURE PIPELINES:  the graph DB (or a graph compute engine like GraphX / DGL / PyG) produces\n  neighbourhood aggregates and embeddings that become features for fraud / recsys models.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>Fraud: individual accounts look clean, but a <b>ring</b> shares devices, IPs, and shipping addresses and cycles money between themselves. A 2-3 hop Cypher query over <code>(:Account)-[:USED_DEVICE]-&gt;(:Device)&lt;-[:USED_DEVICE]-(:Account)</code> plus a community-detection pass surfaces the cluster. The equivalent SQL is many recursive self-joins that time out.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>Data lineage / impact analysis: "if we change this source table, which of 4,000 dashboards break?" Model tables, jobs, and dashboards as nodes with <code>[:FEEDS]</code> edges; a downstream traversal from the changed node returns the blast radius instantly — a query teams re-run before every schema change.</p></div>',
    try: [['📖 Neo4j Graph Data Science library', 'https://neo4j.com/docs/graph-data-science/current/', 'o']] },
  { html:
    '<p><b>When NOT to use a graph DB, and operational notes:</b></p>' +
    '<pre><code>DO NOT reach for a graph DB when:  your queries are mostly single-table filters / aggregations ·\n  the relationships are shallow (1-2 hops — a JOIN is fine) · you need heavy analytical scans over\n  all data (that is a warehouse) · you need the maturity/tooling/ecosystem of Postgres.\nmany "graph" needs are met by:  a recursive CTE in Postgres · a closure table · Postgres + the\n  Apache AGE extension · a graph layer over an existing store.\nOPERATIONS:  property graphs are often single-primary (scale reads with replicas; sharding a graph\n  is HARD because traversals cross partitions — supernodes make it worse). A SUPERNODE (a node\n  with millions of edges — a celebrity, a popular product) blows up traversals → model around it\n  (bucket edges, add intermediate nodes, or filter by edge property + index).\n  bulk import tools, not row-by-row inserts, for loading. Back up like any stateful store.</code></pre>',
    try: [['📖 Postgres recursive CTEs (graph-ish queries)', 'https://www.postgresql.org/docs/current/queries-with.html', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: When does a graph database beat a relational database?\nA: When queries traverse many hops of relationships (friend-of-friend-of-friend, paths, cycles,\n   "everything within N hops", centrality, community detection). Relational self-joins cost an\n   index lookup per hop and become infeasible with depth; graph traversal follows direct edge\n   pointers, scaling with the result size, not the database size.\n\nQ: What is index-free adjacency?\nA: Each node physically stores references to its incident edges, so moving to a neighbour is a\n   pointer dereference (O(1) per edge) rather than a lookup in a global index. This is why deep\n   traversals stay cheap as the graph grows.\n\nQ: Property graph vs RDF triplestore?\nA: Property graph: nodes/edges with arbitrary key-value properties, queried with Cypher/Gremlin —\n   ergonomic for app data. RDF: everything is (subject, predicate, object) triples with global\n   URIs, queried with SPARQL — built for interoperable linked data, ontologies, and reasoning.\n\nQ: Give a concrete case where a graph query is the right tool.\nA: Fraud-ring detection: clean-looking accounts linked by shared devices/IPs/addresses and\n   circular transfers. A 2-3 hop pattern match plus community detection finds the cluster;\n   the SQL equivalent is many recursive self-joins.\n\nQ: What is a supernode and why is it a problem?\nA: A node with a huge number of edges (a celebrity, a hub IP). Any traversal through it explodes\n   in fan-out, and it makes graph sharding worse. Mitigate by bucketing its edges, inserting\n   intermediary nodes, or filtering on indexed edge properties.\n\nQ: You have a 2-hop "manager of my manager" query. Graph DB?\nA: Not necessarily — shallow traversals are fine with a couple of JOINs or a recursive CTE.\n   Reserve a graph DB for variable-depth / many-hop / pathfinding workloads.\n\nQ: How is a graph DB used in an ML pipeline?\nA: To compute graph features — neighbourhood aggregates, PageRank/centrality, community labels —\n   and node embeddings (node2vec / GNNs) that feed fraud, recommendation, or risk models.</code></pre>',
    try: [['📖 Gremlin (Apache TinkerPop)', 'https://tinkerpop.apache.org/', 'o']] } ],
  quiz: [
    { q: '"Index-free adjacency" means...', opts: ['The graph database has no indexes at all', 'Each node stores direct pointers to its edges, so traversing one hop is O(1) and independent of the total graph size (no global index lookup per hop)', 'Indexes are built lazily', 'Edges are stored in a separate SQL table'], ok: 1, why: 'Relational joins do an index lookup per hop, so cost grows with table size and depth. Graph engines keep edge references on the node itself, making each hop a pointer dereference — traversal cost scales with the neighbourhood touched, not the database.' },
    { q: 'Which problem is a genuine fit for a graph database over Postgres?', opts: ['Summing revenue by month', 'Detecting a fraud ring: accounts that look clean individually but are linked within 2-3 hops by shared devices/addresses and circular transfers, plus community detection', 'Storing user profiles', 'A 1-hop foreign-key lookup'], ok: 1, why: 'Multi-hop pattern matching and community detection over relationships are where graph traversal shines. Aggregations, profile storage, and shallow lookups are better served by a relational or analytical database.' },
    { q: 'A "manager of my manager" (exactly 2 hops) query — do you need a graph database?', opts: ['Yes, always use a graph DB for hierarchy', 'No — shallow, fixed-depth traversals are fine with a couple of JOINs or a recursive CTE; reserve graph DBs for variable-depth / pathfinding workloads', 'Yes, JOINs cannot express hierarchy', 'Only if the table has over 1M rows'], ok: 1, why: 'The graph DB advantage grows with traversal depth and variability. A fixed 2-hop relationship is cheap in SQL; adopting a graph database for it adds a system without a proportional benefit.' } ] },

/* ============================ 16. STREAMING, CDC & EVENT-DRIVEN DATA ============================ */
{ num: 16, emoji: '🌊', title: 'Streaming, CDC & Event-Driven Data', layer: 'Streaming',
  tagline: 'Kafka as the log, change data capture to sync systems, and the outbox pattern that fixes the dual-write bug.',
  apps: ['syncing OLTP → warehouse / search / cache', 'event-driven microservices', 'real-time features & pipelines'],
  levels: [
  { html:
    '<p><b>Kafka</b> (and Kinesis, Pulsar, Redpanda) is a durable, replayable, partitioned <b>log</b>. Producers append records; consumers read at their own pace and track their <b>offset</b>. It decouples "something happened" from "everyone who reacts to it".</p>' +
    '<pre><code>TOPIC → PARTITIONS (the unit of parallelism &amp; ordering). A record has a key; key → partition\n  (hash), so all records for one key are ORDERED within their partition (no global order).\nCONSUMER GROUP:  partitions are divided among the group\'s consumers → scale out by adding\n  consumers (up to #partitions). Each group tracks its own offsets → many independent readers.\nRETENTION:  keep N days, or a size cap, OR log COMPACTION (keep only the latest value per key —\n  a changelog / snapshot). Records are immutable; you cannot "delete a message", only advance.\nDELIVERY:  at-least-once by default (a crash between processing and committing the offset →\n  reprocessing) → make consumers IDEMPOTENT, or use Kafka transactions for exactly-once-ish.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A shared ship\'s logbook.</b> Everyone writes new entries at the end; nobody erases. Each reader keeps a bookmark and reads at their own speed. A new reader can start from page 1 and catch up.</p></div>',
    try: [['📖 Kafka: introduction & design', 'https://kafka.apache.org/documentation/#design', 'o']] },
  { html:
    '<p><b>Change Data Capture (CDC)</b> — turn a database\'s changes into a stream:</p>' +
    '<pre><code>instead of a nightly "SELECT * and dump", CDC tails the database\'s REPLICATION LOG (Postgres WAL,\n  MySQL binlog, Mongo oplog) and emits one event per INSERT/UPDATE/DELETE, in commit order, with\n  before/after images. Tools: Debezium (on Kafka Connect), database-native CDC, Fivetran/Airbyte.\nuses:  keep a search index / cache / warehouse / another service continuously in sync ·\n  build materialised views · trigger workflows on data changes · zero-ETL replication.\nadvantages over polling:  no missed changes between polls, no "updated_at" heuristics, low load\n  on the source, captures deletes, ordered.\ncare:  schema changes in the source (evolve the stream schema — use a schema registry) · large\n  transactions / backfills (initial snapshot can be huge) · PII flowing into more systems ·\n  the replication slot must be consumed or it fills the source\'s disk (ch.4).</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A subscription vs re-buying the newspaper daily.</b> Polling re-reads the whole paper to find what changed. CDC delivers just the new articles, in order, the moment they are printed.</p></div>',
    try: [['📖 Debezium', 'https://debezium.io/documentation/reference/stable/index.html', 'o']] },
  { html:
    '<p><b>The dual-write problem and the OUTBOX pattern</b> — a top interview scenario:</p>' +
    '<pre><code>THE BUG:  a service does  (1) INSERT order into Postgres   (2) publish "OrderCreated" to Kafka.\n  if it crashes between (1) and (2), the DB has the order but no event fired → downstream\n  (email, search index, analytics) never learns. If (2) succeeds and the DB txn rolls back → a\n  phantom event. Two systems, no shared transaction = guaranteed drift under failure.\n\nOUTBOX PATTERN:  in the SAME database transaction as the business write, also INSERT a row into an\n  outbox table. A separate process (CDC / a poller) reads the outbox and publishes to Kafka,\n  marking rows sent. Now the event and the state change commit atomically; publishing is\n  at-least-once and idempotent downstream.\nrelated:  TRANSACTIONAL messaging, the "listen to yourself" pattern, event sourcing (the event\n  log IS the source of truth; current state is a projection).</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>Orders occasionally never appear in the search index or the customer\'s email — always around deploys/restarts. Root cause: dual write (DB commit, then a separate Kafka publish that was lost on restart). Fix: outbox table written in the order transaction + Debezium publishing from it; backfill missed orders once.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>A consumer double-charges customers after a rebalance: it processed a payment, then crashed before committing the offset, so on restart it reprocessed. Fix: make the handler idempotent — a unique <code>payment_id</code> with an upsert / "insert if not exists" so reprocessing is a no-op; or Kafka read-process-write transactions.</p></div>',
    try: [['📖 microservices.io: Transactional Outbox', 'https://microservices.io/patterns/data/transactional-outbox.html', 'o']] },
  { html:
    '<p><b>Stream processing and materialised views:</b></p>' +
    '<pre><code>PROCESSING:  Kafka Streams / Flink / Spark Structured Streaming / ksqlDB — filter, enrich, join\n  (stream-stream with a time window, stream-table), aggregate in TUMBLING / HOPPING / SESSION\n  windows, and handle LATE / OUT-OF-ORDER events with watermarks.\nSTATE:  aggregations need state (a running count) → kept in an embedded store (RocksDB) + a\n  changelog topic for recovery. EXACTLY-ONCE via transactions + idempotent producers.\nMATERIALISED VIEW:  a continuously-updated query result (e.g. "orders per merchant per hour")\n  maintained by a stream job and served from a fast store — read latency of a cache, freshness\n  of a stream.\nEVENT SOURCING vs CRUD:  store the sequence of events (OrderPlaced, ItemAdded, OrderShipped) as\n  the truth; derive current state by replaying. Full audit + time travel + easy new projections;\n  cost: more complex reads (CQRS: a separate read model), schema/versioning of events forever.</code></pre>',
    try: [['📖 Apache Flink concepts', 'https://nightlies.apache.org/flink/flink-docs-stable/docs/concepts/overview/', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: In Kafka, what guarantees ordering, and at what granularity?\nA: Ordering is per PARTITION. Records with the same key hash to the same partition and are\n   consumed in order there. There is NO total order across partitions — pick the key so that\n   things that must be ordered (e.g. events for one account) share a partition.\n\nQ: What is the dual-write problem?\nA: Writing to two systems (a database and a message broker) without a shared transaction. A\n   crash between the two leaves them inconsistent — a committed state change with no event, or an\n   event with no state change.\n\nQ: How does the outbox pattern solve it?\nA: The event is inserted into an outbox table within the SAME transaction as the business write,\n   so they commit atomically. A separate relay (CDC or a poller) publishes outbox rows to the\n   broker at-least-once; consumers are idempotent.\n\nQ: Kafka gives at-least-once by default. What must consumers do?\nA: Be idempotent — dedupe by a business key or use upserts / "process only if not already\n   processed" — so that reprocessing after a crash-before-offset-commit is harmless. Or use\n   read-process-write transactions for exactly-once semantics.\n\nQ: CDC vs a nightly batch dump to sync a warehouse. Trade-offs?\nA: CDC: near-real-time, captures every change including deletes, ordered, low source load, but\n   needs a replication slot, schema-evolution handling, and careful PII governance. Batch: simple\n   and robust, but stale, misses intra-day changes, and heavier on the source.\n\nQ: What is log compaction and when do you use it?\nA: Kafka retains only the latest record per key (older ones are eventually removed). Use it for\n   changelog / snapshot topics where you want the current value per key retained indefinitely,\n   not the full history.\n\nQ: Event sourcing vs CRUD — one benefit and one cost.\nA: Benefit: a complete, replayable audit log and the ability to build new read projections after\n   the fact. Cost: reads are harder (you maintain separate read models / CQRS) and you must\n   version and keep every event schema forever.</code></pre>',
    try: [['📚 DDIA ch.11: Stream Processing', 'https://dataintensive.net/', 'o']] } ],
  quiz: [
    { q: 'Orders sometimes never reach the search index or notification service, especially around restarts. The service writes the order to Postgres, then publishes to Kafka. Root cause and fix?', opts: ['Kafka is down; add brokers', 'Dual-write inconsistency — a crash between the DB commit and the publish loses the event; fix with the transactional outbox pattern (write the event to an outbox table in the same DB transaction, relay it via CDC/poller)', 'The search index is too slow', 'Missing database index'], ok: 1, why: 'Two independent writes with no shared transaction drift under failure. The outbox pattern makes the event and the state change atomic by putting both in one transaction, then publishing the outbox rows separately with at-least-once delivery.' },
    { q: 'In Kafka, at what level is message ordering guaranteed?', opts: ['Across the entire topic', 'Per partition — records sharing a key go to the same partition and are consumed in order there; there is no cross-partition total order', 'Per consumer group', 'Per broker'], ok: 1, why: 'Kafka only orders within a partition. Choose the record key so that events which must be processed in sequence (e.g. per account or per entity) land on the same partition; different keys may be interleaved arbitrarily.' },
    { q: 'A Kafka consumer processed a payment, crashed before committing its offset, and on restart charged the customer again. Best fix?', opts: ['Switch to at-most-once delivery', 'Make the handler idempotent — dedupe on a unique payment_id (insert-if-not-exists / upsert) so reprocessing is a no-op — or use read-process-write transactions', 'Reduce the number of partitions to 1', 'Increase the session timeout'], ok: 1, why: 'Kafka is at-least-once by default, so a crash between processing and offset-commit causes replay. Idempotent processing keyed on a business identifier makes replays harmless; transactional exactly-once is the heavier alternative.' } ] },

/* ============================ 17. CHOOSING A DATASTORE — SYSTEM DESIGN ============================ */
{ num: 17, emoji: '🧮', title: 'Choosing a Datastore — System Design', layer: 'System Design',
  tagline: 'Access patterns → consistency → scale → latency → cost. Polyglot persistence and the classic prompts.',
  apps: ['the database system-design round', 'picking storage for a new service', 'justifying a migration'],
  levels: [
  { html:
    '<p>There is no "best database" — only the best fit for a set of requirements. Drive the choice from a checklist, in this order:</p>' +
    '<pre><code>1 ACCESS PATTERNS   what reads and writes, by what key, how often? (this dominates the choice)\n2 CONSISTENCY       does a read need the latest write? cross-entity transactions? or is stale OK?\n3 SCALE            data size now &amp; in 3 years · write throughput · read throughput · #entities\n4 LATENCY          p50 / p99 budget per operation · online vs batch\n5 QUERY SHAPE      point lookups? ranges? full-text? aggregations? graph traversals? ad-hoc?\n6 OPERATIONS + COST managed vs self-hosted · team familiarity · $ at scale · failure modes\n7 DATA SHAPE       relational? document? time-series? key-value? graph? vectors?</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Choosing a vehicle.</b> Nobody asks "what is the best vehicle" — they ask "carry 2 tonnes of gravel down a dirt track" (a truck) vs "commute 5 km in a city" (a bike). The cargo and the road pick the vehicle.</p></div>',
    try: [['📘 Part 4: ML system design framework', '../learn4/#ch13', 'o'], ['📚 DDIA (the whole book is this chapter)', 'https://dataintensive.net/', 'o']] },
  { html:
    '<p><b>A decision cheat-sheet</b> (defaults, not laws):</p>' +
    '<pre><code>need transactions, relations, ad-hoc queries, and you are not huge yet  → PostgreSQL (start here)\nsimple key lookups, caching, sessions, counters, rate limits           → Redis (+ a source of truth)\nmassive write throughput, always-on, multi-DC, query-first access      → Cassandra / ScyllaDB / DynamoDB\ndocument-shaped data, evolving schema, per-doc atomicity is enough      → MongoDB / Firestore (or Postgres+JSONB)\nfull-text search, faceting, log analytics                              → Elasticsearch / OpenSearch (derived index)\nanalytical scans / BI over billions of rows                            → Snowflake / BigQuery / ClickHouse / a lakehouse\nsemantic / similarity search over embeddings                          → pgvector (small) or a vector DB (large)\nmany-hop relationship queries, pathfinding, fraud rings                → Neo4j / Neptune (or recursive CTE if shallow)\ntime-series metrics with retention + downsampling                      → Timescale / InfluxDB / Prometheus / ClickHouse\nan append-only event log other systems react to                       → Kafka (as the backbone, not the store)\nglobal, strongly-consistent, horizontally-scaled SQL                   → Spanner / CockroachDB / YugabyteDB</code></pre>',
    try: [['📖 Database of Databases (compare 900+)', 'https://dbdb.io/', 'o']] },
  { html:
    '<p><b>Worked prompts</b> — say the access pattern, then the store:</p>' +
    '<pre><code>URL SHORTENER:  write once, read massively, point lookup by short code, ~immutable.\n  → KV store (DynamoDB / Redis) for code→URL; a counter/base62 or a hash for id generation;\n    CDN + cache in front; analytics events to Kafka → warehouse. Postgres is also fine at modest scale.\nNEWSFEED:  fan-out on write (push to each follower\'s feed) vs fan-out on read (merge at query time).\n  → feeds in Redis/Cassandra (per-user timeline lists); posts in Postgres/Cassandra; a graph or\n    table for follows; hybrid: push for most users, pull for celebrities (supernodes).\nCHAT:  high write, ordered per conversation, "last N messages", retention.\n  → Cassandra/DynamoDB keyed by ((conversation_id, time_bucket), ts); Kafka for delivery/fan-out;\n    Redis for presence.\nRATE LIMITER:  atomic increment + expiry per (user, window).  → Redis (INCR + EXPIRE, or a Lua\n    token-bucket).\nLEADERBOARD:  top-N by score, rank of a user.  → Redis sorted set (ZADD / ZREVRANGE / ZRANK).\nMETRICS / OBSERVABILITY:  huge append rate, time-range aggregations, downsampling, TTL.\n  → a time-series DB (Prometheus / Timescale / ClickHouse), NOT an OLTP row store.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>Capacity estimate on the spot: 50M users, each posts 2/day → 100M writes/day ≈ 1.2k WPS avg, ~6k peak; each user opens the feed 10x/day → 500M reads/day ≈ 6k RPS avg, ~30k peak. That read:write ratio + "latest 20 per user" says: precomputed per-user feed lists in a fast store, posts in a durable store, and heavy read caching.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>"We store everything in Postgres and search is slow, analytics locks the DB, and the embeddings table is 2 TB." → <b>polyglot persistence</b>: keep the system of record in Postgres, stream via CDC to Elasticsearch (search), to a warehouse (analytics), and to a vector DB (embeddings). One source of truth, purpose-built read paths.</p></div>',
    try: [['📖 System Design Primer', 'https://github.com/donnemartin/system-design-primer', 'o']] },
  { html:
    '<p><b>Polyglot persistence, migrations, and articulating tradeoffs:</b></p>' +
    '<pre><code>POLYGLOT PERSISTENCE:  use several stores, each for what it is best at, with ONE system of record\n  and the rest as derived (kept in sync by CDC / events). Cost: more systems to run, monitor,\n  secure, and reason about consistency across. Do not add a store without a clear, sized reason.\nMIGRATING stores:  dual-write (write both old + new) or CDC-replicate → backfill history →\n  shadow-read + compare → cut reads over gradually → decommission. Never a big-bang switch.\nTHE SENIOR SIGNAL in this round:  you START from access patterns and constraints, you NAME the\n  2-3 real tradeoffs (consistency vs latency, flexibility vs operational cost, one store vs\n  polyglot), you propose the SIMPLE option first (often "just Postgres") and say what would force\n  you off it, and you cover how it is operated (backups, failover, monitoring, cost).</code></pre>',
    try: [['📖 Martin Fowler: PolyglotPersistence', 'https://martinfowler.com/bliki/PolyglotPersistence.html', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: How do you choose a database for a new service?\nA: Start from access patterns (what keys, what queries, read/write ratio), then consistency needs\n   (latest-write? cross-entity transactions?), scale (size + throughput now and later), latency\n   budget, query shape (points/ranges/text/aggregations/graph), and operations/cost/team\n   familiarity. The data shape (relational/document/KV/time-series/graph/vector) falls out of that.\n\nQ: Design storage for a URL shortener.\nA: A key-value store (DynamoDB/Redis, or Postgres at modest scale) mapping short_code → long_url;\n   id generation via a counter+base62 or a hash with collision check; aggressive caching + CDN\n   because it is read-dominated and near-immutable; click events to a stream then a warehouse for\n   analytics.\n\nQ: When would you introduce a second datastore instead of scaling Postgres?\nA: When a specific access pattern is a poor fit AND is sized to matter: full-text relevance\n   search, billion-row analytical scans, embedding similarity search, many-hop graph traversals,\n   or extreme write throughput / always-on multi-region. Keep Postgres as the source of truth and\n   feed the specialised store via CDC.\n\nQ: What is polyglot persistence and its main cost?\nA: Using multiple databases, each for its strength, with one system of record and the rest\n   derived. The cost is operational and cognitive: more systems to run and secure, and\n   cross-store consistency to reason about.\n\nQ: Feed design — fan-out on write vs on read?\nA: Fan-out on write (push a post into every follower\'s timeline) gives fast reads but expensive\n   writes and wasted work for inactive users; fan-out on read (merge followees\' posts at query\n   time) is cheap to write but slow to read for users following many. Hybrid: push for typical\n   users, pull for celebrities.\n\nQ: How do you migrate from one datastore to another safely?\nA: Dual-write or CDC-replicate to the new store, backfill history, run shadow reads and compare\n   results, shift read traffic gradually behind a flag, then decommission the old store. Never a\n   single cutover.\n\nQ: The question says "design X". What do you ask first?\nA: Scale (users, QPS, data size), the access patterns and read/write ratio, the latency budget,\n   consistency requirements, and what "good" means — before naming any database.</code></pre>',
    try: [['📖 ByteByteGo / system design case studies', 'https://bytebytego.com/', 'o']] } ],
  quiz: [
    { q: 'What should drive a datastore choice more than anything else?', opts: ['The most popular database this year', 'The access patterns — which reads/writes, by what key, at what ratio and rate — plus consistency and latency requirements', 'The programming language of the service', 'Whether it is open source'], ok: 1, why: 'The query and write shape determines whether you need points, ranges, text, aggregations, or traversals, and the throughput/latency/consistency needs narrow it further. Data model and specific product fall out of that analysis.' },
    { q: 'Your Postgres system of record is fine, but search is slow, analytics queries lock the DB, and an embeddings table is huge. Best direction?', opts: ['Shard Postgres immediately', 'Polyglot persistence: keep Postgres as the source of truth and stream via CDC to Elasticsearch (search), a warehouse (analytics), and a vector DB (embeddings)', 'Move everything to MongoDB', 'Disable analytics'], ok: 1, why: 'Each pain point is a distinct access pattern poorly served by an OLTP row store. Feeding purpose-built read stores from the existing source of truth via CDC solves them without abandoning the transactional core.' },
    { q: 'For a leaderboard needing "top 10 by score" and "what rank is user X", the natural fit is...', opts: ['A relational table with ORDER BY score on every request', 'A Redis sorted set (ZADD / ZREVRANGE / ZRANK) — O(log n) rank and range operations kept in memory', 'Elasticsearch', 'A graph database'], ok: 1, why: 'A sorted set maintains elements ordered by score with logarithmic insert, rank, and range queries. Recomputing an ORDER BY over a large table per request is far more expensive; the leaderboard is a canonical sorted-set use case.' } ] },

/* ============================ 18. DATABASE OPS & RELIABILITY ============================ */
{ num: 18, emoji: '🛠️', title: 'Database Operations & Reliability', layer: 'Operations',
  tagline: 'Zero-downtime migrations, connection pools, slow-query triage, the metrics that predict an outage, and runbooks.',
  apps: ['on-call for a database', 'shipping a schema change without an incident', 'capacity & cost planning'],
  levels: [
  { html:
    '<p>Most database incidents are not exotic — they are a migration that locked a table, a connection pool that ran out, a query that went quadratic after data grew, a disk that filled, or a backup that was never tested. Operations is knowing these in advance.</p>' +
    '<pre><code>the recurring outage causes:\n  • a schema change took a table-level lock (or queued behind a long query) and froze everything\n  • connections exhausted (a leak, a traffic spike, no pooler) → "too many clients"\n  • a query plan flipped (stale stats / data growth) → p99 10x → cascading timeouts\n  • disk full (WAL not archived, a stuck replication slot, unvacuumed bloat, logs)\n  • failover did not work / was never tested → extended downtime, or split-brain\n  • a backup restore was never rehearsed → RTO is "unknown"</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A fire drill you actually run.</b> The building with rehearsed evacuations gets everyone out in 2 minutes; the one with a dusty printed plan does not. An untested backup / failover is the dusty plan.</p></div>',
    try: [['📘 Part 4: ML in production — monitoring & rollback', '../learn4/#ch16', 'o'], ['📚 The DDIA "operability" thread', 'https://dataintensive.net/', 'o']] },
  { html:
    '<p><b>Zero-downtime schema changes — the expand/contract (parallel-change) pattern:</b></p>' +
    '<pre><code>NEVER in one step:  rename a column · change a column type · add a NOT NULL to a big table ·\n  drop a column the running code still selects.\nEXPAND / CONTRACT:\n  1 EXPAND   add the new column/table (nullable, no volatile default). deploy code that WRITES\n            both old and new, READS old.\n  2 BACKFILL  copy old → new in batches (throttled, off-peak), verify counts.\n  3 MIGRATE   deploy code that READS new (still writes both).\n  4 CONTRACT  stop writing old; drop the old column later, in its own migration.\nrules:  each step is backward-compatible with the previous deploy (so rollback is safe) ·\n  set lock_timeout / statement_timeout on migrations · add indexes CONCURRENTLY · add FKs / CHECKs\n  as NOT VALID then VALIDATE · run big backfills as a job, not in the migration transaction.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario A</span><p>A "quick" migration <code>ALTER TABLE users ALTER COLUMN status TYPE varchar(32)</code> on a 400M-row table took an <code>ACCESS EXCLUSIVE</code> lock, rewrote the whole table, and took the site down for 25 minutes. Correct approach: add <code>status_v2</code>, dual-write, backfill in batches, switch reads, drop <code>status</code> later.</p></div>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario B</span><p>A deploy renamed <code>amount_cents</code> → <code>amount</code> in one migration. The old app pods (still rolling) crashed on every write (<code>column "amount_cents" does not exist</code>). Fix: add <code>amount</code>, backfill, dual-write, cut over, then drop <code>amount_cents</code> — four deploys, zero downtime.</p></div>',
    try: [['📖 GitLab / Stripe: online schema migration guides', 'https://docs.gitlab.com/ee/development/database/avoiding_downtime_in_migrations.html', 'o']] },
  { html:
    '<p><b>Connection pools and slow-query triage:</b></p>' +
    '<pre><code>POOL SIZING:  more connections is NOT more throughput past a point (context switching, lock\n  contention). Rule of thumb for Postgres: pool ≈ ((core_count * 2) + effective_spindles),\n  then measure. Use a pooler (PgBouncer) so thousands of app workers share a small server pool.\n  a leak (connections not returned) looks like a slow creep to "pool exhausted" under load.\nSLOW QUERY TRIAGE (in order):\n  1 EXPLAIN (ANALYZE, BUFFERS)  — seq scan on a big table? bad row estimate? nested loop over\n    millions? sort spilling to disk? index not used?\n  2 is it slow ALWAYS or only sometimes?  → plan instability (stats), or lock waits, or cache-cold\n  3 pg_stat_statements  — is this query even a top consumer, or a symptom of contention?\n  4 the fix ladder:  add/adjust an index · rewrite (avoid functions on columns, OR → UNION,\n    correlated subquery → join) · ANALYZE / raise statistics target · add a covering index ·\n    denormalise / materialise · batch it · cache it · move it off the primary.</code></pre>',
    try: [['📖 Use The Index, Luke — full toc', 'https://use-the-index-luke.com/', 'o']] },
  { html:
    '<p><b>The metrics that predict an outage</b>, plus capacity &amp; cost:</p>' +
    '<pre><code>ALARM on:\n  • replication lag (seconds / bytes behind) — read staleness &amp; a failover data-loss risk\n  • connection count vs max, and % idle-in-transaction — pool exhaustion incoming\n  • cache/buffer hit ratio dropping — working set outgrew RAM → I/O storm\n  • disk free % and growth rate — WAL, bloat, unconsumed slots; project days-to-full\n  • locks / lock wait time / deadlocks/min — contention\n  • table + index bloat, oldest transaction age, autovacuum lag — Postgres-specific time bombs\n  • p50/p95/p99 query latency and error rate — the user-facing signal\n  • CPU / IOPS / IO wait — saturation\nCAPACITY:  project data + throughput growth; know the vertical ceiling and the plan for when you\n  hit it (read replicas → caching → archiving → partitioning → sharding / a distributed DB).\nCOST:  right-size instances; storage tiering; drop unused indexes; kill runaway analytics on the\n  primary; per-scan billing on warehouses rewards partitioning + not SELECT *.\nRUNBOOKS:  for each alarm — what it means, how to confirm, the mitigation, how to escalate.\n  and: rehearse a failover and a restore on a schedule, not during the incident.</code></pre>',
    try: [['📖 Google SRE Book (databases & toil)', 'https://sre.google/books/', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: How do you rename a column in production with zero downtime?\nA: Expand/contract: add the new column, deploy code that writes both and reads the old, backfill\n   in batches, deploy code that reads the new, then in a later migration stop writing and drop the\n   old column. Every step is backward-compatible so a mid-rollout rollback is safe.\n\nQ: A migration froze the whole database. What likely happened and how do you prevent it?\nA: The DDL needed an ACCESS EXCLUSIVE lock and either did a full table rewrite (type change,\n   volatile default) or queued behind a long-running query/autovacuum — and the queued lock then\n   blocked all new transactions on that table. Prevent with lock_timeout, CONCURRENTLY for\n   indexes, NOT VALID + VALIDATE for constraints, batched backfills, and maintenance windows.\n\nQ: Does raising the connection limit fix "too many connections"?\nA: Usually no — past a point more connections reduce throughput (contention, memory). Add a\n   transaction-mode pooler so many app clients share a small server pool, fix any connection\n   leak, and size the pool near (cores * 2) then measure.\n\nQ: A query got slow as data grew. Diagnosis steps?\nA: EXPLAIN (ANALYZE, BUFFERS) to see the plan and estimate errors; check whether it is always\n   slow (plan/stats) or intermittently (locks / cold cache); confirm it is a real top consumer\n   via pg_stat_statements; then index / rewrite / ANALYZE / materialise / cache as appropriate.\n\nQ: Which metrics would you alarm on for a primary database?\nA: Replication lag, connections vs max and idle-in-transaction, buffer cache hit ratio, disk free\n   % and growth, lock waits / deadlocks, bloat and oldest-transaction age (Postgres), and\n   p95/p99 latency + error rate.\n\nQ: Your backups run nightly and "succeed". Are you safe?\nA: Not until you have rehearsed a full restore and measured the time (your real RTO) and the data\n   loss (RPO = backup interval + replication lag). An untested backup is a hope. Test on a schedule.\n\nQ: Disk on the primary is filling fast and nothing obvious changed. Suspects?\nA: An unconsumed replication slot retaining WAL, WAL archiving broken, table/index bloat from\n   stalled autovacuum (often held by a long transaction), or log/temp-file growth. Check slot lag,\n   archiver status, oldest transaction, and dead-tuple counts.</code></pre>',
    try: [['📖 PostgreSQL wiki: monitoring', 'https://wiki.postgresql.org/wiki/Monitoring', 'o']] } ],
  quiz: [
    { q: 'What is the safe way to change a column\'s data type on a very large, live table?', opts: ['ALTER TABLE ... ALTER COLUMN ... TYPE during off-peak hours', 'Expand/contract: add a new column, dual-write from the app, backfill in batches, switch reads, then drop the old column in a later migration', 'Put the database in single-user mode', 'Drop and recreate the table'], ok: 1, why: 'An in-place type change typically rewrites the whole table under an exclusive lock, causing an outage. The parallel-change pattern makes each step backward-compatible and non-blocking, so the change (and any rollback) is safe.' },
    { q: 'Your app reports "too many connections" under load. Best first action?', opts: ['Raise max_connections to 10000', 'Introduce a transaction-mode connection pooler (e.g. PgBouncer), cap the app pool, and check for a connection leak', 'Add read replicas', 'Restart the database hourly'], ok: 1, why: 'Beyond a modest number, more connections hurt throughput. A pooler multiplexes many client sessions onto a small server pool; leaks and unbounded app pools are the usual root cause of exhaustion.' },
    { q: 'Nightly backups "succeed" for months. Why might you still not meet your recovery objectives?', opts: ['Backups are always sufficient', 'You have never rehearsed a full restore, so your real RTO (restore time) is unknown, and RPO equals the backup interval plus replication lag — both unverified', 'Backups do not include indexes', 'The backup format is deprecated'], ok: 1, why: 'A backup that has never been restored is untested. Only a rehearsed restore tells you how long recovery takes (RTO) and how much data you lose (RPO). Schedule regular restore drills and failover tests.' } ] }

];
