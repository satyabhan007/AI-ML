apiVersion: v2
name: ai-ml-serving
description: >-
  Part 6 lab — a KServe InferenceService serving an open-weight LLM with vLLM.
  Wraps the standard runtime (Ch 4) with autoscaling + canary (P7 Ch 5).
type: application
version: 0.1.0
appVersion: "1.0.0"
