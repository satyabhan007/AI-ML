/* AI-ML Learn — PART 4 (ML Interviews & Production Systems). window.CHAPTERS = 17 chapters x 5 levels. */
window.CHAPTERS = [

/* ============================ 1. THE ML INTERVIEW MAP ============================ */
{ num: 1, emoji: '🗺️', title: 'The ML Interview Map', layer: 'Interview Prep',
  tagline: 'Five different rounds, five different games. Know which one you are in and how it is scored.',
  apps: ['DS / MLE / applied-scientist loops', 'take-home + on-site combos', 'levelling & scope calibration'],
  levels: [
  { html:
    '<p>An "ML interview" is really 5 distinct rounds, each with its own rubric. Preparing for all of them as if they were one is why strong candidates fail loops.</p>' +
    '<pre><code>ROUND                     WHAT IT SCORES\ncoding / DSA              can you write correct, efficient code under time pressure\nML fundamentals           do you understand WHY algorithms work (whiteboard, no library)\nML system design          can you design an end-to-end ML product (data → serving → monitoring)\nML case / product sense    can you turn a vague business goal into a measurable ML problem\nbehavioural               collaboration, ownership, conflict, impact — with the STAR structure</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A pentathlon, not a sprint.</b> The person who trains only running loses to the one who is competent at all five events. Each round has a different surface and a different judge.</p></div>',
    try: [['📘 Part 3: the Python-stack interview drills', '../learn3/#ch1', 'o']] },
  { html:
    '<p>How each round is actually graded — the signal the interviewer writes down:</p>' +
    '<pre><code>coding:      correctness → edge cases → complexity → clean code → communication while coding\nfundamentals: correct mental model → can derive, not just recite → knows failure modes & tradeoffs\nsystem design: clarifies scope → picks a metric → reasons about data/latency/scale → names tradeoffs\n              → covers training + serving + monitoring, not just "use XGBoost"\ncase/product:  structures ambiguity → proposes a testable hypothesis → picks success + guardrail metrics\nbehavioural:   specific stories, YOUR actions (not "we"), measurable outcome, honest reflection</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Show your working.</b> A right answer with no reasoning scores lower than a slightly-wrong answer that reasons out loud. The interviewer is grading the <i>process</i> that would generalise to the job.</p></div>',
    try: [['📖 Chip Huyen: ML Interviews Book', 'https://huyenchip.com/ml-interviews-book/', 'o']] },
  { html:
    '<p>A structure for the two hardest rounds:</p>' +
    '<pre><code>SYSTEM DESIGN (see ch.13):\n  1 clarify: users, scale (QPS, #items), latency budget, online/batch, what "good" means\n  2 metric: business KPI → ML proxy (offline) + online metric + guardrails\n  3 data: sources, labels (how obtained, delay, bias), volume, freshness\n  4 features: candidate features, leakage check, feature store / pipeline\n  5 model: baseline first → then the "real" model; justify the family\n  6 training: split strategy, retrain cadence, cold start\n  7 serving: retrieval + ranking, batch vs online, latency, fallback\n  8 monitoring: data/pred drift, delayed-label metrics, A/B, rollback\n\nCASE / PRODUCT:\n  goal → who/what/why → hypothesis → metric (+ guardrail) → design the experiment → decision rule</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>"How would you reduce churn?" is a product case, not a modelling question. Start: define churn, segment users, form a hypothesis (e.g. "onboarding friction drives 30-day churn"), name the metric and guardrail, propose the experiment — <i>then</i> a model if it earns its place.</p></div>',
    try: [['📖 Educative: Grokking the ML Interview', 'https://www.educative.io/courses/grokking-the-machine-learning-interview', 'o']] },
  { html:
    '<p>Red flags interviewers explicitly note, and how to avoid them:</p>' +
    '<pre><code>• jumping to "deep learning" / "an LLM" before defining the metric or checking a baseline\n• no mention of the label: where it comes from, how delayed, how biased\n• ignoring serving latency, cost, and failure modes\n• "we did X" with no clarity on what YOU did\n• accepting the question as stated — not clarifying scope, scale, or success criteria\n• memorised definitions with no ability to derive or discuss when they break\n• no monitoring / iteration story — treating deploy as the finish line</code></pre>' +
    '<p><b>Reverse questions</b> that land well: "What does success look like for this role in 6 months?", "How is ML work prioritised vs product?", "What is the on-call / incident story for models?", "How mature is the eval / experimentation infra?"</p>',
    try: [['📘 Part 2: monitoring & evals as production practice', '../learn2/#ch14', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Walk me through how you would approach an ambiguous prompt like "improve recommendations".\nA: Clarify (which surface, which users, what KPI, what constraints), pick a metric + guardrail,\n   establish a baseline, form a hypothesis, design an offline eval and an online A/B, propose a\n   simple model first, then iterate. Explicitly cover data/labels, serving latency, and monitoring.\n\nQ: You are asked to design a model and you realise a heuristic would solve 80% of it. What do you do?\nA: Say so. Propose the heuristic as the baseline, quantify the gap it leaves, and justify the model\n   only for the remaining value. Interviewers reward this; over-engineering is a red flag.\n\nQ: The behavioural round asks about a project failure. Structure?\nA: STAR — Situation, Task, Action (YOUR actions, first person), Result (measurable), plus a\n   sentence on what you changed afterwards. Pick a real failure with a real lesson.\n\nQ: How do you handle "I do not know" in a fundamentals round?\nA: State what you DO know, reason from first principles toward the answer, name what you would look\n   up. Bluffing a wrong confident answer scores worst.\n\nQ: What separates a senior from a junior answer in system design?\nA: Senior: leads with metrics and constraints, names 2-3 explicit tradeoffs, covers the full\n   lifecycle (labels, serving, drift, rollback), and knows when NOT to use ML.</code></pre>',
    try: [['📖 Meta/Google DS interview guides (search)', 'https://www.metacareers.com/life/preparing-for-your-data-scientist-interview-at-meta/', 'o']] } ],
  quiz: [
    { q: 'In an ML system design round, what should you establish before choosing a model family?', opts: ['The exact hyperparameters', 'The users/scale/latency constraints and the success + guardrail metrics (and a baseline)', 'Which GPU to use', 'The dataset file format'], ok: 1, why: 'Design rounds grade whether you scope the problem and tie it to measurable outcomes first. Metric, constraints, data/labels, and a baseline come before "use model X".' },
    { q: 'Which is a documented red flag in ML interviews?', opts: ['Proposing a simple baseline first', 'Jumping straight to deep learning / an LLM without defining the metric, the label source, or checking a heuristic', 'Asking clarifying questions', 'Mentioning monitoring and rollback'], ok: 1, why: 'Reaching for the most complex tool before framing the problem, and ignoring where labels come from, signals weak judgement. Baselines, clarifying questions, and a monitoring story are positive signals.' },
    { q: 'For the behavioural round, why insist on first-person "I did X" instead of "we did X"?', opts: ['It sounds more confident', 'The interviewer must assess YOUR contribution and decision-making; "we" hides whether you led, followed, or observed', 'Companies dislike teamwork', 'It is shorter'], ok: 1, why: 'STAR answers are scored on your specific actions and judgement. "We" is ambiguous about your role; be explicit about what you personally decided and did, while still crediting the team.' } ] },

/* ============================ 2. PROBABILITY & STATISTICS ============================ */
{ num: 2, emoji: '🎲', title: 'Probability & Statistics (the quant round)', layer: 'Theory',
  tagline: 'Bayes, expectation, MLE, the CLT, and the brain-teasers — derived, not memorised.',
  apps: ['DS / quant-DS screens', 'the "estimate this" whiteboard', 'foundations for A/B testing & causal work'],
  levels: [
  { html:
    '<p>Two rules generate most of probability. <b>Conditional probability:</b> <code>P(A|B) = P(A and B) / P(B)</code>. <b>Bayes:</b> flip the condition — <code>P(A|B) = P(B|A) P(A) / P(B)</code>.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A rare disease + a good test = mostly false alarms.</b> Disease prevalence 0.1%, test 99% sensitive and 99% specific. Positive test → P(disease | +) ≈ (0.99 x 0.001) / (0.99 x 0.001 + 0.01 x 0.999) ≈ <b>9%</b>. The base rate dominates. This is the #1 quant-round trap.</p></div>' +
    '<pre><code>P(+)           = P(+|D)P(D) + P(+|~D)P(~D)      # total probability\nP(D|+)         = P(+|D)P(D) / P(+)              # Bayes\nexpectation    E[X] = Σ x·P(x)   (linear: E[aX+bY] = aE[X] + bE[Y], ALWAYS)\nvariance       Var(X) = E[X^2] - E[X]^2 ;  Var(aX) = a^2 Var(X)</code></pre>',
    try: [['📖 Seeing Theory (visual probability)', 'https://seeing-theory.brown.edu/', 'o']] },
  { html:
    '<p>The distributions you must know cold, and where they show up:</p>' +
    '<pre><code>Bernoulli / Binomial   one / n coin flips — conversions, click/no-click\nPoisson                count of rare events per interval — arrivals, defects (mean = variance = λ)\nExponential            time BETWEEN Poisson events — session gaps (memoryless)\nNormal                 sums/averages of many things (CLT) — measurement noise, test statistics\nUniform                "no information" prior; simulation\nBeta                   a distribution over a probability — Bayesian conversion rate, Thompson sampling\n\nCLT: the mean of n iid samples → Normal(μ, σ^2/n) as n grows, whatever the original shape.\nThat is why the standard error is σ/√n and why doubling precision needs 4x the data.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>The CLT is why averages are boring.</b> Individual users are wild; the average of 10,000 of them is a tight, predictable bell — which is exactly what makes A/B tests possible.</p></div>',
    try: [['📖 Common distributions cheat sheet', 'https://en.wikipedia.org/wiki/List_of_probability_distributions', 'o']] },
  { html:
    '<p><b>MLE vs MAP</b>, estimator quality, and intervals:</p>' +
    '<pre><code>MLE   θ that maximises P(data | θ)         — pick the parameter that makes the data most likely\nMAP   θ that maximises P(data|θ) P(θ)     — MLE + a prior; the prior acts like regularisation\n      (L2 penalty ⇔ Gaussian prior ; L1 ⇔ Laplace prior)\n\nbias        E[θ̂] - θ            (systematic error)\nvariance    Var(θ̂)              (sensitivity to the sample)\nconsistency θ̂ → θ as n → ∞\nMSE = bias^2 + variance\n\nfrequentist CI: "95% of intervals built this way contain the true θ" (θ is fixed, the interval is random)\nBayesian credible interval: "95% probability θ is in here, given the data + prior"</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>Estimating a conversion rate from 40/1000: MLE = 0.04. With a Beta(1,1) prior the posterior is Beta(41, 961), mean ≈ 0.0409, and you get a full distribution to reason about (and to feed a bandit).</p></div>',
    try: [['📘 Part 1: MLE-style loss minimisation from scratch', '../learn/#ch1', 'o']] },
  { html:
    '<p>Worked brain-teasers (say the setup out loud, then compute):</p>' +
    '<pre><code>• "Expected rolls of a fair die to see all 6 faces?" → coupon collector: 6(1 + 1/2 + ... + 1/6) = 14.7\n• "Two fair dice, sum is 7 — probability the first die is 4?" → 6 ways to make 7; (4,3) is one → 1/6\n• "P(at least one shared birthday in 23 people)?" → 1 - 365!/(342! · 365^23) ≈ 0.507\n• "Stick broken at two uniform points — P(triangle)?" → 1/4\n• "E[max of two U(0,1)]?" → 2/3 ;  E[min] = 1/3\n• "Unfair coin, p unknown — simulate a fair coin?" → flip twice: HT→heads, TH→tails, else retry (von Neumann)\n• "A/B: 200/5000 vs 240/5000 — significant?" → SE ≈ sqrt(2·0.044·0.956/5000) ≈ 0.0041; diff 0.008 → z≈2 → p≈0.045</code></pre>',
    try: [['📖 Brainstellar / quant puzzles', 'https://brainstellar.com/', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Explain Bayes theorem and why base rates matter.\nA: P(A|B) = P(B|A)P(A)/P(B). When P(A) (the prior/base rate) is tiny, even an accurate test\n   yields mostly false positives, because the huge negative population contributes many false alarms.\n\nQ: What does the Central Limit Theorem let you do?\nA: Treat the sampling distribution of a mean as Normal(μ, σ^2/n) regardless of the population shape,\n   which is the basis for z/t tests, confidence intervals, and A/B significance.\n\nQ: MLE vs MAP?\nA: MLE maximises the likelihood of the data. MAP adds a prior (maximises likelihood x prior).\n   A Gaussian prior on weights is exactly L2 regularisation; a Laplace prior is L1.\n\nQ: Difference between a 95% confidence interval and a 95% credible interval?\nA: CI: a procedure such that 95% of such intervals cover the fixed true parameter (no probability\n   statement about THIS interval). Credible interval: given the data and prior, 95% posterior\n   probability the parameter lies in it.\n\nQ: E[X] and Var(X) for a Binomial(n, p)?\nA: E = np, Var = np(1-p).\n\nQ: You flip a coin 10 times and get 8 heads. Is it biased?\nA: Under fair, P(≥8 heads) = C(10,8)+C(10,9)+C(10,10) all /1024 ≈ 5.5%. Weak evidence; not\n   conclusive at α=0.05 two-sided. Need more flips / a prior.\n\nQ: Covariance vs correlation?\nA: Covariance = E[(X-μx)(Y-μy)], scale-dependent. Correlation = covariance / (σx σy), in [-1, 1],\n   scale-free; measures LINEAR association only.</code></pre>',
    try: [['📖 MIT 6.041 Probability', 'https://ocw.mit.edu/courses/6-041-probabilistic-systems-analysis-and-applied-probability-fall-2010/', 'o']] } ],
  quiz: [
    { q: 'A disease affects 1 in 1000. A test is 99% sensitive and 99% specific. A person tests positive. P(they have the disease)?', opts: ['~99%', '~50%', '~9%', '~1%'], ok: 2, why: 'P(D|+) = (0.99*0.001) / (0.99*0.001 + 0.01*0.999) ≈ 0.00099 / 0.01098 ≈ 9%. The low base rate means most positives are false positives from the large healthy population.' },
    { q: 'Adding an L2 penalty to a regression is equivalent, in Bayesian terms, to...', opts: ['A uniform prior on the weights', 'A zero-mean Gaussian prior on the weights (MAP estimation)', 'Dropping the likelihood term', 'A Laplace prior on the weights'], ok: 1, why: 'MAP = argmax likelihood x prior. A zero-mean Gaussian prior contributes a squared-weight term to the log-posterior, which is exactly L2. A Laplace prior gives L1.' },
    { q: 'Which statement about a 95% frequentist confidence interval is correct?', opts: ['There is a 95% probability the true parameter is in this specific interval', 'If you repeated the sampling+interval procedure many times, ~95% of the intervals would contain the fixed true parameter', 'The parameter is random and the interval is fixed', '95% of the data lies in the interval'], ok: 1, why: 'In the frequentist view the parameter is fixed and the interval is the random quantity. The 95% refers to the long-run coverage of the procedure, not to a probability about one realised interval.' } ] },

/* ============================ 3. BIAS/VARIANCE & LINEAR MODELS ============================ */
{ num: 3, emoji: '⚖️', title: 'Bias/Variance, Regularization & Linear Models', layer: 'Theory',
  tagline: 'The one decomposition every ML round returns to, and why L1 zeros things out while L2 shrinks.',
  apps: ['the "why is my model overfitting" question', 'linear/logistic regression derivation', 'regularisation tradeoff discussions'],
  levels: [
  { html:
    '<p>Expected test error splits into three parts:</p>' +
    '<pre><code>E[(y - f̂(x))^2]  =  bias(f̂)^2   +   variance(f̂)   +   irreducible noise σ^2\n\nbias      how far the AVERAGE model (over many training sets) is from the truth — too-simple models\nvariance  how much the model WOBBLES between training sets — too-flexible models\nnoise     the floor you can never beat</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Archery.</b> High bias = arrows tightly grouped but off the bullseye (consistent + wrong). High variance = arrows scattered all around it (right on average, unreliable). You want tight <i>and</i> centred.</p></div>' +
    '<p>Underfit = high bias (train and test both bad). Overfit = high variance (train great, test bad). The gap between train and validation error is your variance read-out.</p>',
    try: [['📘 Part 1: fit an MLP and watch it overfit', '../learn/#ch1', 'o']] },
  { html:
    '<p><b>Regularisation</b> trades a little bias for a lot less variance by penalising model complexity.</p>' +
    '<pre><code>Ridge (L2):  loss + λ Σ w_j^2      → shrinks all weights toward 0 smoothly; keeps every feature\nLasso (L1):  loss + λ Σ |w_j|      → drives some weights EXACTLY to 0 → automatic feature selection\nElastic Net: α·L1 + (1-α)·L2       → sparsity + stability with correlated features\n\nWhy L1 zeros out: the |w| penalty has a sharp corner at 0, so the optimum often lands exactly\non an axis (w_j = 0). The w^2 penalty is smooth, so it only ever shrinks.\nλ ↑  → more bias, less variance.  Tune λ by cross-validation.\nSCALE FEATURES FIRST — the penalty is not scale-invariant.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A budget with a hard cap vs a soft nudge.</b> L1 is a strict spending cap that forces some line items to zero. L2 is a gentle "spend less on everything" that trims but keeps all line items alive.</p></div>',
    try: [['📖 ISLR (free): Linear Models & Regularization', 'https://www.statlearning.com/', 'o']] },
  { html:
    '<p><b>Linear & logistic regression</b> — derive them, do not recite:</p>' +
    '<pre><code>LINEAR:   ŷ = w·x + b ;  loss = mean (ŷ - y)^2\n  gradient: dL/dw = (2/n) Xᵀ(ŷ - y) ;  closed form: w = (XᵀX)^-1 Xᵀy  (OLS normal equation)\n  assumptions: linearity, independent errors, constant error variance, ~normal errors (for inference)\n\nLOGISTIC: p = σ(w·x + b) ;  loss = binary cross-entropy = -mean[ y log p + (1-y) log(1-p) ]\n  gradient has the SAME clean form: (1/n) Xᵀ(p - y)\n  output is a probability; decision threshold is a separate choice (default 0.5 is rarely optimal)\n  NOT solvable in closed form → gradient descent / Newton (IRLS)</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>Credit scoring picks logistic regression over a boosted tree despite lower AUC: it is monotonic, the coefficients are auditable log-odds, and regulators can be shown exactly why an applicant was declined.</p></div>',
    try: [['📖 CS229 notes: linear/logistic regression', 'https://cs229.stanford.edu/main_notes.pdf', 'o']] },
  { html:
    '<p>Fixing each failure mode:</p>' +
    '<pre><code>HIGH BIAS (underfit): more features / interactions, a more flexible model, less regularisation,\n                     train longer, better features. More DATA will NOT help.\nHIGH VARIANCE (overfit): more data, stronger regularisation, fewer features, simpler model,\n                     early stopping, bagging/ensembling, data augmentation, dropout.\n\n• diagnose with LEARNING CURVES: plot train & val error vs training-set size.\n  curves converge high → bias.  large persistent gap → variance.\n• the "double descent" caveat: very overparameterised models can improve again past the\n  interpolation threshold — modern DL lives here.\n• regularisation ≠ only a penalty: dropout, early stopping, data aug, weight sharing, and\n  smaller batch sizes all regularise.</code></pre>',
    try: [['📖 Learning curves (sklearn)', 'https://scikit-learn.org/stable/modules/learning_curve.html', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Define the bias-variance tradeoff.\nA: Test error = bias^2 + variance + noise. Simple models have high bias (miss real structure);\n   flexible models have high variance (fit noise). You tune complexity/regularisation to minimise\n   the sum, judged on held-out data.\n\nQ: Your model has 0.02 train error and 0.30 val error. Diagnosis and 3 fixes?\nA: High variance / overfitting. Fixes: more data, stronger regularisation (λ up / dropout),\n   fewer features, simpler model, early stopping, ensembling.\n\nQ: L1 vs L2 — mechanism and when to use each.\nA: L1 (|w|) has a corner at 0 so it produces exact zeros → sparse models / feature selection,\n   good when you believe few features matter. L2 (w^2) is smooth → shrinks all weights, more\n   stable with correlated features. Elastic Net combines both.\n\nQ: Why must you scale features before regularised regression?\nA: The penalty sums w_j (or w_j^2); a feature on a larger scale gets an artificially larger\n   coefficient and is penalised unfairly. Standardise so the penalty is comparable across features.\n\nQ: Does logistic regression have a closed-form solution?\nA: No — the log-likelihood is concave but transcendental. Fit with gradient descent, L-BFGS, or\n   IRLS/Newton. Linear regression does have one: the normal equation (XᵀX)^-1 Xᵀy.\n\nQ: Adding more training data — when does it help?\nA: It reduces variance, so it helps an overfitting model. It does essentially nothing for a\n   high-bias (underfitting) model — you need more capacity or better features there.</code></pre>',
    try: [['📖 Understanding the Bias-Variance Tradeoff (Scott Fortmann-Roe)', 'https://scott.fortmann-roe.com/docs/BiasVariance.html', 'o']] } ],
  quiz: [
    { q: 'Train error 3%, validation error 25%. What is the problem and which fix does NOT help?', opts: ['High bias; fix = add features', 'High variance; more training data helps, but adding capacity/features would make it worse', 'High variance; more training data will NOT help', 'Irreducible noise; nothing helps'], ok: 1, why: 'A large train/val gap is high variance (overfitting). More data, regularisation, and simpler models help; adding capacity or features worsens it. (Note: for high bias, more data would not help.)' },
    { q: 'Why does L1 regularisation produce exactly-zero coefficients while L2 does not?', opts: ['L1 uses integers', 'The |w| penalty is non-differentiable at 0 with a sharp corner, so the constrained optimum frequently sits exactly on an axis; the smooth w^2 penalty only shrinks', 'L2 is applied after training', 'L1 has a larger lambda by default'], ok: 1, why: 'Geometrically, the L1 constraint region is a diamond with corners on the axes, so the loss contour often first touches it at a corner (a zeroed weight). The L2 ball is round, so contact points rarely have any coordinate exactly zero.' },
    { q: 'Logistic regression is often chosen over gradient boosting for credit decisions primarily because...', opts: ['It always has higher AUC', 'Its coefficients are interpretable log-odds and the model is monotonic/auditable for regulators', 'It trains faster', 'It needs no data cleaning'], ok: 1, why: 'Regulated lending requires explainable, monotonic, auditable decisions. Logistic regression gives per-feature log-odds contributions you can show an applicant, even at some cost in raw accuracy.' } ] },

/* ============================ 4. TREES, SVM, kNN, NAIVE BAYES ============================ */
{ num: 4, emoji: '🌲', title: 'Trees, SVM, kNN & Naive Bayes', layer: 'Theory',
  tagline: 'The classic non-linear models: how each one draws its decision boundary, and where each one breaks.',
  apps: ['whiteboard "how does a decision tree split"', 'model-choice justification', 'small-data / interpretable baselines'],
  levels: [
  { html:
    '<p>Four ways to carve up feature space:</p>' +
    '<pre><code>Decision tree   recursively splits on one feature at a time → axis-aligned rectangular regions\nSVM             finds the single hyperplane with the LARGEST MARGIN between classes (+ kernels for curves)\nkNN             no training; predict = vote of the k nearest stored points\nNaive Bayes     P(class | features) ∝ P(class) · Π P(feature_i | class)   assuming features independent</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Tree = a game of 20 questions. SVM = drawing the widest possible no-mans-land between two armies. kNN = "you are who your neighbours are". Naive Bayes = a gullible detective who assumes every clue is unrelated to every other.</b></p></div>',
    try: [['📘 Part 1: Random Forest vs Gradient Boosting recap', '../learn/#ch1', 'o']] },
  { html:
    '<p><b>Decision trees</b>, in detail:</p>' +
    '<pre><code>at each node, pick the (feature, threshold) that most reduces impurity:\n  Gini    = 1 - Σ p_c^2        (probability two random picks differ in class)\n  Entropy = -Σ p_c log p_c     (bits of surprise); "information gain" = entropy drop\nstop when: max depth, min samples per leaf, or no split improves impurity\nprediction: majority class (or mean) in the leaf\n\n+ interpretable, no scaling needed, handles mixed types, captures interactions for free\n- HIGH VARIANCE: a tiny data change reshuffles the whole tree → almost never used alone\n  → Random Forest (bag many trees + random feature subsets) or Boosting (fit residuals) fix this</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>One tree is a single opinionated interviewer; a forest is a hiring panel.</b> Any one interviewer is noisy; averaging an independent panel cancels the noise.</p></div>',
    try: [['📖 sklearn: decision trees', 'https://scikit-learn.org/stable/modules/tree.html', 'o']] },
  { html:
    '<p><b>SVM</b> and the <b>kernel trick</b>:</p>' +
    '<pre><code>maximise the margin (distance from the boundary to the nearest points = "support vectors").\nsoft margin: C controls the tradeoff — large C = few misclassifications, narrow margin (overfit);\n            small C = wider margin, more slack (underfit).\nKERNEL TRICK: replace dot products x·x\' with K(x, x\') = an implicit dot product in a\n            higher-dimensional space → a curved boundary WITHOUT ever computing the coordinates.\n  linear · polynomial · RBF/Gaussian (most common, one param γ: reach of each point)\ncost: O(n^2)-O(n^3) training → great for small/medium n, poor for millions of rows.\nSCALE FEATURES (distance-based).</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>Text classification on 5k labelled docs with TF-IDF: a linear SVM (LinearSVC) is a classic strong baseline — high-dimensional sparse features, small n, and margins generalise well.</p></div>',
    try: [['📖 sklearn: SVM + kernels', 'https://scikit-learn.org/stable/modules/svm.html', 'o']] },
  { html:
    '<p><b>kNN</b> and <b>Naive Bayes</b> — cheap, and their failure modes:</p>' +
    '<pre><code>kNN:  no training; all cost at inference (O(n·d) per query without an index → use KD-tree/Ball-tree/ANN)\n  k small → noisy/overfit; k large → oversmoothed. Use odd k for binary. WEIGHT by distance.\n  CURSE OF DIMENSIONALITY: in high-d, all points are ~equidistant → "nearest" is meaningless.\n  Must scale features; sensitive to irrelevant features.\n\nNaive Bayes:  P(y|x) ∝ P(y) Π P(x_i|y).  The independence assumption is almost always FALSE,\n  yet it works well for text (bag-of-words) because it only needs the RANKING of classes to be right.\n  Laplace/add-one smoothing so an unseen word does not zero out the whole product.\n  Very fast, tiny data footprint, strong baseline; poorly calibrated probabilities.</code></pre>',
    try: [['📖 sklearn: Naive Bayes', 'https://scikit-learn.org/stable/modules/naive_bayes.html', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: How does a decision tree decide where to split?\nA: It scores every candidate (feature, threshold) by the impurity reduction (Gini or entropy /\n   information gain; variance reduction for regression) and greedily takes the best, recursing until\n   a stopping rule.\n\nQ: Why is a single tree rarely used, and what fixes it?\nA: High variance — small data perturbations change the whole structure. Bagging (Random Forest:\n   bootstrap samples + random feature subsets, then average) reduces variance; boosting reduces bias.\n\nQ: What is the kernel trick and why is it useful?\nA: Kernels compute inner products in a high-dimensional feature space implicitly (K(x,x\')), letting\n   a linear algorithm (SVM) learn a non-linear boundary without ever materialising those features.\n\nQ: What do C and gamma control in an RBF SVM?\nA: C = penalty for margin violations (high C → low bias, high variance). gamma = how far a single\n   training point\'s influence reaches (high gamma → wiggly, overfit boundary).\n\nQ: Why does kNN fail in high dimensions?\nA: Curse of dimensionality: distances concentrate, so every point is roughly equally far and\n   "nearest neighbour" carries little signal. Also every extra irrelevant feature adds noise to the\n   distance.\n\nQ: Naive Bayes assumes feature independence, which is false for text. Why does it still work?\nA: For classification you only need the correct class to have the highest posterior; the crude\n   independence assumption biases the probabilities but often preserves the argmax, especially with\n   many weakly-informative features.\n\nQ: Which of these need feature scaling?\nA: SVM and kNN (distance/margin based) — yes. Decision trees / RF / GBM — no (split points are\n   scale-invariant). Naive Bayes — depends on the variant (GaussianNB is somewhat scale-sensitive).</code></pre>',
    try: [['📖 CS229: SVM notes', 'https://cs229.stanford.edu/notes2022fall/main_notes.pdf', 'o']] } ],
  quiz: [
    { q: 'A single decision tree gets 99% train accuracy and 71% test accuracy. Best remedy?', opts: ['Increase max_depth', 'Bag many trees on bootstrap samples with random feature subsets (Random Forest), or limit depth / min_samples_leaf', 'Switch to kNN with k=1', 'Remove feature scaling'], ok: 1, why: 'That gap is the classic high-variance failure of an unpruned tree. Averaging many de-correlated trees (Random Forest) or constraining the tree (depth, leaf size) reduces variance.' },
    { q: 'In an RBF-kernel SVM, increasing gamma tends to...', opts: ['Widen the margin and underfit', 'Make each training point\'s influence local, producing a wigglier boundary that can overfit', 'Have no effect', 'Force a linear boundary'], ok: 1, why: 'gamma sets the radius of influence of each support vector. Large gamma means very local influence, so the boundary contorts around individual points — high variance. Small gamma gives smoother, more global boundaries.' },
    { q: 'Which pair of models does NOT require feature scaling?', opts: ['SVM and kNN', 'Decision trees and gradient-boosted trees', 'Logistic regression with L2 and kNN', 'k-means and SVM'], ok: 1, why: 'Tree-based models split on thresholds per feature, so monotonic rescaling does not change the splits. Distance- and margin-based methods (kNN, SVM, k-means) and regularised linear models are scale-sensitive.' } ] },

/* ============================ 5. CLUSTERING & DIMENSIONALITY REDUCTION ============================ */
{ num: 5, emoji: '🧩', title: 'Clustering & Dimensionality Reduction', layer: 'Theory',
  tagline: 'Finding structure with no labels: k-means, DBSCAN, GMM, PCA, and why t-SNE plots can lie.',
  apps: ['customer segmentation', 'anomaly detection', 'visualisation & feature compression'],
  levels: [
  { html:
    '<p><b>Clustering</b> groups points by similarity; <b>dimensionality reduction</b> compresses many features into a few while keeping structure.</p>' +
    '<pre><code>k-means      partition into k blobs; each point → nearest centroid; minimise within-cluster variance\nDBSCAN       density-based: dense regions = clusters, sparse points = noise; finds arbitrary shapes, k not needed\nHierarchical build a tree of merges (dendrogram); cut it at any level\nGMM          soft clusters: each point has a probability of belonging to each Gaussian (fit by EM)\n\nPCA          new orthogonal axes ordered by variance explained; keep the top few (linear)\nt-SNE / UMAP  non-linear; preserve LOCAL neighbourhoods for a 2-D picture (for viewing, not features)</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>k-means sorts marbles into a fixed number of bowls by nearest bowl. DBSCAN follows the crowd — wherever marbles are packed tight is a group, loners are ignored. PCA is the best camera angle that shows the most spread of the marbles in one photo.</b></p></div>',
    try: [['📘 Part 1: embeddings & geometry of meaning', '../learn/#ch5', 'o']] },
  { html:
    '<p><b>k-means</b> — the algorithm and its assumptions:</p>' +
    '<pre><code>Lloyd\'s algorithm:  1) pick k centroids  2) assign each point to nearest  3) recompute centroids\n                    4) repeat until stable.  Converges to a LOCAL optimum → run several inits.\nk-means++:  spread the initial centroids out → far better, faster convergence (the sklearn default)\nchoosing k:  elbow of inertia vs k · silhouette score · gap statistic · a downstream metric\n\nASSUMES: spherical, similar-size, similar-density clusters; sensitive to scale and outliers;\n         forces every point into a cluster. Fails on elongated / nested / uneven clusters.\nSCALE FEATURES FIRST.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>RFM customer segmentation: standardise Recency/Frequency/Monetary, k-means++ with k chosen by silhouette, then <i>profile</i> each cluster ("lapsed big spenders", "new frequent") — the business value is the interpretation, not the algorithm.</p></div>',
    try: [['📖 sklearn: clustering comparison', 'https://scikit-learn.org/stable/modules/clustering.html', 'o']] },
  { html:
    '<p><b>DBSCAN</b>, <b>GMM</b>, and <b>PCA</b> mechanics:</p>' +
    '<pre><code>DBSCAN(eps, min_samples): core point = ≥ min_samples neighbours within eps; clusters grow from\n  core points; unreachable points = noise. Finds arbitrary shapes, auto #clusters, labels outliers.\n  Struggles with varying density and picking eps (use a k-distance plot).\n\nGMM: k Gaussians with means/covariances/weights, fit by Expectation-Maximisation\n  (E: responsibilities, M: refit params). Gives soft assignments + a likelihood → can pick k via BIC.\n\nPCA: centre X, compute covariance (or SVD of X), eigenvectors = principal components,\n  eigenvalues = variance along each. Keep components covering e.g. 95% variance.\n  Uses: denoise, speed up / decorrelate features, visualise. Components are usually not interpretable.\n  Needs SCALING; assumes variance = signal (not always true).</code></pre>',
    try: [['📖 A tutorial on PCA (Shlens)', 'https://arxiv.org/abs/1404.1100', 'o']] },
  { html:
    '<p><b>t-SNE / UMAP</b> — powerful and easy to misread:</p>' +
    '<pre><code>• cluster SIZES in a t-SNE plot are meaningless (it equalises densities)\n• DISTANCES BETWEEN clusters are mostly meaningless — do not read "these two groups are close"\n• the "perplexity" (t-SNE) / n_neighbors (UMAP) knob changes the picture a lot → try several\n• it can INVENT clusters from noise; always cross-check against a real metric or PCA\n• non-deterministic (t-SNE); slow on large n → subsample or use UMAP\n• NEVER feed t-SNE coordinates as model features — it does not define a stable mapping for new points\n  (UMAP can transform new points, but still: use it for EDA, not production features)</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A subway map.</b> Great for "which stops connect to which" (local structure); useless for actual distances or how big a neighbourhood is. Read t-SNE the same way.</p></div>',
    try: [['📖 How to Use t-SNE Effectively (Distill)', 'https://distill.pub/2016/misread-tsne/', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: How do you choose k for k-means?\nA: There is no single answer: the elbow of inertia, silhouette score, the gap statistic, or —\n   best — a downstream business/model metric. Also sanity-check that clusters are interpretable\n   and stable across seeds.\n\nQ: When does k-means fail, and what do you use instead?\nA: Non-spherical, unequal-size, or unequal-density clusters; outliers; unknown k. Alternatives:\n   DBSCAN (arbitrary shapes + noise), GMM (elliptical, soft), hierarchical (nested structure),\n   spectral clustering (manifolds).\n\nQ: k-means vs GMM?\nA: k-means is hard assignment to the nearest centroid (a GMM special case with equal spherical\n   covariances and hard responsibilities). GMM gives soft, probabilistic membership and models\n   elliptical clusters, at higher cost and risk of degenerate fits.\n\nQ: What does PCA actually compute and what are its assumptions?\nA: Orthogonal directions of maximum variance (eigenvectors of the covariance matrix / SVD of the\n   centred data), ordered by eigenvalue. Assumes the interesting structure is linear and aligned\n   with high variance; requires centring and usually scaling.\n\nQ: Two clusters look close together in a t-SNE plot. Can you conclude they are similar?\nA: No. t-SNE distorts global distances and cluster sizes; only local neighbourhoods are\n   trustworthy. Verify with PCA, actual distances, or a metric.\n\nQ: Can you use t-SNE output as features for a downstream classifier?\nA: No — t-SNE has no stable out-of-sample mapping and optimises a visualisation objective, not a\n   feature space. Use PCA (or an autoencoder / UMAP with care) for feature compression.</code></pre>',
    try: [['📖 UMAP docs (and caveats)', 'https://umap-learn.readthedocs.io/en/latest/', 'o']] } ],
  quiz: [
    { q: 'Your clusters are long, curved streaks and k-means keeps slicing them in half. Best alternative?', opts: ['Increase k', 'DBSCAN or spectral clustering, which handle arbitrary shapes; k-means assumes convex, roughly spherical clusters', 'PCA then k-means', 'Use k=2 always'], ok: 1, why: 'k-means minimises within-cluster variance around centroids, so it can only produce convex, blob-like partitions. Density-based (DBSCAN) or graph-based (spectral) clustering follow non-convex structure.' },
    { q: 'Which statement about a t-SNE scatter plot is TRUE?', opts: ['The area of a cluster reflects how many points it has', 'The gap between two clusters tells you how different they are', 'Only local neighbour relationships are reliable; global distances and cluster sizes are distorted', 'It gives a stable coordinate you can reuse as a model feature'], ok: 2, why: 't-SNE optimises preservation of local neighbourhoods and equalises densities, so between-cluster distances and cluster areas are not meaningful, and it provides no stable mapping for new points.' },
    { q: 'You run PCA and the first 2 components explain only 30% of variance. What does that imply?', opts: ['PCA failed and should not be used', 'The data\'s structure is high-dimensional / not concentrated in a few linear directions; a 2-D PCA view will lose most information', 'You must standardise and it will jump to 95%', 'The data has exactly 2 clusters'], ok: 1, why: 'Low cumulative explained variance in the top components means the variance is spread across many directions (or is non-linear). A 2-D projection is then a poor summary; consider more components or a non-linear method for visualisation.' } ] },

/* ============================ 6. FEATURE ENGINEERING ============================ */
{ num: 6, emoji: '🧱', title: 'Feature Engineering', layer: 'Applied Systems',
  tagline: 'Usually the highest-leverage work on a tabular problem — and the easiest place to leak the answer.',
  apps: ['every tabular model', 'fraud / risk / churn feature pipelines', 'feature stores for training/serving parity'],
  levels: [
  { html:
    '<p>The model can only learn from what you give it. <b>Feature engineering</b> turns raw columns into signals the model can use, and it routinely beats swapping model families.</p>' +
    '<pre><code>raw:        signup_ts, last_login_ts, plan, country, n_support_tickets, invoice_amounts[]\nengineered: days_since_signup, days_since_login, login_gap_trend, tickets_per_month,\n            avg_invoice, invoice_volatility, is_weekend_signup, plan x country interaction</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Prepping ingredients before you cook.</b> Handing a chef a whole unpeeled onion and a live chicken is technically "the data". Diced onion and portioned chicken is what actually makes the dish.</p></div>',
    try: [['📘 Part 3: pandas & sklearn preprocessing', '../learn3/#ch6', 'o']] },
  { html:
    '<p><b>Categoricals</b> — the choice matters:</p>' +
    '<pre><code>one-hot            low cardinality; explodes with high cardinality; fine for linear models\nordinal            only when there is a real order (S &lt; M &lt; L)\nfrequency / count   "how common is this value" — cheap, leak-safe, works with trees\ntarget / mean       E[y | category], SMOOTHED toward the global mean, computed with\n                   OUT-OF-FOLD encoding (KFold) or you WILL leak the label\nhashing            fixed width, no vocab, collisions — huge cardinality / streaming\nembeddings         learned dense vectors — high cardinality + neural nets / entity embeddings\n\nNUMERIC: scaling (ch.3), log/Box-Cox for skew, binning, clipping outliers, ratios & interactions.\nDATETIME: hour, dayofweek, month, is_holiday, and CYCLICAL encoding (sin/cos of hour) so 23:00 is\n          near 00:00.  TEXT/IMAGE: TF-IDF / embeddings / pretrained features.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Target encoding without out-of-fold is studying with the answer key.</b> The category "knows" its own rows\' labels, so training accuracy soars and production collapses.</p></div>',
    try: [['📖 category_encoders', 'https://contrib.scikit-learn.org/category_encoders/', 'o']] },
  { html:
    '<p><b>Feature selection</b> and building features safely at scale:</p>' +
    '<pre><code>FILTER   correlation / mutual information / variance threshold — fast, model-agnostic\nWRAPPER  recursive feature elimination, forward/backward — accurate, expensive\nEMBEDDED L1 / tree importances / SHAP — selection as a side effect of fitting\n\n• fit ALL feature transforms inside the CV fold / Pipeline (ch.3) — imputation values, scalers,\n  target encoders, PCA, feature selection: all leak if fit on the full data\n• point-in-time correctness: a feature\'s value must be what you would have KNOWN at prediction time\n• FEATURE STORE: one definition of each feature, materialised for batch training AND served online,\n  so training and serving compute the identical value (kills train/serve skew)</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>Fraud model uses "merchant_avg_fraud_rate". Built naively over the whole history it includes fraud that happened AFTER the transaction you are scoring → leakage. Fix: compute it only from data strictly before each transaction\'s timestamp.</p></div>',
    try: [['📖 Feast (feature store)', 'https://docs.feast.dev/', 'o']] },
  { html:
    '<p>The leakage catalogue — memorise this:</p>' +
    '<pre><code>1  preprocessing fit before the split (scaler/imputer/encoder/PCA/feature-selector)\n2  target leakage: a feature computed FROM the label or from post-outcome data\n3  time leakage: using future information (rolling stats not shifted, "next event" features)\n4  train/test contamination: duplicate or near-duplicate rows across splits; the same user/entity\n   in both (use GroupKFold)\n5  proxy leakage: an ID or timestamp that encodes the outcome (e.g. "case_id" ordered by verdict)\n6  metric leakage: tuning hyperparameters or the threshold on the test set\n7  giveaway columns present in training that will NOT exist at serving time\n</code></pre>' +
    '<p><b>Tell:</b> a single feature with suspiciously high importance, or a model that is "too good". Drop it and see if the score collapses.</p>',
    try: [['📖 Kaggle: data leakage', 'https://www.kaggle.com/code/alexisbcook/data-leakage', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: You have a categorical with 50,000 unique values. Options?\nA: Frequency/count encoding, hashing (fixed width, accept collisions), out-of-fold target encoding\n   with smoothing, or learned entity embeddings if using a neural net. Avoid raw one-hot.\n\nQ: How do you target-encode without leaking?\nA: Compute E[y|category] using only OTHER folds (KFold / leave-one-out) for each training row,\n   smooth toward the global mean by count, and fit the final encoder on all training data for\n   inference. Never let a row see its own label.\n\nQ: Encode "hour of day" so a model treats 23:00 and 00:00 as close.\nA: Cyclical: hour_sin = sin(2π·h/24), hour_cos = cos(2π·h/24). Now adjacent hours are adjacent in\n   feature space; a raw integer 0..23 puts them 23 apart.\n\nQ: Name five ways data leakage happens.\nA: Fitting preprocessing before the split; a feature derived from the target/outcome; future\n   information (time leakage); the same entity in train and test; tuning on the test set;\n   giveaway columns absent at serving.\n\nQ: A feature has 0.7 permutation importance and everything else is near 0. What do you check?\nA: Leakage. Is it derived from the label? Does it use post-event data? Is it available at serving\n   time and at the right point-in-time? Drop it and re-evaluate.\n\nQ: Why use a feature store?\nA: A single source-of-truth definition per feature, materialised for both batch training and\n   low-latency online serving, guaranteeing training/serving parity and point-in-time correctness,\n   with reuse across teams.</code></pre>',
    try: [['📖 Feature Engineering for ML (Zheng & Casari)', 'https://www.oreilly.com/library/view/feature-engineering-for/9781491953235/', 'o']] } ],
  quiz: [
    { q: 'You mean-encode a high-cardinality category by E[y|category] over the full training set, then cross-validate and get a great score that collapses in production. Why?', opts: ['The category has too many values', 'Target leakage — each row saw its own label through the encoding; use out-of-fold encoding with smoothing', 'You forgot to scale', 'The model is underfitting'], ok: 1, why: 'Computing the target mean over all training rows lets every row peek at its own label. The encoder must be computed out-of-fold (per row, from other folds) and smoothed, so it reflects only information available before the label.' },
    { q: 'Which encoding is most appropriate for a 100k-cardinality categorical feeding a neural network?', opts: ['One-hot', 'A learned entity embedding', 'Ordinal by alphabetical order', 'Drop the feature'], ok: 1, why: 'One-hot at 100k dimensions is wasteful and sparse; ordinal-by-alphabet invents a false order. A trainable embedding maps each category to a dense vector the network learns jointly with the task.' },
    { q: 'A fraud feature "merchant_historical_fraud_rate" is computed over the merchant\'s entire transaction history. What is the risk?', opts: ['It is too slow to compute', 'Time leakage — it can include fraud that occurred AFTER the transaction being scored; compute it only from data strictly before each transaction timestamp', 'Merchants change names', 'It needs one-hot encoding'], ok: 1, why: 'Point-in-time correctness: any aggregate feature must use only information available at prediction time. Including later fraud events leaks the outcome and inflates offline metrics.' } ] },

/* ============================ 7. DEEP LEARNING ARCHITECTURES ============================ */
{ num: 7, emoji: '🏗️', title: 'Deep Learning Architectures', layer: 'DL',
  tagline: 'MLP → CNN → RNN/LSTM → Transformer: what each inductive bias buys you, and why Transformers won.',
  apps: ['DL fundamentals whiteboard', 'model-choice for vision/text/sequence', 'reading a paper\'s architecture section'],
  levels: [
  { html:
    '<p>An architecture is a set of <b>inductive biases</b> — assumptions baked into the wiring that make some patterns easy to learn.</p>' +
    '<pre><code>MLP          "any input position can relate to any other"; no structure assumed; parameter-heavy\nCNN          "nearby pixels matter together, and a pattern is the same anywhere in the image"\nRNN/LSTM     "process a sequence step by step, carrying a memory"\nTransformer  "let every token look directly at every other token, weighted by relevance"</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Tools with a built-in shape.</b> A CNN is a rubber stamp you slide across the page. An RNN reads left to right with a notepad. A Transformer lays every word on the table and draws arrows between the ones that matter.</p></div>',
    try: [['📘 Part 1: build attention & a mini-transformer from scratch', '../learn/#ch4', 'o']] },
  { html:
    '<p><b>CNNs</b>:</p>' +
    '<pre><code>convolution:  a small filter (e.g. 3x3) slides over the image; same weights everywhere\n  → translation equivariance + weight sharing → far fewer params than a Dense layer\npooling / strided conv: downsample → larger RECEPTIVE FIELD, some translation invariance\nstack conv→norm→activation blocks: edges → textures → parts → objects (a feature hierarchy)\n\nResNet skip connection:  out = F(x) + x   → gradients flow straight through → trains 100+ layers\n  (solved the "deeper networks got WORSE" degradation problem, not just vanishing gradients)\nmodern: BatchNorm, global average pooling instead of huge FC heads, depthwise-separable convs\n  (MobileNet), and Vision Transformers now compete with / beat CNNs given enough data.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A skip connection is a bypass road.</b> If the new neighbourhood (the extra layers) is congested or useless, traffic (the signal and its gradient) can still get through on the bypass, so adding it never hurts.</p></div>',
    try: [['📖 CS231n: CNNs for Visual Recognition', 'https://cs231n.github.io/', 'o']] },
  { html:
    '<p><b>RNN / LSTM / GRU</b> and why they lost ground:</p>' +
    '<pre><code>vanilla RNN:  h_t = tanh(W·[h_{t-1}, x_t]).  Backprop through time multiplies many Jacobians\n  → gradients VANISH (can\'t learn long dependencies) or EXPLODE (clip them).\nLSTM:  adds a CELL STATE with additive updates + input/forget/output GATES → a gradient highway\n  for long-range memory. GRU = a lighter 2-gate version, similar performance.\nlimits:  sequential → cannot parallelise over time (slow training); still struggles past a few\n  hundred steps; the final hidden state is an information bottleneck.\n\nTransformers replaced them because self-attention is (a) parallel over the sequence and\n(b) gives every position direct O(1)-path access to every other — no compression through a\nsingle hidden vector.</code></pre>',
    try: [['📖 Understanding LSTM Networks (Olah)', 'https://colah.github.io/posts/2015-08-Understanding-LSTMs/', 'o']] },
  { html:
    '<p><b>Transformers</b> — the three variants and their jobs:</p>' +
    '<pre><code>block = [ multi-head self-attention ] + [ position-wise MLP ], each wrapped in residual + LayerNorm\nself-attention:  softmax(QKᵀ / √d) V   — Q,K,V are linear projections of the input (Part 1 ch.4)\npositional info:  sinusoidal / learned / rotary (RoPE) — attention itself is order-blind\n\nENCODER-only (BERT):    bidirectional; masked-LM pretraining; for classification / embeddings / NER\nDECODER-only (GPT):     causal mask; next-token pretraining; for generation, and now general LLMs\nENCODER-DECODER (T5):   encode a source, cross-attend while decoding; translation, summarisation\n\ncost: attention is O(n^2) in sequence length (Part 2 ch.13/ch.8 cover FlashAttention, KV-cache, GQA).</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>Ticket classification: an encoder-only model (DeBERTa-v3-small) fine-tuned. Chatbot answers: a decoder-only LLM. Doc-to-summary: an encoder-decoder (or a decoder-only LLM with the doc in context).</p></div>',
    try: [['📖 The Illustrated Transformer (Alammar)', 'https://jalammar.github.io/illustrated-transformer/', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Why a CNN instead of a fully-connected net for images?\nA: Local receptive fields + weight sharing → orders of magnitude fewer parameters, translation\n   equivariance, and a hierarchical feature composition that matches how images are structured.\n\nQ: What problem do ResNet skip connections solve?\nA: The degradation problem — plain deep nets trained WORSE than shallow ones. The identity shortcut\n   makes it trivial for a block to represent identity, so extra depth cannot hurt, and it gives\n   gradients a direct path (helps but is not solely "vanishing gradients").\n\nQ: Why do vanilla RNNs fail on long sequences, and how does an LSTM help?\nA: Repeated multiplication of Jacobians through time drives gradients to 0 or ∞. The LSTM cell\n   state is updated ADDITIVELY and gated, creating a near-constant-error carousel that preserves\n   long-range gradient flow.\n\nQ: Why did Transformers replace RNNs for sequence modelling?\nA: Self-attention is parallelisable across the whole sequence (fast training) and gives every\n   token a direct connection to every other, avoiding the single-hidden-state bottleneck. Cost is\n   O(n^2) attention, which spawned FlashAttention / sparse / linear variants.\n\nQ: Encoder-only vs decoder-only vs encoder-decoder — when each?\nA: Encoder-only (BERT): understanding tasks — classification, NER, embeddings (bidirectional).\n   Decoder-only (GPT): generation and general LLMs (causal). Encoder-decoder (T5/BART):\n   sequence-to-sequence — translation, summarisation.\n\nQ: Why is positional encoding needed?\nA: Self-attention is permutation-invariant; without position signals "dog bites man" and\n   "man bites dog" are identical. Sinusoidal, learned, or rotary encodings inject order.\n\nQ: BatchNorm vs LayerNorm — where is each used?\nA: BatchNorm normalises across the batch per channel — standard in CNNs, but batch-size sensitive\n   and awkward for sequences. LayerNorm normalises across features per example — used in\n   Transformers and RNNs, batch-size independent.</code></pre>',
    try: [['📖 Attention Is All You Need', 'https://arxiv.org/abs/1706.03762', 'o']] } ],
  quiz: [
    { q: 'A plain 56-layer CNN gets higher training error than a 20-layer one. What does the ResNet paper attribute this to, and what fixes it?', opts: ['Overfitting; add dropout', 'The degradation problem (deep plain nets are hard to optimise); identity skip connections let extra layers learn residuals and default to identity', 'Vanishing gradients only; use ReLU', 'Too small a learning rate'], ok: 1, why: 'ResNet showed deeper plain nets had worse TRAINING error — an optimisation/degradation issue, not just overfitting or vanishing gradients. Skip connections make identity easy to represent, so depth cannot degrade performance.' },
    { q: 'Which is the primary reason Transformers train faster than LSTMs on long sequences?', opts: ['They have fewer parameters', 'Self-attention processes all sequence positions in parallel, whereas an RNN must step through them one at a time', 'They do not use backprop', 'They skip positional information'], ok: 1, why: 'RNNs are inherently sequential in time. Self-attention computes all pairwise interactions in parallel matrix operations, which maps well to GPUs, even though it costs O(n^2) memory/compute.' },
    { q: 'You need sentence embeddings for a semantic-search index. Which Transformer variant?', opts: ['Decoder-only (GPT-style) with a causal mask', 'Encoder-only (BERT-style), which is bidirectional and designed for representation tasks', 'Encoder-decoder for translation', 'None — use an RNN'], ok: 1, why: 'Encoder-only models see the whole sentence bidirectionally and are trained/tuned for representations (classification, NER, embeddings). Decoder-only models are optimised for generation, though instruction-tuned LLMs can also produce usable embeddings.' } ] },

/* ============================ 8. TRAINING DEEP NETS ============================ */
{ num: 8, emoji: '🎚️', title: 'Training Deep Nets — Optimization & Regularization', layer: 'DL',
  tagline: 'Optimizers, LR schedules, init, normalization, and the "my loss is not going down" checklist.',
  apps: ['DL training-debugging round', 'reproducing a paper', 'making a model actually converge'],
  levels: [
  { html:
    '<p>Training = repeatedly nudging weights down the loss gradient. The <b>optimizer</b> decides how to turn gradients into updates.</p>' +
    '<pre><code>SGD               w -= lr · g                                 simple, strong generaliser, needs tuning + schedule\nSGD + momentum    v = βv + g ;  w -= lr · v                    smooths noisy gradients, escapes small ruts\nAdam              per-parameter adaptive lr from 1st & 2nd moment estimates   fast, forgiving, default\nAdamW             Adam with DECOUPLED weight decay (not L2 in the gradient)   the modern default for Transformers</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>SGD is walking downhill in fog one careful step at a time. Momentum is a heavy ball that keeps rolling through flat spots. Adam is that ball with independent suspension on each wheel, adjusting to the terrain under each one.</b></p></div>',
    try: [['📘 Part 1: SGD training loop from scratch', '../learn/#ch1', 'o']] },
  { html:
    '<p>The knobs that decide whether it converges:</p>' +
    '<pre><code>LEARNING RATE   the single most important hyperparameter. Too high → loss NaNs / oscillates;\n               too low → crawls. Find it with an LR-range test.\nLR SCHEDULE     warmup (ramp up over the first few hundred steps — vital for Transformers) then\n               decay (cosine / step / linear). "1cycle" is a strong default.\nBATCH SIZE      larger → lower-variance gradients, can raise LR (roughly linearly), less regularising\nINIT           Xavier/Glorot (tanh/sigmoid), He/Kaiming (ReLU) — keeps activation variance stable\n               across layers so signals neither vanish nor blow up at step 0\nGRAD CLIPPING   cap the global grad norm (e.g. 1.0) — essential for RNNs / Transformers</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>Fine-tuning a Transformer: AdamW, lr ~2e-5, linear warmup for ~6% of steps then linear decay, grad-norm clip 1.0, weight decay 0.01, bf16. Deviate from this and you usually get a worse or diverging run.</p></div>',
    try: [['📖 A Recipe for Training Neural Networks (Karpathy)', 'https://karpathy.github.io/2019/04/25/recipe/', 'o']] },
  { html:
    '<p><b>Regularisation</b> for deep nets, and <b>normalisation</b>:</p>' +
    '<pre><code>dropout          randomly zero activations during training → prevents co-adaptation; off at inference\nweight decay     penalise large weights (AdamW does it right)\nearly stopping   stop when val loss stops improving\ndata augmentation the strongest regulariser for vision/audio; also mixup / cutmix / label smoothing\n\nBatchNorm  normalise each channel over the batch → faster training, higher LR, mild regularisation;\n           breaks with tiny batches; train vs eval statistics differ (a classic bug)\nLayerNorm  normalise over features per sample → used in Transformers/RNNs; batch-size independent\nRMSNorm / GroupNorm  cheaper / batch-free variants used in modern LLMs and detection</code></pre>',
    try: [['📖 BatchNorm paper', 'https://arxiv.org/abs/1502.03167', 'o']] },
  { html:
    '<p>The "loss is not decreasing / is NaN" checklist — say this in order:</p>' +
    '<pre><code>1  can the model OVERFIT a tiny batch (2-5 examples) to ~0 loss? If not → a bug, not a tuning issue\n2  input scaling: are inputs normalised? any NaNs/Infs in the data?\n3  learning rate: try 10x lower (NaN/oscillation) or 10x higher (flat)\n4  loss function: right one for the task? logits vs probabilities? label range/shape correct?\n5  labels: shuffled correctly? off-by-one? all one class?\n6  zero_grad each step; model.train()/eval() correct; no accidental torch.no_grad in training\n7  gradient norm: exploding → clip; ~0 everywhere → dead ReLUs / bad init / detached graph\n8  numerical: log(0), divide-by-0, softmax overflow (subtract max), fp16 overflow → bf16 / loss scaling\n9  data leak the other way: is val loss suspiciously LOWER than train? augmentation on val?</code></pre>',
    try: [['📖 Troubleshooting Deep Neural Networks (Josh Tobin)', 'http://josh-tobin.com/troubleshooting-deep-neural-networks.html', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Adam vs SGD — tradeoffs?\nA: Adam converges faster and is robust to LR choice (per-parameter adaptive steps), great for\n   Transformers/sparse gradients. Well-tuned SGD+momentum often GENERALISES better on vision and\n   is the research standard for final results. AdamW (decoupled weight decay) is the modern default.\n\nQ: Why does a Transformer need learning-rate warmup?\nA: Early in training the adaptive moment estimates (and LayerNorm statistics) are unreliable; a\n   full LR causes huge, destabilising updates. Ramping up over the first few hundred steps lets\n   the optimiser state settle.\n\nQ: He vs Xavier initialisation?\nA: Xavier/Glorot keeps activation variance stable for symmetric activations (tanh). He/Kaiming\n   accounts for ReLU zeroing half the activations, using variance 2/fan_in. Wrong init → vanishing\n   or exploding activations from step 0.\n\nQ: BatchNorm behaves oddly with batch size 2. Why, and the fix?\nA: Batch statistics are noisy with tiny batches, hurting training and causing train/eval mismatch.\n   Use GroupNorm / LayerNorm, or accumulate statistics, or increase the effective batch.\n\nQ: Loss is NaN after a few steps. First three things you check?\nA: Learning rate too high; unnormalised or corrupt inputs; a numerically unstable loss (log(0),\n   softmax overflow) or fp16 overflow. Also check for exploding gradients (add clipping).\n\nQ: Your model cannot even overfit 4 examples. What does that tell you?\nA: There is a bug — wrong loss, broken data pipeline, labels not connected to inputs, frozen\n   parameters, or a shape/broadcast error. Fix that before touching hyperparameters.\n\nQ: What regularises a neural net besides an explicit penalty?\nA: Dropout, early stopping, data augmentation, smaller batch size, label smoothing, weight\n   sharing, and injecting noise.</code></pre>',
    try: [['📖 Deep Learning Tuning Playbook (Google)', 'https://github.com/google-research/tuning_playbook', 'o']] } ],
  quiz: [
    { q: 'Your Transformer fine-tune diverges (loss to NaN) in the first 50 steps with lr=2e-3. Most likely fix?', opts: ['Train for more epochs', 'Lower the LR (e.g. 2e-5) and add linear warmup; also clip gradient norm', 'Increase the model size', 'Remove LayerNorm'], ok: 1, why: 'Transformers are highly LR-sensitive and need warmup while optimiser/normalisation statistics stabilise. 2e-3 is ~100x too high for fine-tuning; 1e-5 to 5e-5 with warmup and grad clipping is standard.' },
    { q: 'Before tuning hyperparameters, what is the single most useful sanity check for a training pipeline?', opts: ['Run on the full dataset for one epoch', 'Verify the model can overfit a handful of examples to near-zero loss', 'Switch optimisers', 'Add more dropout'], ok: 1, why: 'If the model cannot drive loss to ~0 on 2-5 examples, there is a bug (loss, data wiring, labels, frozen weights, shapes). Tuning is pointless until the pipeline can memorise a tiny set.' },
    { q: 'Which normalisation is standard inside Transformer blocks, and why not BatchNorm?', opts: ['BatchNorm, because it is faster', 'LayerNorm — it normalises across features per token, is independent of batch size, and works for variable-length sequences where batch statistics are ill-defined', 'No normalisation is used', 'GroupNorm with 1 group'], ok: 1, why: 'BatchNorm depends on batch composition and is awkward for sequences and small/variable batches. LayerNorm normalises each token\'s feature vector independently, giving stable, batch-size-agnostic training.' } ] },

/* ============================ 9. RECOMMENDER SYSTEMS ============================ */
{ num: 9, emoji: '🎬', title: 'Recommender Systems', layer: 'Applied Systems',
  tagline: 'The retrieval-then-ranking pattern behind every feed, "for you" row, and "customers also bought".',
  apps: ['"design YouTube / Spotify recommendations"', 'e-commerce personalisation', 'feed ranking'],
  levels: [
  { html:
    '<p>The task: from millions of items, pick the ~20 a given user will engage with next. Two families:</p>' +
    '<pre><code>COLLABORATIVE FILTERING  "users like you liked X" — uses the user-item interaction matrix only\nCONTENT-BASED            "you liked X, here is something with similar attributes" — uses item features\nHYBRID / two-tower       learn a user vector and an item vector; recommend by dot-product similarity</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A great librarian.</b> Collaborative: "readers with your taste also borrowed this". Content-based: "you liked this thriller, here is another by a similar author". The best librarian uses both.</p></div>',
    try: [['📘 Part 1: embeddings — meaning as geometry', '../learn/#ch5', 'o']] },
  { html:
    '<p><b>Matrix factorisation</b> — the CF workhorse:</p>' +
    '<pre><code>R (users x items, mostly missing)  ≈  U (users x k)  ·  Vᵀ (k x items)\nlearn U, V so U_u · V_i ≈ observed ratings; predict the blanks with the same dot product.\n\nEXPLICIT feedback (star ratings): minimise (r_ui - U_u·V_i)^2 + λ(||U||^2 + ||V||^2)\nIMPLICIT feedback (clicks/plays): there are no negatives, only "seen" vs "not seen".\n  → weight observed interactions by confidence (ALS for implicit), or use BPR\n    (Bayesian Personalised Ranking: score(seen) &gt; score(random unseen)).\nadd bias terms (global, per-user, per-item) — they explain a lot of variance cheaply.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Everyone and every movie gets a set of hidden "taste dials" (k of them).</b> Your enjoyment ≈ how well your dials line up with the movie\'s dials. Factorisation is discovering those dials from the ratings alone.</p></div>',
    try: [['📖 Matrix Factorization Techniques for Recommender Systems (Koren)', 'https://ieeexplore.ieee.org/document/5197422', 'o']] },
  { html:
    '<p>The production shape — <b>candidate generation + ranking</b>:</p>' +
    '<pre><code>1 RETRIEVAL / candidate generation:  millions → ~hundreds, in a few ms\n   two-tower model: user_tower(context) and item_tower(item) → ANN search (Part 2 ch.5) over item vectors\n   + other sources: recently-popular, from followed creators, similar-to-last-watched\n2 RANKING:  ~hundreds → ordered ~20, using a heavier model (GBM or a deep net) with rich features:\n   user history, item stats, context (time, device), user x item cross features; predict p(click),\n   p(watch time), p(like) and combine into one score\n3 RE-RANKING / policy:  diversity (do not show 10 near-duplicates), freshness, business rules,\n   exploration (show some uncertain items to learn)\n\nmetrics: OFFLINE recall@k / NDCG / MAP on held-out interactions; ONLINE CTR, watch time,\n         retention, and GUARDRAILS (diversity, creator fairness, reports).</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>"Design the YouTube home feed": name the two stages, the candidate sources, the ranking objective (a blend of predicted watch time + satisfaction, not raw clicks — clickbait guardrail), and the online experiment. Cold start: fall back to popularity + onboarding interests, use content features until interaction data accrues.</p></div>',
    try: [['📖 Deep Neural Networks for YouTube Recommendations', 'https://research.google/pubs/deep-neural-networks-for-youtube-recommendations/', 'o']] },
  { html:
    '<p>The hard problems:</p>' +
    '<pre><code>COLD START     new user (no history) → popularity, onboarding prefs, content features, contextual bandits\n               new item → content features, forced exploration, creator priors\nFEEDBACK LOOP  you only get labels on what you SHOWED → the model reinforces its own past choices;\n               popularity bias, filter bubbles. Fix: log propensities, inverse-propensity weighting,\n               explicit exploration (epsilon-greedy / Thompson), off-policy evaluation.\nPOSITION BIAS  higher slots get more clicks regardless of quality → model position, or debias labels\nSCALE          billions of (user,item) pairs → embeddings + ANN retrieval, not a full score\nEVAL GAP       offline NDCG up but online metrics flat/down → the offline set is itself biased by the\n               old policy; trust the A/B test.</code></pre>',
    try: [['📖 Recommenders (Microsoft) — best practices', 'https://github.com/recommenders-team/recommenders', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Design recommendations for a video platform. Structure your answer.\nA: Metric (blend of predicted watch time + explicit satisfaction; guardrails: diversity, clickbait,\n   creator fairness) → data/labels (implicit, position-biased, delayed) → two-stage: two-tower\n   retrieval + ANN over item embeddings, then a heavy ranker with user/item/context/cross features\n   → re-rank for diversity + exploration → serve within a latency budget with fallbacks → A/B test,\n   monitor retention and guardrails.\n\nQ: Collaborative vs content-based filtering — pros/cons?\nA: CF captures taste patterns it was never told about but suffers cold start and popularity bias.\n   Content-based handles new items and is explainable but is limited to known attributes and tends\n   to over-specialise. Hybrids get both.\n\nQ: Explicit vs implicit feedback — how does training differ?\nA: Explicit (ratings): regression on observed values. Implicit (clicks/plays): only positives\n   exist; you must sample or weight negatives (ALS with confidence weights, or BPR ranking loss).\n\nQ: What is the feedback loop problem and how do you mitigate it?\nA: The system only observes outcomes for items it chose to show, so it reinforces its own biases\n   (popularity, filter bubbles). Mitigate with logged propensities + inverse-propensity weighting,\n   explicit exploration (epsilon-greedy / Thompson sampling), and off-policy evaluation.\n\nQ: Offline NDCG improved but the online A/B is flat. Why might that happen?\nA: The offline evaluation set was generated by the OLD policy, so it rewards mimicking it; position\n   and selection bias are baked in. Trust the randomised online experiment.\n\nQ: How do you handle a brand-new user?\nA: Popularity / trending, onboarding-declared interests, contextual signals (device, locale, time),\n   content-based similarity, and a bandit to learn preferences quickly with low regret.</code></pre>',
    try: [['📖 Two-tower / dual encoder retrieval', 'https://research.google/pubs/sampling-bias-corrected-neural-modeling-for-large-corpus-item-recommendations/', 'o']] } ],
  quiz: [
    { q: 'Why do large-scale recommenders split into a retrieval stage and a ranking stage?', opts: ['To use two teams', 'Scoring millions of items with a heavy model per request is infeasible in the latency budget; cheap retrieval narrows to hundreds, then a rich model ranks those', 'Retrieval is more accurate than ranking', 'Ranking does not use features'], ok: 1, why: 'A precise ranking model is too expensive to run over the full catalogue per request. A lightweight retrieval step (e.g. two-tower + ANN) reduces candidates to a few hundred, which the expensive ranker can then order within the latency budget.' },
    { q: 'With implicit feedback (plays/clicks only), what is the core modelling challenge?', opts: ['Too many explicit ratings', 'There are no explicit negatives — "not played" is not the same as "disliked" — so you must sample/weight negatives or use a ranking loss like BPR', 'The data is always balanced', 'Matrix factorisation cannot be used'], ok: 1, why: 'Implicit data is one-class: you see positives and unknowns. Approaches include confidence-weighted ALS (treat unobserved as weak negatives) or pairwise ranking objectives (BPR) that only require positive > sampled-negative.' },
    { q: 'Your recommender keeps surfacing already-popular items and rarely shows the long tail. Best mitigation?', opts: ['Increase the embedding dimension', 'Add explicit exploration (epsilon-greedy / Thompson sampling) and correct for logged exposure with inverse-propensity weighting', 'Train longer', 'Remove the ranking stage'], ok: 1, why: 'This is the feedback-loop / popularity-bias problem: the model only learns from what it showed. Deliberate exploration plus propensity-corrected training break the loop and give tail items a chance to accumulate signal.' } ] },

/* ============================ 10. SEARCH & LEARNING TO RANK ============================ */
{ num: 10, emoji: '🔎', title: 'Search & Learning to Rank', layer: 'Applied Systems',
  tagline: 'Ordering results so the best one is on top — pointwise, pairwise, listwise, and the biases that fool you.',
  apps: ['"design search ranking for a marketplace"', 'e-commerce / jobs / docs search', 'feed & notification ranking'],
  levels: [
  { html:
    '<p>Search = <b>retrieve</b> a candidate set (keyword + vector, Part 2 ch.5) then <b>rank</b> it. <b>Learning to Rank (LTR)</b> is training a model whose job is the ORDER, not a per-item score in isolation.</p>' +
    '<pre><code>query + user + context  →  retrieve ~1000 candidates  →  LTR model scores each  →  sort  →  top 10\nthe label is relevance: from human judgements (graded 0-4) or from CLICKS / conversions (noisy, biased)</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A talent-show judge, not a thermometer.</b> You do not need each act\'s absolute score — you need the finalists in the right order. LTR optimises the ordering directly.</p></div>',
    try: [['📘 Part 2: reranking (cross-encoders, RRF)', '../learn2/#ch7', 'o']] },
  { html:
    '<p>Three ways to frame LTR:</p>' +
    '<pre><code>POINTWISE   predict each item\'s relevance independently (regression/classification), then sort.\n            simple, but ignores that ranking is relative; optimises the wrong thing.\nPAIRWISE    for pairs (i, j) in the same query, learn score(i) &gt; score(j) when i is more relevant.\n            RankNet loss; LambdaRank/LambdaMART weight each pair by how much swapping it changes NDCG.\n            LambdaMART (GBM + lambda gradients) is the classic production winner.\nLISTWISE    optimise a loss over the WHOLE ordered list (ListNet, SoftRank, or directly approximate NDCG).\n            closest to the true objective, harder to optimise.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Pointwise grades each essay alone. Pairwise asks "is A better than B?" for many pairs. Listwise reads the whole shortlist and judges the ranking as a unit.</b></p></div>',
    try: [['📖 From RankNet to LambdaRank to LambdaMART (Burges)', 'https://www.microsoft.com/en-us/research/publication/from-ranknet-to-lambdarank-to-lambdamart-an-overview/', 'o']] },
  { html:
    '<p>Features and metrics:</p>' +
    '<pre><code>FEATURES  query-doc match (BM25, cosine of embeddings, exact-title match),\n          doc quality (freshness, ratings, CTR history, price), user features (history, locale),\n          query features (length, intent class), and cross features (user x category affinity).\n\nOFFLINE METRICS (need graded relevance + a held-out set):\n  Precision@k / Recall@k          did relevant items make the top k\n  MAP (Mean Average Precision)    binary relevance, position-aware\n  MRR (Mean Reciprocal Rank)      1 / rank of the first relevant result — good for "one right answer"\n  NDCG@k                          graded relevance, log-discounted by position; the standard\nONLINE METRICS  CTR, conversion, dwell time, "no result / reformulation" rate, revenue,\n                and GUARDRAILS (latency, diversity, seller fairness).</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>Marketplace search: LambdaMART on ~200 features, label = a blend of click + purchase weighted by position-debiased attention, offline NDCG@10 for iteration, online A/B on conversion with a latency guardrail. Retrieval stays BM25 + vector hybrid.</p></div>',
    try: [['📖 NDCG explained', 'https://en.wikipedia.org/wiki/Discounted_cumulative_gain', 'o']] },
  { html:
    '<p>The biases that make click-trained rankers lie:</p>' +
    '<pre><code>POSITION BIAS   users click top results because they are on TOP, not because they are best.\n  fix: model P(click) = P(examined | position) · P(relevant), estimate the examination curve\n       (RandPair / result randomisation / intervention), then train on debiased labels (IPS).\nPRESENTATION BIAS  bigger thumbnails, badges, ads → more clicks, unrelated to relevance.\nSELECTION BIAS   you only get labels for items you SHOWED (same as recommenders, ch.9).\nTRUST BIAS      users trust the top slot even when it is wrong.\nNOVELTY / repeat-query effects; seasonality.\n\nSo: OFFLINE metrics on click logs are optimistic and self-reinforcing. The A/B test on a real\noutcome (purchase, task success) is the source of truth.</code></pre>',
    try: [['📖 Unbiased Learning to Rank with Biased Feedback (Joachims)', 'https://arxiv.org/abs/1608.04468', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Pointwise vs pairwise vs listwise LTR?\nA: Pointwise scores items independently then sorts (simple, misaligned with ranking). Pairwise\n   learns relative order over pairs within a query (RankNet; LambdaMART weights pairs by NDCG\n   change — the production classic). Listwise optimises a loss over the entire ordering (closest\n   to NDCG, hardest to train).\n\nQ: Which offline metric for search, and why NDCG over precision@k?\nA: NDCG@k uses GRADED relevance and discounts by position (log), so a highly-relevant result at\n   rank 1 is rewarded more than at rank 5, and partial relevance counts. Precision@k is binary and\n   position-blind within the top k.\n\nQ: MRR vs MAP vs NDCG — when each?\nA: MRR when there is essentially one correct answer (find-a-fact, navigational). MAP for binary\n   relevance with several relevant docs. NDCG when relevance is graded.\n\nQ: You train a ranker on clicks. What goes wrong and how do you fix it?\nA: Position/presentation/selection bias — the model learns "was shown high", not "is relevant".\n   Fix: estimate the examination probability by position (randomisation experiments) and train\n   with inverse-propensity-weighted labels; validate with an online A/B on a downstream outcome.\n\nQ: Offline NDCG went up 3%, online conversion is flat. What now?\nA: The click log is generated by the current ranker, so offline gains can be self-fulfilling.\n   Check for train/serve feature skew, latency regressions, and whether the label proxy (clicks)\n   diverges from the real goal (purchases). Trust the experiment; consider a debiased offline\n   estimator.\n\nQ: How do you keep search fast enough?\nA: Cheap retrieval (BM25 + ANN) to ~1000, a mid reranker to ~100, the heavy LTR model on those;\n   cache popular queries; precompute doc features; enforce a latency budget with a fallback\n   ordering.</code></pre>',
    try: [['📖 TF-Ranking / LightGBM LambdaRank', 'https://lightgbm.readthedocs.io/en/latest/Parameters.html#objective', 'o']] } ],
  quiz: [
    { q: 'Why is NDCG@10 generally preferred over Precision@10 for evaluating search ranking?', opts: ['It is faster to compute', 'It uses graded relevance AND discounts by rank position, so a great result at position 1 counts more than at position 10', 'It ignores position', 'It requires no labels'], ok: 1, why: 'Precision@k treats all top-k hits equally and relevance as binary. NDCG rewards putting the most relevant items highest (logarithmic position discount) and supports multi-level relevance grades.' },
    { q: 'A ranker trained purely on historical clicks tends to reinforce which bias, and what is a principled fix?', opts: ['Label smoothing bias; use dropout', 'Position bias — users click top items regardless of relevance; fix by estimating examination probability per position and training with inverse-propensity-weighted labels', 'Class imbalance; use SMOTE', 'Overfitting; add trees'], ok: 1, why: 'Clicks conflate "was examined" (driven by position) with "was relevant". Unbiased LTR estimates P(examined | position) via randomisation and reweights labels so the model learns relevance, not layout.' },
    { q: 'Which metric best fits a navigational query where exactly one result is the right answer?', opts: ['NDCG@100', 'Mean Reciprocal Rank (MRR)', 'Recall@1000', 'MAP with graded relevance'], ok: 1, why: 'MRR = average of 1/(rank of the first relevant result). It directly rewards putting the single correct answer as high as possible, which matches navigational / find-a-fact intent.' } ] },

/* ============================ 11. A/B TESTING & EXPERIMENTATION ============================ */
{ num: 11, emoji: '🧫', title: 'A/B Testing & Experimentation', layer: 'Experimentation',
  tagline: 'Randomise, power the test, do not peek, reduce variance, and beware novelty and network effects.',
  apps: ['"how would you test this feature?"', 'launch decisions', 'model rollout via experiment'],
  levels: [
  { html:
    '<p>An A/B test randomly assigns units to <b>control</b> and <b>treatment</b> so the only systematic difference is the change. Randomisation is what turns a correlation into a causal estimate.</p>' +
    '<pre><code>1 hypothesis:   "reducing signup steps from 4 to 2 increases activation rate"\n2 unit:         user (or session, or cluster) — must match how the effect spreads\n3 metric:       PRIMARY (activation rate) + GUARDRAILS (revenue, latency, complaints)\n4 power / n:    given a baseline rate and a minimum effect worth detecting (MDE), how many units?\n5 run:          fixed horizon; do NOT stop early on a peek\n6 analyse:      difference in means, CI, p-value; decide against a pre-registered rule</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A clinical trial for your product.</b> Random assignment + a control group + a pre-set endpoint. Skip any of those and "it went up" could just be the weather.</p></div>',
    try: [['📘 Part 3: hypothesis testing & p-values', '../learn3/#ch4', 'o']] },
  { html:
    '<p><b>Power, sample size, and MDE</b> — the pre-launch maths:</p>' +
    '<pre><code>power = P(detect a real effect of size MDE) — target 80%\nα     = P(false positive) — target 5% (two-sided)\nfor comparing two proportions:  n per arm ≈ 16 · p(1-p) / MDE^2      (rule of thumb, α=.05, power=.8)\n  e.g. baseline p=0.10, want to detect a 1pp lift (MDE=0.01): n ≈ 16·0.09/0.0001 ≈ 14,400 per arm\n\nsmaller MDE → quadratically more users. Underpowered tests waste traffic and give noise you\ncannot interpret ("not significant" ≠ "no effect").</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>PM wants to ship if the test shows "any improvement". Push back: agree an MDE and power the test for it. If you only have traffic for a 3pp MDE, a real 1pp win will look like a null — and you will wrongly kill it.</p></div>',
    try: [['📖 Trustworthy Online Controlled Experiments (Kohavi et al.)', 'https://experimentguide.com/', 'o']] },
  { html:
    '<p>The traps and the fixes:</p>' +
    '<pre><code>PEEKING / early stopping     checking daily and stopping at the first p&lt;0.05 inflates the false-\n                            positive rate massively. Fix: fixed horizon, OR sequential tests\n                            (always-valid p-values / mSPRT), OR group-sequential boundaries.\nMULTIPLE METRICS / VARIANTS  more comparisons → more false positives. Correct (Bonferroni/FDR);\n                            pre-register one primary metric.\nNOVELTY & PRIMACY effects    users react to "new", not to "better" — effect fades over 1-2 weeks.\n                            Run long enough; segment new vs existing users.\nSRM (Sample Ratio Mismatch)  50/50 split arrives 48/52 → assignment or logging is broken → INVALIDATE.\nSIMPSON\'S PARADOX            aggregate flips when you segment → check key segments.\nRATIO METRICS (e.g. CTR = clicks/impressions)  units are not iid → use the DELTA METHOD or\n                            bootstrap for correct variance.\nHETEROGENEOUS effects        an average of +0 can hide +5 for one segment and -5 for another.</code></pre>',
    try: [['📖 Peeking / sequential testing (Optimizely stats engine)', 'https://www.optimizely.com/insights/blog/how-optimizelys-new-stats-engine-works/', 'o']] },
  { html:
    '<p><b>Variance reduction and harder designs:</b></p>' +
    '<pre><code>CUPED   use a pre-experiment covariate (e.g. the user\'s baseline metric) to subtract predictable\n        variance → same power with ~10-50% fewer users. metric* = metric - θ(X_pre - E[X_pre]).\nstratification / post-stratification, capping/winsorising outliers, trigger analysis\n        (only count users who could experience the change).\nNETWORK EFFECTS (social, marketplace, comms): user-level randomisation LEAKS (your treated friend\n        affects your control experience). Use CLUSTER randomisation (by community / region / market)\n        or a switchback design (toggle the whole system on/off over time).\nquasi-experiments when you cannot randomise: diff-in-diff, synthetic control, regression\n        discontinuity, geo experiments (see ch.12).</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>CUPED is noise-cancelling headphones.</b> It does not change the signal (the true effect); it removes the predictable background hum (users who were always going to be high/low), so you hear the effect sooner.</p></div>',
    try: [['📖 CUPED (Deng, Xu, Kohavi, Walker)', 'https://www.microsoft.com/en-us/research/publication/improving-the-sensitivity-of-online-controlled-experiments-by-utilizing-pre-experiment-data/', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: How do you determine sample size for an A/B test?\nA: From the baseline metric, the minimum detectable effect (MDE) the business cares about, the\n   significance level (α=0.05), and target power (80%). For proportions, n per arm ≈\n   16·p(1-p)/MDE^2. Smaller MDE costs quadratically more traffic.\n\nQ: Why is it wrong to stop a test as soon as p &lt; 0.05?\nA: Repeated looks each get their own chance at a false positive, so the actual Type I error can\n   be 20-40% instead of 5%. Use a fixed horizon, or a sequential method with always-valid p-values\n   / group-sequential boundaries.\n\nQ: Your 50/50 experiment reports 47/53 traffic. What do you do?\nA: Sample Ratio Mismatch — the randomisation or logging is broken (a redirect, a crash biased to\n   one arm, bot filtering). Do NOT interpret the results; fix the pipeline and rerun.\n\nQ: What is CUPED and why use it?\nA: A variance-reduction technique: regress out a pre-experiment covariate (usually the user\'s\n   own pre-period metric), which shrinks the metric\'s variance without biasing the effect,\n   buying 10-50% more sensitivity for free.\n\nQ: You are testing a feature in a social network. Why is user-level randomisation risky?\nA: Interference/network effects — a treated user changes the experience of their control friends,\n   so control is contaminated and the effect is under- or over-estimated. Randomise by cluster\n   (community/region) or use a switchback design.\n\nQ: The test is not significant. Can you conclude the feature has no effect?\nA: No. "Not significant" means you could not detect an effect at your power. Report the confidence\n   interval — if it is tight around 0, that is evidence of a small effect; if it is wide, the test\n   was underpowered and inconclusive.\n\nQ: Novelty effect — how do you detect and handle it?\nA: Plot the treatment effect over time; if it decays, novelty is likely. Run longer, analyse the\n   post-novelty window, and segment by new vs tenured users.</code></pre>',
    try: [['📖 A/B testing pitfalls (Georgi Georgiev)', 'https://www.analytics-toolkit.com/', 'o']] } ],
  quiz: [
    { q: 'Halving the minimum detectable effect (MDE) you want to catch changes the required sample size by roughly...', opts: ['Half', 'No change', '4x (sample size scales with 1/MDE^2)', '2x'], ok: 2, why: 'Required n is proportional to 1/MDE^2 for a fixed power and alpha. Detecting an effect half as large needs about four times the users per arm.' },
    { q: 'You check your experiment every day and ship when p first drops below 0.05. What is the consequence?', opts: ['Nothing — p-values are always valid', 'The real false-positive rate is far above 5% because each peek is another chance to cross the threshold by noise', 'The test becomes more powerful', 'It only affects the confidence interval'], ok: 1, why: 'Fixed-horizon p-values assume a single analysis. Repeated peeking with an early-stop rule inflates Type I error to 20-40%. Use a pre-set duration or a sequential/always-valid testing procedure.' },
    { q: 'For an experiment on a marketplace pricing change, why prefer cluster (e.g. by city) randomisation over user-level?', opts: ['It is easier to implement', 'Interference: a treated buyer/seller affects control participants in the same market, contaminating the control group; randomising whole markets isolates the effect', 'User-level randomisation is illegal', 'It needs fewer users'], ok: 1, why: 'Marketplaces and social products have spillovers between units. If treatment and control users interact in the same market, the SUTVA/no-interference assumption breaks. Cluster or switchback designs keep interference within an experimental unit.' } ] },

/* ============================ 12. CAUSAL INFERENCE ============================ */
{ num: 12, emoji: '➡️', title: 'Causal Inference for Data Science', layer: 'Experimentation',
  tagline: 'When you cannot run an A/B test: confounders, DAGs, diff-in-diff, IV, and uplift modelling.',
  apps: ['"does X cause Y?" from observational data', 'policy / pricing / marketing impact', 'targeting with uplift models'],
  levels: [
  { html:
    '<p>Prediction asks "given X, what is Y?". <b>Causation</b> asks "if I <i>change</i> X, how does Y change?" — a different, harder question.</p>' +
    '<pre><code>potential outcomes:  each unit has Y(1) if treated and Y(0) if not; we only ever see ONE.\ncausal effect:       ATE = E[Y(1) - Y(0)] over the population\nthe fundamental problem:  the counterfactual is missing → we must ESTIMATE it\nCONFOUNDER:  a variable that affects both the treatment AND the outcome → creates spurious\n             correlation ("ice-cream sales and drownings" — the confounder is hot weather)</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Umbrellas do not cause rain, even though they are strongly correlated with it.</b> Something upstream (the forecast, the clouds) drives both. Causal inference is the discipline of finding and blocking those upstream paths.</p></div>',
    try: [['📖 The Book of Why (Pearl) / Causal Inference: The Mixtape (free)', 'https://mixtape.scunning.com/', 'o']] },
  { html:
    '<p><b>RCT is the gold standard</b> because randomisation breaks every confounding path. When you cannot randomise, you need identification strategies and assumptions:</p>' +
    '<pre><code>DAG + backdoor criterion:  draw the causal graph; adjust for a set of variables that BLOCKS every\n  "backdoor" path from treatment to outcome, WITHOUT conditioning on a collider or a mediator.\n  (conditioning on a collider — a common effect — CREATES bias.)\nignorability / unconfoundedness:  given the adjusted covariates, treatment is "as good as random"\noverlap / positivity:  every covariate profile has some chance of both treatment and control</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A collider is a bouncer.</b> "Talented OR rich people get into the club." Inside the club, talent and wealth look negatively correlated — not because they are, but because you selected on their common effect (admission). Controlling for a collider manufactures a relationship.</p></div>',
    try: [['📖 DAGitty (draw & analyse DAGs)', 'http://dagitty.net/', 'o']] },
  { html:
    '<p>The observational toolkit:</p>' +
    '<pre><code>PROPENSITY SCORES     model P(treated | X); then match, weight (IPW), or stratify so treated and\n                     control have similar X. Only removes OBSERVED confounding.\nDIFF-IN-DIFF (DiD)    compare the CHANGE in the treated group vs the CHANGE in a control group\n                     around an intervention. Assumes PARALLEL TRENDS absent treatment.\nSYNTHETIC CONTROL     build a weighted combo of untreated units that tracks the treated unit pre-\n                     intervention; the post gap is the effect. (geo experiments, policy changes)\nINSTRUMENTAL VARIABLE (IV)  a variable that moves treatment but affects Y ONLY through treatment\n                     (relevance + exclusion). e.g. random encouragement, distance to a facility.\nREGRESSION DISCONTINUITY  a cutoff assigns treatment (score ≥ 60 → scholarship); compare units\n                     just above vs just below — locally randomised.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>You launched a loyalty programme to all users at once (no control). Use diff-in-diff against a comparable region that did not get it, after checking their pre-launch trends were parallel; or a synthetic control from a basket of regions.</p></div>',
    try: [['📖 Causal Inference for The Brave and True (free)', 'https://matheusfacure.github.io/python-causality-handbook/landing-page.html', 'o']] },
  { html:
    '<p><b>Uplift / CATE modelling</b> — who to treat, not whether treatment works on average:</p>' +
    '<pre><code>ATE  = average effect.  CATE(x) = E[Y(1) - Y(0) | X=x]  = the effect FOR people like x.\nuplift model predicts CATE → target the "persuadables" (respond only if treated), skip:\n  sure things (buy anyway), lost causes (never buy), sleeping dogs (treatment BACKFIRES).\nmethods: two-model (T-learner), single model with treatment feature (S-learner), X-learner,\n         causal forests, uplift trees (split to maximise effect difference, not outcome purity).\neval: you cannot see the counterfactual per person → use Qini / uplift curves on a randomised\n      holdout (measure incremental outcome by decile of predicted uplift).</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A vaccine trial vs a marketing campaign.</b> The trial wants the average effect. The campaign wants to spend its budget only on the people whose behaviour the coupon actually changes — that is uplift.</p></div>',
    try: [['📖 CausalML / EconML (uplift libraries)', 'https://github.com/uber/causalml', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: A dashboard shows users who use feature X churn less. Can you tell PM "X reduces churn"?\nA: Not from this. Selection/confounding: engaged users both use X and churn less anyway. You need\n   an RCT (ship X to a random half) or a credible quasi-experiment (DiD, IV, RDD, propensity\n   methods with an unconfoundedness argument).\n\nQ: What is a confounder, and what is a collider? Why treat them differently?\nA: A confounder causes both treatment and outcome — you MUST adjust for it to remove bias. A\n   collider is a common EFFECT of two variables — conditioning on it CREATES a spurious\n   association. Draw the DAG and apply the backdoor criterion.\n\nQ: Explain difference-in-differences and its key assumption.\nA: Effect ≈ (post - pre in treated) - (post - pre in control). It nets out fixed differences\n   between groups and common time trends. Key assumption: PARALLEL TRENDS — absent treatment, the\n   two groups would have moved in parallel. Check with pre-period data.\n\nQ: What makes a valid instrumental variable?\nA: Relevance (it actually shifts the treatment) and the exclusion restriction (it affects the\n   outcome ONLY through the treatment, not directly or via other paths), plus no confounding of\n   the instrument itself.\n\nQ: Propensity score matching removes confounding. True?\nA: Only OBSERVED confounding. It cannot fix unobserved confounders, and it depends on the\n   propensity model being right and on overlap/positivity holding.\n\nQ: Difference between ATE and CATE, and when do you need CATE?\nA: ATE is the population-average effect; CATE is the effect conditional on covariates. You need\n   CATE (uplift) for targeting decisions — spend the incentive only on those it actually moves.\n\nQ: How do you evaluate an uplift model with no per-person counterfactual?\nA: On a randomised holdout, sort by predicted uplift, and plot the incremental outcome (treated -\n   control) cumulatively by decile — the Qini / uplift curve; area above random = the model\'s value.</code></pre>',
    try: [['📖 EconML (Microsoft)', 'https://econml.azurewebsites.net/', 'o']] } ],
  quiz: [
    { q: 'An analysis finds newsletter subscribers have 2x lifetime value. Marketing wants to conclude the newsletter causes higher LTV. Main flaw?', opts: ['Sample too small', 'Confounding / self-selection — people who subscribe are already more engaged; without randomisation or a valid quasi-experiment you cannot attribute the LTV gap to the newsletter', 'LTV is the wrong metric', 'They should use a t-test'], ok: 1, why: 'Subscription is chosen, not assigned. Engagement drives both subscribing and LTV. You need an RCT (randomly send the newsletter) or a design like DiD/IV/propensity with a defensible unconfoundedness argument.' },
    { q: 'Difference-in-differences relies most critically on which assumption?', opts: ['Equal group sizes', 'Parallel trends — absent the intervention, treated and control groups would have followed the same trajectory', 'Normally distributed outcomes', 'Random treatment assignment'], ok: 1, why: 'DiD subtracts out fixed group differences and a common time trend. Its validity hinges on the counterfactual trends being parallel, which you support (not prove) with pre-treatment data.' },
    { q: 'For deciding WHO should receive a retention offer (limited budget), which do you need?', opts: ['The average treatment effect (ATE)', 'The conditional average treatment effect (CATE) / uplift, to target users whose behaviour the offer actually changes', 'Only the outcome model P(churn)', 'A bigger control group'], ok: 1, why: 'A positive ATE says the offer works on average, not on whom. Uplift/CATE modelling ranks users by their individual incremental response so you spend on "persuadables" and skip sure-things, lost-causes, and sleeping-dogs.' } ] },

/* ============================ 13. ML SYSTEM DESIGN FRAMEWORK ============================ */
{ num: 13, emoji: '🏛️', title: 'ML System Design — The Framework', layer: 'Applied Systems',
  tagline: 'A repeatable 8-step structure for the design round, plus a worked example end to end.',
  apps: ['the ML system design interview', 'writing a design doc', 'scoping a new ML project'],
  levels: [
  { html:
    '<p>The design round is not "which model" — it is "can you design a system that reliably produces business value". Use a fixed skeleton so you never freeze.</p>' +
    '<pre><code>1 CLARIFY     users, use case, scale (QPS, #items, data volume), latency budget, online vs batch,\n              what "good" means, constraints (cost, privacy, on-device?)\n2 METRIC      business KPI → offline ML metric (a proxy) → online metric → GUARDRAILS\n3 DATA        sources, label definition + how obtained + delay + bias, volume, freshness, PII\n4 FEATURES    candidate signals, cross features, leakage + point-in-time, feature store\n5 MODEL       baseline (heuristic / logistic / GBM) FIRST → then the real model; justify the family\n6 TRAINING    split strategy (time-based? grouped?), retrain cadence, cold start, class imbalance\n7 SERVING     architecture (retrieval + ranking?), batch vs online, latency, fallback, caching\n8 MONITOR     data/pred drift, delayed-label metrics, A/B for launch, alerting, rollback, iteration</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A pre-flight checklist.</b> Pilots do not improvise the order; they run the list so nothing critical is skipped under pressure. Same list, every design question.</p></div>',
    try: [['📖 Chip Huyen: Designing ML Systems', 'https://huyenchip.com/machine-learning-systems-design/toc.html', 'o']] },
  { html:
    '<p><b>Capacity / latency reasoning</b> — show the numbers:</p>' +
    '<pre><code>estimate: 100M DAU · 20 requests/user/day ≈ 2B req/day ≈ ~23k QPS avg, ~5x peak ≈ 115k QPS\nlatency budget: 200 ms end-to-end → ~50 ms for the model → rules out scoring 1M items with a deep net\n  → retrieval (ANN, ~10 ms) narrows to ~500, ranker scores 500 in ~30 ms\nstorage: 1B items · 256-d fp16 embeddings ≈ 512 GB → shard the ANN index\nfreshness: does the model need real-time features (last click 2s ago)? → online feature store + streaming\ncost: GPU vs CPU inference; batch offline where latency allows (email digests, precomputed feeds)</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A kitchen at dinner rush.</b> You cannot cook every dish to order in 5 minutes. Prep components ahead (precompute features/embeddings), keep the hot line small (retrieval), and only the final plating is bespoke (ranking).</p></div>',
    try: [['📖 System Design Primer (numbers to know)', 'https://github.com/donnemartin/system-design-primer', 'o']] },
  { html:
    '<p><b>Worked example — "Design a fraud detection system for payments":</b></p>' +
    '<pre><code>1 clarify: score each transaction in &lt; 100 ms, block/allow/step-up; ~5k TPS; cost of a false\n           block (lost sale + angry customer) vs a false allow (chargeback + fines)\n2 metric:  offline: PR-AUC / recall at a fixed low false-positive rate. online: $ fraud caught,\n           block rate, customer complaints (guardrail), approval rate (guardrail)\n3 data:    transactions + labels from chargebacks (arrive WEEKS later → label delay) + manual\n           review outcomes; heavy class imbalance (~0.1% fraud); adversary adapts → drift\n4 features: amount, velocity (txns/user/hour), device/IP reputation, geo mismatch, merchant risk,\n           time-since-signup, graph features (shared card/device). STRICT point-in-time.\n5 model:   baseline rules + logistic regression → gradient boosting (handles interactions, missing,\n           imbalance via scale_pos_weight); a graph/deep model later\n6 training: time-based split; retrain weekly (adversary drift); reserve recent weeks for eval;\n           handle label delay (only use transactions old enough to have matured labels)\n7 serving: online, low latency; real-time velocity features from a streaming store; rules engine\n           in front for hard blocks; fallback to rules if the model service is down; human review\n           queue for the uncertain band\n8 monitor: score distribution drift, feature drift, precision/recall as labels mature, alert on\n           block-rate spikes, champion/challenger, fast rollback</code></pre>',
    try: [['📖 Real-time fraud detection architectures (search: Stripe/Uber blogs)', 'https://stripe.com/blog/how-we-built-it-stripe-radar', 'o']] },
  { html:
    '<p>Tradeoffs to name out loud (this is the senior signal):</p>' +
    '<pre><code>• accuracy vs latency vs cost — a deeper model may not fit the budget\n• online vs batch inference — freshness vs simplicity/cost\n• precision vs recall — set by the business cost matrix, not 0.5\n• build vs buy — a managed API vs owning the pipeline\n• one big model vs retrieval+ranking — simplicity vs scale\n• real-time features vs point-in-time batch — power vs infra complexity + skew risk\n• interpretability vs raw performance — regulated domains, debuggability\n• retrain often (fresh, drift-resistant) vs rarely (stable, cheap, auditable)\nAlways end with: "how do we know it is working in prod, and how do we roll back?"</code></pre>',
    try: [['📘 Part 2: deterministic pipelines & observability', '../learn2/#ch10', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: First questions you ask for ANY "design an ML system for X"?\nA: Who are the users and what decision does the model drive? Scale (QPS, #items, data size)?\n   Latency budget and online-vs-batch? What does success mean as a metric, and what are the\n   guardrails? What data and labels exist, and how delayed/biased are the labels? Constraints\n   (cost, privacy, on-device, regulation)?\n\nQ: Why start with a baseline?\nA: It sets the bar, exposes data/label/feature problems cheaply, is often 80% of the value, and\n   is what a complex model must beat to justify its cost and risk.\n\nQ: Batch vs online inference — how do you choose?\nA: Online when predictions depend on fresh request-time signals or must respond to user actions\n   within a latency budget. Batch when inputs change slowly and you can precompute (daily digests,\n   feed pre-materialisation) — simpler, cheaper, easier to monitor.\n\nQ: How do you handle a label that only arrives weeks later (chargebacks, churn)?\nA: Train only on rows old enough for labels to mature; monitor a leading proxy in the meantime;\n   accept a retrain lag; use manual-review labels as a faster (biased) signal, corrected for.\n\nQ: The design round asks about scale. What do you actually say?\nA: Estimate QPS from DAU x requests, apply a peak multiplier, derive the per-component latency\n   budget, and show that it forces the architecture (e.g. retrieval + ranking, precomputed\n   features, index sharding) rather than reciting a model name.\n\nQ: How do you launch the model safely?\nA: Shadow mode (log predictions, do not act) → canary on a small % → A/B vs the incumbent on the\n   online metric + guardrails → gradual ramp → keep the old model as an instant rollback.</code></pre>',
    try: [['📖 ML System Design case studies (evidentlyai / applyingml)', 'https://applyingml.com/', 'o']] } ],
  quiz: [
    { q: 'In an ML system design round, before proposing a model you should establish...', opts: ['The exact neural architecture', 'Users/decision, scale and latency budget, the success + guardrail metrics, and the label situation', 'The cloud provider', 'The programming language'], ok: 1, why: 'The round grades scoping and lifecycle thinking. Constraints and metrics determine the architecture; the model family is a late, justified choice, and a baseline comes first.' },
    { q: 'A transaction-fraud model\'s labels (chargebacks) arrive 6-8 weeks after the transaction. How should training handle this?', opts: ['Use the most recent week of data for training', 'Train only on transactions old enough that their labels have matured, and monitor a faster leading proxy in the meantime', 'Assume all recent transactions are non-fraud', 'Ignore the delay'], ok: 1, why: 'Recent transactions have incomplete labels (fraud not yet reported), which biases training toward "non-fraud". Restrict training to a matured-label window and use proxies/manual review for freshness.' },
    { q: 'You must serve recommendations for 1B items within a 50ms model budget. What does that imply architecturally?', opts: ['Score all 1B items with a deep network', 'A cheap retrieval stage (e.g. ANN over item embeddings) to a few hundred candidates, then a heavier ranker on those', 'Use a bigger GPU', 'Batch everything nightly regardless of freshness'], ok: 1, why: 'Full-catalogue scoring is infeasible in the latency budget. The standard pattern is sub-linear retrieval (ANN / inverted index) to shortlist candidates, then an expensive ranking model on the shortlist.' } ] },

/* ============================ 14. DATA & SQL FOR DS INTERVIEWS ============================ */
{ num: 14, emoji: '🗃️', title: 'Data & SQL for DS Interviews', layer: 'Applied Systems',
  tagline: 'The SQL screen: joins, GROUP BY, window functions, and the funnel / retention / top-N patterns.',
  apps: ['the live SQL round', 'building analysis datasets', 'metric / dashboard definitions'],
  levels: [
  { html:
    '<p>Most DS SQL screens test the same handful of shapes. Master these and you pass:</p>' +
    '<pre><code>JOINs           INNER / LEFT / (rarely) FULL; watch key duplication → row fan-out\nGROUP BY + agg  COUNT / SUM / AVG / COUNT(DISTINCT); HAVING filters groups (after aggregation)\nCTEs (WITH)     name intermediate steps → readable, debuggable, no nested-subquery spaghetti\nWINDOW fns      aggregate WITHOUT collapsing rows: ROW_NUMBER / RANK / DENSE_RANK,\n                SUM(...) OVER (PARTITION BY ... ORDER BY ...), LAG / LEAD, running totals\ndate logic      DATE_TRUNC, date diffs, generate a date spine for gap-free time series</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>GROUP BY squashes a table into a summary; a WINDOW function writes the summary in the margin next to every original row.</b> "Each employee\'s salary AND their department average" needs a window, not a GROUP BY.</p></div>',
    try: [['📘 Part 3: pandas is SQL-shaped (groupby, merge, window)', '../learn3/#ch2', 'o']] },
  { html:
    '<p><b>Window functions</b> — the round that separates candidates:</p>' +
    '<pre><code>-- top 3 products by revenue PER category\nSELECT * FROM (\n  SELECT category, product, revenue,\n         ROW_NUMBER() OVER (PARTITION BY category ORDER BY revenue DESC) AS rn\n  FROM sales\n) t WHERE rn &lt;= 3;\n\n-- 7-day rolling active users\nSELECT day,\n       SUM(active) OVER (ORDER BY day ROWS BETWEEN 6 PRECEDING AND CURRENT ROW) AS wau\nFROM daily;\n\n-- days since a user\'s previous order\nSELECT user_id, order_ts,\n       order_ts - LAG(order_ts) OVER (PARTITION BY user_id ORDER BY order_ts) AS gap\nFROM orders;</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>PARTITION BY is "do this calculation separately for each group"; ORDER BY inside OVER is "and in this sequence".</b> ROW_NUMBER + PARTITION + ORDER is the "top N per group" swiss-army knife.</p></div>',
    try: [['📖 Mode: SQL window functions tutorial', 'https://mode.com/sql-tutorial/sql-window-functions/', 'o']] },
  { html:
    '<p>The classic analysis patterns:</p>' +
    '<pre><code>FUNNEL:  count users reaching each step (LEFT JOIN each step table to the previous, or\n         conditional aggregation: COUNT(DISTINCT CASE WHEN step=\'checkout\' THEN user_id END))\nRETENTION (cohort):  group users by signup week; for each later week, what fraction returned\n         → self-join activity to the cohort, or generate a week spine and LEFT JOIN\nSESSIONISATION:  new session when the gap since the previous event &gt; 30 min\n         → LAG(ts), flag gap, SUM(flag) OVER (...) as session_id\nRUNNING / CUMULATIVE metrics, month-over-month growth (LAG on the aggregate),\nDEDUP:  ROW_NUMBER() OVER (PARTITION BY natural_key ORDER BY updated_at DESC) = 1</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>"What is week-4 retention for the Jan cohort?": bucket users by signup week, LEFT JOIN their activity 4 weeks later, <code>COUNT(DISTINCT returning) / COUNT(DISTINCT cohort)</code>. Watch the LEFT JOIN so non-returners stay in the denominator.</p></div>',
    try: [['📖 StrataScratch / LeetCode DB (practice)', 'https://platform.stratascratch.com/coding', 'o']] },
  { html:
    '<p><b>Data modelling</b> concepts that come up in the DS/analytics-engineer round:</p>' +
    '<pre><code>STAR SCHEMA:  a central FACT table (events/measures, one row per transaction) + DIMENSION tables\n              (descriptive: user, product, date). Denormalised for fast analytical queries.\nGRAIN:        the meaning of one row in the fact table — define it before anything else\n              ("one row per order line item"). Mixing grains double-counts.\nSLOWLY CHANGING DIMENSIONS:  keep history of dimension changes (SCD type 2 = a new row per version\n              with valid_from/valid_to) so historical facts join to the attributes-as-they-were.\nOLTP vs OLAP:  row-store, normalised, transactional vs column-store, denormalised, analytical.\ndbt:          SQL + Jinja to build tested, documented, version-controlled transformation DAGs.\ndata quality:  not-null / unique / accepted-values / referential-integrity tests on every model.</code></pre>',
    try: [['📖 The Data Warehouse Toolkit (Kimball) / dbt docs', 'https://docs.getdbt.com/', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: WHERE vs HAVING?\nA: WHERE filters rows BEFORE aggregation; HAVING filters groups AFTER aggregation. You cannot\n   put an aggregate in WHERE.\n\nQ: Get the 2nd highest salary per department.\nA: SELECT dept, salary FROM (\n     SELECT dept, salary, DENSE_RANK() OVER (PARTITION BY dept ORDER BY salary DESC) r\n     FROM emp) t WHERE r = 2;\n   (DENSE_RANK handles ties as "same rank"; ROW_NUMBER would arbitrarily pick one.)\n\nQ: RANK vs DENSE_RANK vs ROW_NUMBER?\nA: ROW_NUMBER: unique 1..n, ties broken arbitrarily. RANK: ties share a rank, then it SKIPS\n   (1,1,3). DENSE_RANK: ties share a rank, no gaps (1,1,2).\n\nQ: A LEFT JOIN returned more rows than the left table. Why?\nA: The right table has multiple rows per join key → each left row fans out. Aggregate/dedupe the\n   right side first, or use a window to pick one row.\n\nQ: How do you compute a 7-day rolling average in SQL?\nA: AVG(metric) OVER (ORDER BY day ROWS BETWEEN 6 PRECEDING AND CURRENT ROW), on a gap-free date\n   spine (LEFT JOIN a generated calendar so missing days count as 0).\n\nQ: What is the "grain" of a table and why does it matter?\nA: What one row represents. If you join tables of different grains (orders vs order-items) without\n   care you multiply rows and double-count measures. Define and preserve grain deliberately.\n\nQ: How do you find the latest record per entity from a change log?\nA: ROW_NUMBER() OVER (PARTITION BY entity_id ORDER BY updated_at DESC) = 1, or a correlated\n   subquery on MAX(updated_at). Beware ties on the timestamp.</code></pre>',
    try: [['📖 SQLBolt / PostgreSQL Exercises', 'https://pgexercises.com/', 'o']] } ],
  quiz: [
    { q: 'You need each employee\'s salary alongside their department\'s average salary, keeping one row per employee. Which SQL feature?', opts: ['GROUP BY department', 'A window function: AVG(salary) OVER (PARTITION BY department)', 'A self-join with HAVING', 'DISTINCT'], ok: 1, why: 'GROUP BY collapses rows to one per department. A window function computes the partition aggregate but leaves every original row intact, so you get the per-employee row plus the department average.' },
    { q: 'To rank rows so ties get the same rank with NO gaps (1, 1, 2, 3), use...', opts: ['ROW_NUMBER()', 'RANK()', 'DENSE_RANK()', 'NTILE(4)'], ok: 2, why: 'ROW_NUMBER gives unique numbers (ties broken arbitrarily). RANK shares ranks but skips (1,1,3). DENSE_RANK shares ranks with no gaps (1,1,2).' },
    { q: 'In a star schema, what must you define first about the fact table?', opts: ['Its primary key type', 'Its grain — exactly what one row represents (e.g. one row per order line item)', 'Which BI tool will read it', 'The number of dimensions'], ok: 1, why: 'Grain is the contract of the fact table. All measures and dimension joins must be consistent with it; mixing grains (orders vs line items) silently double-counts sums and averages.' } ] },

/* ============================ 15. DISTRIBUTED & LARGE-SCALE TRAINING ============================ */
{ num: 15, emoji: '🖧', title: 'Distributed & Large-Scale Training', layer: 'Production',
  tagline: 'Data / model / pipeline / tensor parallelism, ZeRO/FSDP, and the Spark shuffle that kills your job.',
  apps: ['"train a model too big for one GPU"', 'TB-scale ETL on Spark', 'multi-node training infra'],
  levels: [
  { html:
    '<p>When one machine is not enough, you split one of three things: the <b>data</b>, the <b>model</b>, or the <b>computation graph</b>.</p>' +
    '<pre><code>DATA PARALLEL     every GPU has a FULL copy of the model, processes a different shard of the batch;\n                 gradients are averaged across GPUs (all-reduce) each step. Scales throughput.\nMODEL PARALLEL    the model is too big for one GPU → split its LAYERS across GPUs\n TENSOR PARALLEL  split individual WEIGHT MATRICES across GPUs (within a layer) — needs fast interconnect\n PIPELINE PARALLEL split layers into stages; micro-batches flow through like an assembly line\nusually COMBINED (3D parallelism) for the largest models.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>Data parallel = 8 accountants each doing 1/8 of the invoices with their own copy of the rulebook, then reconciling. Model parallel = one giant ledger too big for one desk, so pages are spread across 8 desks and the work passes down the line.</b></p></div>',
    try: [['📖 HuggingFace: model parallelism guide', 'https://huggingface.co/docs/transformers/perf_train_gpu_many', 'o']] },
  { html:
    '<p><b>Data parallel</b> in practice, and fitting bigger models:</p>' +
    '<pre><code>DDP (PyTorch DistributedDataParallel):  one process per GPU, overlap gradient all-reduce with\n  backward, near-linear scaling. (DataParallel — single-process, is deprecated/slow.)\nGRADIENT ACCUMULATION:  simulate a big batch by summing gradients over N micro-batches before\n  stepping → fit a large effective batch on small GPUs.\nMIXED PRECISION (AMP, bf16):  ~2x throughput + half the activation memory, negligible accuracy cost.\nZeRO / FSDP:  shard the OPTIMIZER STATE, then GRADIENTS, then PARAMETERS across GPUs, gathering\n  each shard just-in-time → train models far larger than one GPU\'s memory with ~data-parallel simplicity.\nGRADIENT / ACTIVATION CHECKPOINTING:  recompute activations in the backward pass instead of\n  storing them → trade compute for memory.\nCPU / NVMe OFFLOAD:  park rarely-touched state off-GPU.</code></pre>',
    try: [['📖 ZeRO paper (DeepSpeed)', 'https://arxiv.org/abs/1910.02054', 'o']] },
  { html:
    '<p><b>Spark internals</b> — the distributed-data-processing questions:</p>' +
    '<pre><code>PARTITIONS   the unit of parallelism; one task per partition. Too few → no parallelism; too many\n             → scheduling overhead.\nNARROW vs WIDE transforms:  map/filter (narrow, no data movement) vs groupByKey/join/repartition\n             (WIDE → a SHUFFLE: data crosses the network, sorted/hashed, written to disk). Shuffles\n             are the main cost — minimise them.\nSKEW         one key has 90% of the rows → one task runs forever. Fix: salt the key, broadcast the\n             small side, or use AQE skew-join handling.\nBROADCAST JOIN:  ship the small table to every executor → no shuffle of the big table.\nCACHE / PERSIST  a reused DataFrame; SPILL to disk when memory is tight.\nLAZY:  transformations build a DAG; nothing runs until an ACTION (count/collect/write).\ncollect() pulls all data to the driver → OOM the driver on big results. Use write() / take(n).</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>A Spark join runs for hours. The join key is <code>country</code> and 70% of rows are "US" → skew. Fix: broadcast the dimension table if small, or salt the "US" key into 100 sub-keys and un-salt after.</p></div>',
    try: [['📖 Spark: performance tuning', 'https://spark.apache.org/docs/latest/sql-performance-tuning.html', 'o']] },
  { html:
    '<p>Choosing scale, and the gotchas:</p>' +
    '<pre><code>• do you EVEN need a cluster? a single big box + Polars/DuckDB (Part 3 ch.16) or one multi-GPU\n  node with FSDP handles more than people think. Distribution adds failure modes and cost.\n• communication is the bottleneck: all-reduce bandwidth, interconnect (NVLink/InfiniBand vs PCIe/Ethernet)\n• bigger global batch → tune LR (linear scaling + warmup); too big can hurt final accuracy\n• stragglers & fault tolerance: checkpoint often; one dead node should not lose a week of training\n• data loading can be the limiter — shard the dataset, use enough workers, prefetch, use a fast format\n• reproducibility across nodes: seed per rank, deterministic ops, fixed data sharding\n• DEBUG small first: get it correct on 1 GPU, then 1 node, then multi-node.</code></pre>',
    try: [['📘 Part 1: KV-cache / compute cost intuition', '../learn/#ch8', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Data parallelism vs model parallelism — when do you need each?\nA: Data parallel when the model fits on one GPU and you want more throughput (each GPU: full\n   model, different data shard, gradients all-reduced). Model parallel (tensor/pipeline) when the\n   model itself does not fit — split weights/layers across GPUs. Large LLMs combine both.\n\nQ: What does ZeRO / FSDP do?\nA: Shards optimizer state, gradients, and parameters across data-parallel ranks instead of\n   replicating them, gathering each shard just in time. You get data-parallel simplicity but can\n   train models many times larger than one GPU\'s memory.\n\nQ: You increase the number of GPUs 8x but training only speeds up 4x. Likely causes?\nA: Communication overhead (gradient all-reduce, slow interconnect), input pipeline can\'t feed the\n   GPUs, small per-GPU batch, or synchronisation stragglers. Profile the step; overlap comms with\n   compute; increase batch; use a faster data format.\n\nQ: A Spark stage takes hours on one task while others finish fast. Diagnosis and fix?\nA: Data skew — one partition/key holds most of the rows. Fix: broadcast the small side of the\n   join, salt the hot key, enable adaptive query execution skew handling, or repartition.\n\nQ: Why avoid df.collect() in Spark?\nA: It pulls the entire result to the single driver JVM, which OOMs on large outputs. Use write()\n   to storage, or take(n) / show() for a peek.\n\nQ: What is a shuffle and why is it expensive?\nA: A wide transformation (join, groupBy, repartition) that redistributes data across the network\n   by key, with sort/hash and disk spill. It is the dominant cost of most Spark jobs — minimise\n   the number and size of shuffles.\n\nQ: Gradient accumulation — what problem does it solve?\nA: It lets you train with a large effective batch size when GPU memory only fits a small\n   micro-batch, by summing gradients over several micro-batches before the optimiser step.</code></pre>',
    try: [['📖 The Ultra-Scale Playbook (HF)', 'https://huggingface.co/spaces/nanotron/ultrascale-playbook', 'o']] } ],
  quiz: [
    { q: 'Your model does not fit in a single GPU\'s memory even with batch size 1. Which approach directly addresses this?', opts: ['More data-parallel replicas', 'Tensor/pipeline (model) parallelism and/or ZeRO/FSDP sharding of parameters, gradients and optimizer state', 'A larger learning rate', 'Gradient clipping'], ok: 1, why: 'Data parallelism replicates the whole model per GPU, so it cannot help when the model itself does not fit. You must partition the model (tensor/pipeline parallel) or shard its states across ranks (ZeRO/FSDP).' },
    { q: 'A Spark join on "country" has one task processing 70% of the rows. This is...', opts: ['Normal behaviour', 'Data skew — mitigate by broadcasting the small table, salting the hot key, or enabling AQE skew-join handling', 'A driver memory issue', 'Caused by too many partitions'], ok: 1, why: 'Skew concentrates rows for a few keys into one partition/task while others idle. Broadcast joins avoid shuffling the big table; key salting spreads the hot key across many partitions; adaptive execution can split skewed partitions.' },
    { q: 'You scale from 1 to 16 GPUs (data parallel) and throughput scales only ~7x. Least likely cause?', opts: ['Gradient all-reduce communication overhead on a slow interconnect', 'The input data pipeline cannot feed 16 GPUs fast enough', 'Per-GPU batch became tiny, so compute per step is dominated by overhead', 'The optimizer is Adam instead of SGD'], ok: 3, why: 'Sub-linear scaling is almost always communication, input-pipeline throughput, or too-small per-GPU work / stragglers. The choice of Adam vs SGD does not explain a scaling shortfall across GPUs.' } ] },

/* ============================ 16. ML IN PRODUCTION: MONITORING, DRIFT, GOVERNANCE ============================ */
{ num: 16, emoji: '📟', title: 'ML in Production — Monitoring, Drift & Governance', layer: 'Production',
  tagline: 'Deploy is the start. Data drift vs concept drift, delayed labels, retraining triggers, and rollback.',
  apps: ['"how do you know your model is still good?"', 'on-call for ML services', 'model governance & audits'],
  levels: [
  { html:
    '<p>A model is a photo of a world that keeps moving. Accuracy at launch tells you nothing about accuracy next quarter. Production ML is mostly <b>watching for decay and reacting</b>.</p>' +
    '<pre><code>DATA / COVARIATE DRIFT   P(X) changes — the input distribution shifts (new user mix, a new device,\n                        a logging change). The model may still be fine, but it is now extrapolating.\nCONCEPT DRIFT            P(Y | X) changes — the RELATIONSHIP shifts (fraud tactics evolve, a\n                        promotion changes buying behaviour). The model is now wrong.\nLABEL / PREDICTION DRIFT the distribution of the model\'s OUTPUT shifts — often the first visible sign.\nTRAIN/SERVE SKEW        features computed differently offline vs online — a bug, not drift.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A map of a coastline that is slowly eroding.</b> The map (model) was accurate when drawn. Nobody redrew it, and now the road on the map runs into the sea.</p></div>',
    try: [['📘 Part 2: observability, traces, online eval', '../learn2/#ch14', 'o']] },
  { html:
    '<p><b>What to monitor</b>, and how:</p>' +
    '<pre><code>INPUTS   per-feature: mean/std/quantiles, % missing, cardinality, new categories.\n         distribution shift: PSI (Population Stability Index; &gt;0.2 = investigate, &gt;0.25 = act),\n         KS test, or a "drift classifier" (can a model tell train from live? high AUC = drift).\nOUTPUTS  prediction distribution, score calibration, % in each decision bucket, abstention rate.\nQUALITY  once labels arrive: the real metric, sliced by segment; a leading proxy in the meantime.\nOPS      latency p50/p95/p99, error rate, throughput, cost per prediction, cache hit rate.\nBUSINESS the KPI the model exists to move; guardrails.\nALERT on thresholds; page on the ones that need a human now.</code></pre>' +
    '<div class="reallife"><span class="lbl">🏭 Scenario</span><p>A pricing model\'s inputs look normal but revenue-per-visit drops. Labels (purchases) are same-day, so the sliced metric shows the "mobile / new-user" segment regressed after an app release changed a feature\'s meaning — train/serve skew, not drift. Rollback + fix the feature.</p></div>',
    try: [['📖 Evidently AI (open-source drift monitoring)', 'https://github.com/evidentlyai/evidently', 'o']] },
  { html:
    '<p><b>Retraining and safe deployment:</b></p>' +
    '<pre><code>RETRAIN TRIGGERS:  scheduled (daily/weekly) · drift threshold crossed · metric below SLO ·\n                  a data/schema change · enough new labels accrued.\nautomated retrain pipeline: pull fresh data → validate (schema, distribution, volume) → train →\n  evaluate on a rolling held-out set + slices → gate: only promote if it beats the incumbent AND\n  passes guardrails → register the new version with lineage.\nDEPLOY:  shadow (log only) → canary (small %) → A/B vs incumbent → gradual ramp.\n         keep the previous model hot for INSTANT ROLLBACK. blue-green or feature-flagged.\nMODEL REGISTRY:  versioned artifacts + the data snapshot, code SHA, metrics, and approval trail.\nreproducibility: pin data version + code + environment so any past model can be rebuilt.</code></pre>',
    try: [['📖 Made With ML: monitoring & CI/CD for ML', 'https://madewithml.com/courses/mlops/monitoring/', 'o']] },
  { html:
    '<p><b>Governance</b> — increasingly asked, especially for regulated domains:</p>' +
    '<pre><code>MODEL CARDS       intended use, training data, evaluation (incl. by subgroup), limitations, ethical\n                 considerations — a spec sheet that ships with the model.\nLINEAGE / AUDIT   which data → which features → which model version → which decisions. Reproducible.\nAPPROVALS         a human sign-off before a model touches customers; documented risk assessment.\nEXPLAINABILITY    for adverse decisions (credit, hiring): per-decision reasons (SHAP), monotonic\n                 constraints where required.\nINCIDENT RESPONSE a runbook: detect → mitigate (rollback / fall back to rules) → root-cause →\n                 postmortem → add a monitor/test so it cannot recur silently.\nREGULATION        EU AI Act risk tiers, GDPR (right to explanation, data minimisation),\n                 sector rules (finance model risk management, health).</code></pre>',
    try: [['📖 Model Cards for Model Reporting (Mitchell et al.)', 'https://arxiv.org/abs/1810.03993', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Data drift vs concept drift — define and give an example of each.\nA: Data drift: P(X) changes (input mix shifts) — e.g. a marketing push brings a new user segment.\n   Concept drift: P(Y|X) changes (the mapping shifts) — e.g. fraudsters change tactics so the same\n   features now imply different risk. Data drift may be harmless; concept drift makes the model wrong.\n\nQ: Your labels arrive weeks late. How do you monitor quality in the meantime?\nA: Track leading proxies (early engagement, manual-review agreement), monitor input and prediction\n   distributions for drift, and compute the true sliced metric on a rolling basis as labels mature.\n\nQ: How do you detect input drift without labels?\nA: Per-feature statistics and distribution tests (PSI, KS) against a training reference; or train\n   a classifier to distinguish training vs live data — a high AUC means the distributions have\n   diverged.\n\nQ: What triggers a retrain?\nA: A schedule, a drift metric crossing a threshold, the online metric dropping below its SLO, a\n   schema/upstream data change, or accumulation of enough new labels. Retrains go through the same\n   validation + eval gate + canary as any deploy.\n\nQ: How do you roll out a new model safely and roll it back fast?\nA: Shadow → canary (small %) → A/B vs incumbent on the online metric + guardrails → ramp. Keep\n   the previous version deployed behind a flag / blue-green so rollback is a config change, not a\n   redeploy.\n\nQ: Model metric looks fine but the business KPI dropped. What do you check?\nA: Slice the metric by segment (Simpson\'s paradox), check for train/serve skew from a recent\n   feature change, verify the label pipeline, and confirm the online metric definition matches the\n   offline proxy. The aggregate can hide a broken segment.\n\nQ: What goes in a model card and why?\nA: Intended use, data, evaluation (including per-subgroup), limitations, and ethical notes — so\n   downstream users and auditors know where the model is valid and where it is not.</code></pre>',
    try: [['📖 Google: MLOps levels & continuous delivery', 'https://cloud.google.com/architecture/mlops-continuous-delivery-and-automation-pipelines-in-machine-learning', 'o']] } ],
  quiz: [
    { q: 'The input feature distributions are unchanged, but the model\'s accuracy has dropped over three months. This is most consistent with...', opts: ['Data / covariate drift', 'Concept drift — P(Y|X) has changed, so the learned relationship no longer holds', 'Train/serve skew', 'Overfitting during original training'], ok: 1, why: 'If P(X) is stable but performance falls, the relationship between features and target has shifted (e.g. behaviour or adversaries changed). That is concept drift and typically requires retraining on recent data.' },
    { q: 'You have no fresh labels yet. Which is a valid way to detect that a deployed model may be degrading?', opts: ['Wait for labels; nothing can be done', 'Monitor input feature distributions and the prediction-score distribution for shift (PSI/KS, or a train-vs-live drift classifier), and watch leading proxies', 'Retrain on the training set again', 'Increase the model size'], ok: 1, why: 'Label-free monitoring watches the distributions you can see now: per-feature stats, PSI/KS vs a reference, prediction distribution, and leading behavioural proxies. These flag problems before the delayed ground truth confirms them.' },
    { q: 'What is the safest deployment sequence for a new model version?', opts: ['Replace the old model everywhere at once', 'Shadow (log only) → canary on a small % → A/B vs the incumbent on the online metric + guardrails → gradual ramp, keeping the old version hot for instant rollback', 'A/B test for a year before any traffic', 'Deploy to the largest customer first'], ok: 1, why: 'Progressive delivery limits blast radius: shadow catches gross errors with no user impact, canary/A-B measures real effect against the incumbent and guardrails, and keeping the previous version deployed makes rollback a flag flip.' } ] },

/* ============================ 17. RESPONSIBLE AI ============================ */
{ num: 17, emoji: '🧭', title: 'Responsible AI — Fairness, Privacy & Robustness', layer: 'Responsible AI',
  tagline: 'Fairness definitions that cannot all hold at once, privacy techniques, and adversarial robustness.',
  apps: ['fairness / bias audits', 'privacy-preserving ML', 'regulated & high-stakes deployments'],
  levels: [
  { html:
    '<p>Bias enters ML from the world, not the algorithm: <b>the data reflects historical and societal inequities, and the model faithfully learns them.</b></p>' +
    '<pre><code>SOURCES  historical bias (past decisions were unfair) · representation bias (a group is\n         under-sampled) · measurement bias (the label is a flawed proxy — "arrested" ≠ "committed\n         a crime") · aggregation bias (one model for groups that need different ones) ·\n         deployment bias (used differently than intended) · feedback loops (ch.9/10).\n"just remove the sensitive attribute" DOES NOT WORK — other features (zip code, name, purchase\nhistory) are PROXIES for it. This is "fairness through unawareness" and it fails.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A photocopier for the status quo.</b> Feed it a biased hiring history and it will produce more of the same, faster and with a veneer of objectivity — "the algorithm decided".</p></div>',
    try: [['📖 Fairness and Machine Learning (free book)', 'https://fairmlbook.org/', 'o']] },
  { html:
    '<p><b>Fairness metrics</b> — and the impossibility result:</p>' +
    '<pre><code>DEMOGRAPHIC PARITY   equal positive-prediction RATE across groups:  P(ŷ=1 | A=a) equal for all a\nEQUAL OPPORTUNITY    equal TRUE-POSITIVE rate:  P(ŷ=1 | Y=1, A=a) equal\nEQUALISED ODDS       equal TPR AND FPR across groups\nCALIBRATION          a score of 0.7 means 70% actual positives, IN EVERY group\nPREDICTIVE PARITY    equal precision across groups\n\nIMPOSSIBILITY (Kleinberg / Chouldechova): if base rates differ between groups, you CANNOT\nsimultaneously have calibration AND equalised odds (except in trivial cases). You must choose\nwhich fairness criterion matches the harm, with stakeholders — it is a values decision, not a\nmetric to maximise.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A blanket that is too small.</b> Pull it over "equal error rates" and "calibrated scores" pops out. When the underlying base rates differ, some fairness definitions are mathematically mutually exclusive.</p></div>',
    try: [['📖 Fairness Definitions Explained (Verma & Rubin)', 'https://fairware.cs.umass.edu/papers/Verma.pdf', 'o']] },
  { html:
    '<p><b>Mitigation</b> (at three stages) and <b>privacy</b>:</p>' +
    '<pre><code>PRE-PROCESS   reweight / resample groups, learn fair representations, fix labels/features\nIN-PROCESS    add a fairness constraint or penalty to the training objective (adversarial debiasing)\nPOST-PROCESS  group-specific decision thresholds to equalise the chosen metric (simple, effective,\n              but uses the sensitive attribute at decision time — may be legally restricted)\nalways: report metrics BY SUBGROUP, not just the average; audit with a tool (Fairlearn, AIF360).\n\nPRIVACY:\n  PII handling + minimisation + retention limits (GDPR)\n  k-ANONYMITY / l-diversity — each record indistinguishable among k others (weak against linkage)\n  DIFFERENTIAL PRIVACY — add calibrated noise so any one person\'s presence barely changes the\n    output; a formal (ε, δ) guarantee; used in aggregate stats and DP-SGD training (accuracy cost)\n  FEDERATED LEARNING — train on-device, send gradients/updates not raw data (+ secure aggregation)</code></pre>',
    try: [['📖 Fairlearn / AIF360', 'https://fairlearn.org/', 'o']] },
  { html:
    '<p><b>Robustness and other harms:</b></p>' +
    '<pre><code>ADVERSARIAL EXAMPLES  a tiny, targeted input perturbation flips the prediction (imperceptible to\n  humans). Defences: adversarial training, input sanitisation, ensembles — none complete.\nDISTRIBUTION SHIFT robustness  the model should degrade gracefully, and flag when it is out of\n  its competence (OOD detection, abstention).\nSPURIOUS CORRELATIONS  "classifies husky vs wolf by whether there is snow" — the model latched onto\n  a shortcut. Fix: better data, group DRO, feature audits (SHAP), stress tests.\nLLM-SPECIFIC (Part 2)  jailbreaks, prompt injection, hallucination, toxic / unsafe generation,\n  training-data extraction, copyright — mitigated with guardrails, red-teaming, RLHF/DPO, evals.\nENVIRONMENTAL & ACCESS  compute cost and energy; who can afford to build vs who bears the risk.</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><b>A student who aces the test by memorising that "questions with a diagram are answer C".</b> Right for the wrong reason — and it fails the moment the pattern changes.</p></div>',
    try: [['📖 Adversarial examples (Goodfellow et al.)', 'https://arxiv.org/abs/1412.6572', 'o']] },
  { html:
    '<p><b>Interview drill</b></p>' +
    '<pre><code>Q: Your model is unfair to a group. Why does dropping the sensitive attribute not fix it?\nA: Other features are proxies (zip code, name, device, browsing history) that let the model\n   reconstruct group membership. "Fairness through unawareness" fails; you need to measure and\n   constrain outcomes by group.\n\nQ: Name three fairness definitions and explain why you cannot satisfy all of them.\nA: Demographic parity (equal positive rate), equalised odds (equal TPR and FPR), calibration\n   (scores mean the same thing per group). The impossibility theorem: when base rates differ\n   across groups, calibration and equalised odds cannot both hold, so you must choose based on the\n   real-world harm.\n\nQ: Where can you intervene to reduce bias?\nA: Pre-processing (reweight/relabel/fair representations), in-processing (fairness constraint or\n   adversarial term in the loss), or post-processing (group-specific thresholds). Trade-offs in\n   effectiveness, legality of using the attribute, and transparency.\n\nQ: What does differential privacy guarantee?\nA: That the presence or absence of any single individual\'s data changes the output distribution by\n   at most a bounded factor (ε), so an observer cannot confidently infer whether someone was in\n   the dataset. It costs accuracy (noise) and a privacy budget across queries.\n\nQ: What is federated learning and when is it used?\nA: Training across many devices/silos that keep their raw data local; only model updates\n   (ideally with secure aggregation + DP) are shared. Used when data cannot be centralised —\n   phones, hospitals, banks.\n\nQ: How do you catch a model relying on a spurious shortcut?\nA: SHAP / saliency audits, evaluate on subgroups and shifted/stress-test sets, group DRO,\n   counterfactual tests (change only the spurious feature), and out-of-distribution checks.\n\nQ: What is a model card and who is it for?\nA: A short document shipped with the model stating intended use, training data, evaluation\n   (including per-subgroup), limitations and ethical considerations — for downstream developers,\n   auditors, and regulators to judge where the model is and is not appropriate.</code></pre>',
    try: [['📖 NIST AI Risk Management Framework', 'https://www.nist.gov/itl/ai-risk-management-framework', 'o']] } ],
  quiz: [
    { q: 'Removing "gender" from the feature set still leaves the model biased by gender. Why?', opts: ['The model has a bug', 'Other features (name, zip code, purchase history, device) act as proxies that reconstruct the protected attribute — "fairness through unawareness" is insufficient', 'Bias only comes from the label', 'You must also remove the target'], ok: 1, why: 'Protected attributes are correlated with many permissible features. A model can and will recover group membership from proxies, so you must measure outcomes by group and apply an explicit fairness intervention.' },
    { q: 'The fairness "impossibility theorem" says that when group base rates differ, you generally cannot simultaneously have...', opts: ['Accuracy and precision', 'Calibration and equalised odds', 'Low bias and low variance', 'Demographic parity and high recall'], ok: 1, why: 'Kleinberg et al. / Chouldechova showed that with unequal base rates, a score cannot be both calibrated within every group and satisfy equalised odds (equal TPR and FPR), except in degenerate cases. The choice of criterion is a values decision.' },
    { q: 'A hospital consortium wants to train a shared model but cannot pool patient records. Best approach?', opts: ['Anonymise by removing names and pool the data', 'Federated learning — train locally at each site and share only model updates, ideally with secure aggregation and differential privacy', 'Each hospital trains its own model and they average predictions ad hoc', 'Upload everything to one cloud bucket with encryption at rest'], ok: 1, why: 'Federated learning keeps raw records on-site and exchanges only model updates; combined with secure aggregation and DP it lets multiple silos benefit from a joint model without centralising sensitive data. Name removal alone is not sufficient anonymisation.' } ] }

];
