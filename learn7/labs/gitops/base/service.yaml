apiVersion: v1
kind: Service
metadata:
  name: ranker
  labels:
    app.kubernetes.io/name: ranker
spec:
  selector:
    app.kubernetes.io/name: ranker
  ports:
    - name: http
      port: 80
      targetPort: http
